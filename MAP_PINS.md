# 地図の共有ピン

地図の任意の場所を押すか焚き火を選び、「ここへ行こう」で同じ部屋の仲間へ知らせる。選択やドラッグだけでは送信しない。地図を動かす矢印と「中央にピンを打つ」を使えばコントローラーでも場所を指定できる。キーボードでは地図をクリックして矢印で移動、Enterで中央を選び、共有ボタンで決定する。

地図とミニマップに本人の色の菱形を表示。ミニマップの範囲外なら、その方向の端に表示する。「仲間のピン」の名前を選ぶと地図がその場所を中心に表示する。受信時は「名前：ここへ行こう」の通知と会話履歴を残す。

1人1本、新しい共有で置き換え。「自分のピンを消す」で削除。5分経過か本人の退出で消える。一時的な誘いとして扱い、サーバーの保存・再起動には残さない。途中参加には有効なピンを配信する。

サーバーが有限の座標・世界の範囲・接続本人を検査し、本人の名前と色、5分の期限を付ける。送信は1.5秒間隔。削除で送信間隔を回避できず、他人のピンを削除できない。既存の通信制限も適用する。持ち物・元気・位置・移動・作業・ワープ条件を変えない。

実装は `shared/map-pins.mts`、`application/protocol.mts`・`game-core.mts`・`snapshot.mts`、`src/map-screen.ts`・`map-warp-ui.ts`・`world-map.ts`・`main.ts`。

検証：全470テスト（新規5）、strictを含むビルド・型検査、248 JavaScript構文・依存境界を通過。`tests/map-pins.test.mjs` は偽造・無効座標、部屋の分離、5人、置き直し・連打制限・本人削除、途中参加・期限・退出、保存への非混入を検査。期限はテスト用時計の準備値。

`scripts/qa-map-pins.mjs` は実Chrome2画面で、地図クリックの未送信・共有・名前通知・ミニマップ・仲間一覧・置き直し・ドラッグの未送信・本人削除・キーボード決定・模擬Gamepadの決定を確認。1440×900、390×844、844×390を画像確認し、ブラウザーエラー0。人物の位置・持ち物は準備値なしで前後一致。名前・ガイド設定・Gamepad APIのみ準備値。実機Gamepad・タッチ端末・長時間負荷は未検証。記録は `output/playwright/map-pins/`。

通常3000番はPID35152（開始2026-09-10 12:21:27）のまま。自動承認審査が「実装の依頼は、稼働中の通常サーバーの終了・保存変更・反映までを許可していない」として更新を拒否した。終了・再起動・保存再適用は0。反映には明示承認が必要。承認後も対象PIDと起動時刻を再照合し、その時点の最新状態を既存終了ハンドラーで保存してから再起動する。過去の保存を再適用しない。展示／PC2更新・公開デプロイなし。

確認用 http://127.0.0.1:3033/ は `output/map-pins/preview.mjs` から非表示起動（PID59368）、保存先 `output/map-pins/preview-save` は通常の保存と独立。自動保存正常と6配信ファイルのSHA一致を確認。未対応の通常サーバーではピン操作を表示しない互換処理を追加し、最終ビルド・関連15テスト・実Chrome2画面QAを再確認した。
