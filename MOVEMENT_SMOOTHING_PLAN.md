# 歩行・走行のかくつきとちらつきの修正

2026-09-09。ユーザーの「キャラクターが走ったり歩いたりするときに、かくかくチカチカします。動きが。直せる？」に対応する。

## 対象と調査

- 実際に開かれている展示版は `http://localhost:4173/?room=EXHIBITION`。LAN予測を使い、配信中の `local-prediction.js` は通信ごとに座標を戻す実装。
- その補正差分を `world3d.ts` が歩幅と向きとして使用している。位置補正の往復が振り向きや再生速度の急変へつながるかを、同じ入力と通信条件で測定する。
- シャドウマップは15Hz更新。移動中の姿勢と影がずれるちらつきも実画面で確認する。

## 作業

1. 独立したローカルQAで修正前の連続歩行・走行と描画を記録する。
2. 通信による位置補正を滑らかにし、補正量を足の速度・向きとして使わない。衝突、入力停止、250msの通信切れ、被弾、攻撃、乗降、再接続、大きな位置変更を保持する。
3. 回帰テストと型・ビルド検査、実Chromeで歩行→走行→停止・旋回・再読込を検証する。必要な影の修正は負荷も測定する。
4. ソースと検証記録を保存し、現在利用中の版から修正2ファイルだけを替えたパッケージを用意する。稼働中の展示版への切替は自動承認審査により拒否されたため、明示的な許可を待つ。PC2は今回の修正の対象外。

モデル、GLB、移動速度、当たり判定や手動操作の仕様は変更しない。将来課題全体の再開や公開デプロイは今回の対象外。

## 完了した修正と検証

- LAN予測の計算を毎回サーバー位置へ合わせる方針は保持し、表示位置との差だけを約83msの時定数で減衰させる。表示にも衝突判定を通す。通信切れ・攻撃では補正中の移動も止める。
- 向きと足の再生速度を補正差分から求めず、実際の移動計算の `facing`・`moving`・`speed` に合わせる。小さい魔法使いの1フレームの移動が1.2cm未満で待機へ戻る不具合を解消。
- 15Hzに制限していた影を描画フレームと同期させ、前の姿勢の影が現在の体に重なることを防ぐ。
- 全419テスト、225 JS構文、型・strictを含むビルド、依存境界、変更ソース書式を確認。追加3テストは全7外見の歩行／走行、通信の揺れと重複パケット、衝突、停止、通信切れ、被弾、乗降等を扱う。
- 実Chromeで人間・砂耳の魔法使い・大猿の歩行／走行／停止／旋回／ジャンプ／攻撃、390×844・844×390、再読込を確認。プロフィールだけを準備し、時計・位置・持ち物の準備値はなし。画面録画あり。ブラウザーエラー0。
- 魔法使いの定常歩行で待機が混ざるフレームは修正前54/96、修正後0/101。再生速度は0〜1.690m/s相当から0.600m/s相当へ安定。体の向きの誤差は約70.7度から0.006度未満。実際の速度指定は変更していない。
- 同条件の魔法使いの平均フレーム時間は、歩行18.75→17.78ms、走行17.82→18.49ms。GPUの負荷にはばらつきがあるため、常時60FPSの保証とはしない。大猿の測定中は全テストも実行していたため、負荷比較には使わない。

## 反映状態

ソース修正と独立した確認版 `http://127.0.0.1:3023/` は完了。現在の4173番・8081番、PID49056、元の展示ビルドは保持。
修正パッケージは `output/exhibition-motion-r01`、ビルド `83b51e80dd9db6091b10bf5740ce7a701ce18fedc98bd5aea6d2ed09f5affdcc`。
元の293ファイルのうち変更は `dist/src/local-prediction.js` と `dist/src/world3d.js` の2ファイルだけ。55 GLBを含む残り291ファイルは一致し、パッケージ全体を独立検査済み。

稼働中の1部屋・1人を読み取り、持ち物・位置・元気・畑・住人等の復元照合は成功。ただしこれは反映前の確認であり、切替用の保存ではない。自動承認審査は「移動不具合の修正依頼は稼働中の展示サーバーの停止・置換と反映を明示的には許可していない」として切替を拒否した。停止・切替・保存の再適用は実行していない。許可後も直前の状態を新しく保存し、事前確認の古い保存を使い回さない。調査用デバッガーは終了する。

PC2への読み取り接続はタイムアウトしたが、PC2は今回の修正に必要ない。更新・転送・設定変更は0。別作業の `scripts/build-exhibition.mjs` と `README-EXHIBITION-NOW.md` の変更を保持する。

詳細は [検証記録](assets/movement-smoothing/README.md)。物理コントローラー、物理PC2、長時間負荷、全地形の検証は今回行っていない。
