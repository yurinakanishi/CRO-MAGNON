# その場でキャラクターを切り替える

メニューの「キャラクターを変える」から既存7外見の画像を選び、金色の「このキャラクターにする」で確定する。選択だけでは変更しない。名前・部屋の入力や再参加は挟まない。

サーバーは固定モデルIDと現在の状態を確認する。座標・向き・持ち物・元気・進行・個人ID・再開トークンを保持し、種族・性別を変更する。入力、保留攻撃、本人の光弾、肩乗りの誘いは解除する。乗船・騎乗・肩乗り・力尽き・ジャンプ・攻撃の準備中・作業中は完了後に選べる。

ユーザー最終訂正：当面は「少し広い場所で変更しよう」という案内でよい。体格・通行判定・攻撃距離は既存のまま。変更後の半径で今の座標が空いていない場合は変更せず案内する。別の座標へ自動移動させない。途中で試した全外見の半径統一は取り消した。

通常の状態配信で本人と他プレイヤーのモデルを交換し、古い非同期モデル読込を破棄する。予測・HUD・保存プロフィールも追従する。部屋変更は既存の参加し直しとして別項目にする。

検証：全7外見の相互切替、位置と進行の保持、偽造ID・状態・壁・他人物との衝突、通信・保存復帰、非同期描画交換、PC／縦横の小画面、キーボード操作を確認する。展示版・PC2・公開デプロイは対象外。

## 実装・確認結果

- メニューへ「キャラクターを変える」を追加。既存ポートレート7枚を並べ、選択と確定を分けた。本人の画像を初期選択し、現在のキャラクターは確定不可。部屋と名前の再入力なし。従来の参加し直しはオンラインの「部屋を変える（参加し直す）」へ分離。
- `changeCharacter` をサーバーの既存アクションへ追加。固定モデルID、状態、攻撃間隔、現在地での体格の空きを確認。切り替え時に再接続・再出発・座標変更は行わない。古い非同期モデル読込は破棄し、遅れて失敗しても現在の描画を止めない。
- 全461テスト成功（今回8件、同時作業の城テスト3件を含む）。42通りの相互切替、保持対象、偽造入力、作業・騎乗状態、壁・人物との衝突、肩乗りの誘い・光弾の解除、予測と遅延読込、通信2人と保存の復帰を検証。
- 初回全体検査は別作業の城素材が更新中でSHA不一致1件、次回は既存保存テストの通知待ちがタイムアウト1件。城3件と保存13件をそれぞれ単独で再確認し、最終全461件成功。型・strictを含むビルドと型検査、244JS構文、依存境界、対象書式、diff検査成功。
- 実Chrome2画面で全7モデルを実際に交換し、描画モデル・同一座標・元気・持ち物・本人IDを照合。1440×900／390×844／844×390で選択・取消・確定とスクロールを確認。キーボードの矢印・Enterでカード選択と確定を分離。再読込の復帰、切替後の手動歩行と攻撃も成功。最終ブラウザーエラー0。
- 小画面のダイアログ上部の背景が横へはみ出す問題を修正。城の配信素材が差し替わった途中に一度読込の長さ不一致で停止したが、一致後に全行程をやり直して成功。
- 通常3000番は作業中に既存PID52700からPID35152（2026-09-10 12:21:27）へ切替済みだった。本作業からの停止・再起動・保存再適用は0。通常版の独立した確認用の部屋SWITCH-CHECKで、実UIの変更、同位置・持ち物・元気・ID、再読込、タイトルへ戻る、自動保存成功と7配信ファイルのSHA一致を確認した。ユーザーの部屋の位置や持ち物を試験値へ変更していない。
- 物理Gamepad、長時間負荷、全地形での全組合せは未検証。GLB変更なし。別作業の城関係差分は保持。展示／PC2・公開デプロイなし。

再現：`node scripts/build.mjs`、`node --test tests/character-switch.test.mjs`、`node scripts/qa-character-switch.mjs`。証跡は `output/playwright/character-switch/`、全テストは `output/character-switch-tests-final.log`。通常の確認補助は `output/character-switch-normal-smoke.mjs`。
