## 2026-09-07 最新：五つの地域をつなぐオープンワールド

ユーザー指定の雪・氷・火山・砂漠・草原を、640 × 640 mの有限の共有ワールドへ実装済み。座標範囲は各軸 -256〜384 m。地域の切り替え画面を挟まず、世界地図の地域ボタンで野営地まで実際に走る。元の人物6種類、マンモス、白羽の呪術師、採集・狩猟は保持する。追加4野営地を含む6か所の焚き火で調理できる。

新しい `snow-ground`、`ice-ground`、`desert-ground`、`volcanic-ground`、`glacier-spires`、`volcanic-cone` は、各Candidate 1としてimagegen→ローカルTRELLIS-2→元形状に沿う軽量化・補修→exact GLB QAを完了。雪の上面UV修正と砂の元メッシュをレイで計測した再メッシュを別revisionで保持する。火山の粗いLODは斜面が崩れたため不採用とし、13,382三角形の採用版を2か所で共用する。旧モデルや可視プリミティブへのフォールバックはない。静物に不要なリグは付けない。

32 mの地形区画を半径128 mだけ保持し、確認時は最大69区画。インスタンス描画、距離LOD、2本までの地域読込、1フレーム2区画／3 msまでの追加、12秒後の未使用素材解放、遠方の静物・人物・動作の省略、テクスチャの共有を実装した。雪・灰・砂塵は固定384点のシェーダー。FPS上限60・影15 Hz・画素比上限1.25を継続する。空と霧の色空間も一致させる。

全29モデルの配信検証、110テスト、5人接続（実ブラウザー2＋通信参加者3）、390×844の地図・走行選択・自由攻撃を確認。草原→砂漠、草原→雪原は通常の移動処理で境界を越えた。GLBと足場の6,000点照合は最大約2.2 cm、氷壁・火山は採用GLBの輪郭を1 mセルで計測して当たり判定に使う。近接用の保守的な足形であり、自由な山登り物理ではない。通信検査6,150状態で静物貫通0件、描画エラー0件。実機の観測範囲は43〜60 FPSで、常時60とは記さない。

現在のプレイヤー・持ち物・拠点・経過日数を保存して3000番へ反映済み。これは今回の更新時の復元であり、通常の永続保存・再接続保証を追加したものではない。制作状態は `assets/open-world-request.json`、ゲームQAは `assets/open-world-game-qa.json`、足場QAは `assets/open-world-surface-qa.json`、更新確認は `assets/open-world-rollout-qa.json`。元画像・dense・全revision・QAの計550ファイルを隣の `threed-model-creation/models/` の該当6フォルダーへ追加保存し、全SHAを照合した。既存の制作物は上書きしない。

## 2026-09-07 追加制作：猫耳のクノイチ・こぐまの魔法使い

ユーザー指定の猫耳の成人女性クノイチ、小さな独自のくま風魔法使い、装備の刀を各Candidate 1として制作・採用した。imagegen参照画像→ローカルTRELLIS-2 dense→形状保持の軽量化・補修→必要なリグと9動作→exact GLB QAの工程を使用。Claudeと可視プリミティブの代用、読込フォールバックは使用しない。

猫耳は1.68 m・66,766三角形・7,340,180 bytes・rig revision 06。こぐまは0.78 m・66,795三角形・7,711,648 bytes・rig revision 08。両方22骨・9動作。刀は長さ1.05 m・22,201三角形・2,019,308 bytes・low-poly revision 08で、握りの原点から+Yを刀身方向にする。静的な刀自体には不要な骨を付けない。

猫耳の手首・肩と衣装、こぐまの腰・裾のウェイトを補修し、最終GLBを再読込して全9動作×6方向×5時刻の静止画、動画、60 Hzの全表面検査を確認した。刀は7方向の最終静止画、同じ形状の無彩色表示、denseと既存の石槍を含むブラウザー比較を実施。元形状・UV・色の系統を保ち、JPEG化では非画像バッファの完全一致とPSNR 40 dB以上を検証した。元PNG・dense・失敗と修正の各revisionを保持する。

猫耳は歩行1.25／走行3.5 m/s、こぐまは0.6／1.8 m/s。歩幅の実測値からクリップ速度を合わせる。刀は267 ms時点、魔法は400 ms時点でサーバー処理を同期。魔法は実行時の光の粒子で、可視モデルの代用品ではない。全23モデル・97テスト、5人の実画面と390×844の操作、通常の地形で両武器による討伐まで検証済み。記録は `assets/fantasy-playables-request.json` と `assets/fantasy-game-qa.json`。

## 2026-09-07 追加制作：白羽の呪術師

ユーザーが候補画像3番のカラス人間「白羽の呪術師」を選択した。新モデル `crow-shaman` は白い冠羽と頬羽、黒いカラスの頭・くちばし・羽毛、獣皮の衣装、石を結んだ木の杖を保ち、TRELLIS-2から制作する。選択画像の原本と、杖・身体・手足の隙間を読みやすくした制作参照を両方保持する。杖も復元メッシュ由来の形状を使い、手と連動する骨またはノードへ剛体として接続する。

専用の二足リグと `Idle_Loop`、`Walk_Loop`、`Run_Loop`、`Attack`、`Hit`、`Death` を用意する。杖の攻撃は1.0秒で450 ms時点を打撃姿勢に合わせ、被弾は0.4秒、死亡は1.2秒。根のワールド移動を含めず、歩行・走行の実測歩幅とゲームの速度を合わせる。身体と杖の貫通、獣皮の裾、鳥の足の接地、冠羽・嘴の形を全動作と複数方向で検証する。生成・採用・ゲームQAは `assets/crow-shaman-request.json` で区別する。

## 2026-09-06 追加指示：マンモス狩猟・肉の調理

マンモスを2か所の開けた草原で歩かせ、槍で倒す、肉を採る、焚き火で焼く、焼き肉を食べる流れを実装する。今回の構成は全19モデル。新しい肉モデル `mammoth-meat` は参照画像からTRELLIS-2で復元し、静物として原点・寸法・材質を整える。人物4種類に `Attack` を加えて各9動作、マンモスに `Death` を加えて5動作とする。既存の形状・UV・リグ・以前のクリップは保持し、追加したGLBを別revisionとして保存する。

狩場は北西 `(25, 21)` と南西 `(24, 85)`。F／5で槍を突き出し、333 ms後の距離・遮蔽物の再確認で命中を確定する。HP 100、1発25ダメージ、4発で討伐、攻撃動作0.70秒、間隔850 ms。死亡動作1.2秒の後に共有の生肉4個へ変わり、近くの E／クリックで1個ずつ採れる。中央の焚き火から3.4 m以内で3秒調理し、完成時に生肉1個を焼き肉1個へ変える。中止や範囲外への移動で生肉を失わない。焼き肉1個で元気45回復。肉を取り切ってから90秒後に再出現する。

**今回の最終GLB採用・ゲームQAは完了。** 全19モデルの配信検証、48テスト、5人接続での狩猟→採取→調理→食事、390×844の画面操作を確認済み。結果は `assets/hunting-game-qa.json`、`assets/hunting-network-qa.json`、`assets/hunting-mobile-qa.json` に記録した。 新しい肉は2,826三角形・約0.58 MB。人物4体とマンモスの追加動作は全表面を60 fpsで測定し、6方向のexact GLB動画・静止画を確認。既存のメッシュ・リグ・UV・材質・以前のクリップは元のバイト列と一致することを検証した。以前の全18モデル・女性版8動作・30テストの記録は履歴として保持する。

## 2026-09-06 先行指示：残り全モデルをCodexが制作

ユーザーは「claudeは使わなくていいから、他のすべての3dモデルについても、あなたの方でimage to threed を使って修正してください。」と明示した。Claudeを起動せず、Codexが残りの全可視3Dモデルをimagegen参照画像とローカルTRELLIS-2から制作・修正・必要なrig／animation・検証・ゲーム組み込みまで行う。今回の範囲は `assets/world-models.json` の全素材であり、以前の1体で停止する区切りや1指示1モデルの制限より、この全素材の明示指示を優先する。形状の制作系統、既存候補の保持、性能・外観検証は維持する。

# 全3Dモデルの制作・リギング・アニメーション方針

女性追加時点の完了記録: クロマニョン人・ネアンデルタール人の女性版をローカルTRELLIS-2で制作し、女性を初期選択にした。成人のアイドル風の顔立ちと女性的な体型、毛皮・獣皮を使う原始的な服装を採用。当時は両モデルとも22骨・8動作を持ち、書き出したGLBの再検証と男女4種類を含む5人接続を確認した。制作記録は `assets/female-model-requests.json`、ゲーム検証は `assets/female-game-qa.json`、参照画像の履歴は各モデルの `source/original/` に保持する。既存16素材の配信GLBは保持した。

更新日: 2026-09-06。**先行する全素材制作では、既存クロマニョン人を保持し、残り15素材をCodexが制作し、ゲーム組み込みと5人接続QAを完了した。参照画像はbuilt-in imagegen、3D復元はローカルTRELLIS-2を使用し、Claudeは使用しない。生成・採用・ゲーム内検証の最新状態は [制作進行記録](assets/README.md) と [全素材一覧](assets/world-models.json) を参照する。**

## 対象と制作方式

生き物だけでなく、全可視3Dモデルを **TRELLIS-2（image-to-3D）** で制作する。当初のClaude担当は、2026-09-06のユーザー指示でCodexへ引き継いだ。画像からの制作系統と、必要なrig／animationの要件は維持する。基本工程は次のとおり。

> 仕様・参照画像 → TRELLIS-2による高密度メッシュ → 形状を保った軽量化・補修 → UV／材質／構造 → 必要なリギング・アニメーション → 書き出したGLBの検証 → ゲームに組み込み

人物や動物に加え、木・草・茂み・岩・地形・テント・橋・焚き火の薪と石・道具・装備・採集資源まで対象に含める。地形や川は配置可能なパーツへ分割し、各モデルに同じ制作経路を適用する。画像からの復元が難しい対象も、まず参照画像、マスク、復元条件、パーツ分割を改善する。

Three.jsの球・円柱・箱などを組み合わせた初期の可視モデルは削除済みであり、再導入しない。Blenderのプリミティブやスクリプトで形を一から作り直したモデルを、完成モデルやTRELLIS生成物として採用しない。この制約は肉を含む静物や環境モデルにも適用する。

カメラ・照明・配置・地表の高さ照会・不可視の当たり判定・物理・LOD切り替え・アニメーション再生は実行時コードの役割。火の明滅、煙・火の粉、水の流れや反射はシェーダー／粒子で表現してよい。可視の地形や水面のモデルが必要な箇所は、これらの効果とは別に上記経路で制作する。

## 参照するリポジトリ

参照元は `../threed-model-creation`。一般のモデル制作と、実際にリグ・アニメーション付きGLBを完成させた実例の両方を確認した。

| 参照資料 | 引き継ぐ内容 |
| --- | --- |
| [AGENTS.md](../threed-model-creation/AGENTS.md) | Claude実行、候補管理、画像からの制作系統 |
| [MODELING_WORKFLOW_POLICY.md](../threed-model-creation/MODELING_WORKFLOW_POLICY.md) | 参照画像、TRELLIS-2、形状を保つ軽量化、表面・材質・GLB検証 |
| [SEQUENTIAL_MODEL_GENERATION.md](../threed-model-creation/docs/SEQUENTIAL_MODEL_GENERATION.md) | coordinator、事前確認、1候補ずつの生成と再開 |
| [REPOSITORY_LAYOUT.md](../threed-model-creation/docs/REPOSITORY_LAYOUT.md) | モデル単位で原画像・中間成果・QAを保持する配置 |
| [RIGGING_ANIMATION_HANDOFF.md](../threed-model-creation/models/cool-cyberpunk-woman-rigged/RIGGING_ANIMATION_HANDOFF.md) | Blenderでの骨格、ウェイト、クリップ作成、exact GLBの変形検証 |

人物リグの実装例は、参照元の `models/cool-cyberpunk-woman-rigged/workflow/batch-010/run-01/attempt-01/` にある `blender_rig.py`、`clips_data.py`、`validate_candidate.py`。既存の完成候補を上書きせず、新しいモデルの作業場所に合わせて処理を移植する。サイバーパンク人物そのものの外見をゲームに流用する指示ではない。

## CodexとClaudeの役割

ユーザーの最新指示「claude 停止したので、代わりにあなたがやって」に従い、現在はCodexが制作工程も担当する。最初の人物はClaudeのTRELLIS・軽量化・リグ・8動作を保持し、Codexが胴体ウェイトとQAを仕上げた。Claudeの失敗履歴を書き換えず、`work/codex-completion/` と `work/candidate-ledger.json` に独立した完了を記録する。下記のClaude起動手順はClaudeを再び使う場合の手順であり、今回の完了や次の着手の前提ではない。

Claudeの起動は参照元の `tools/generate_model_variants.py` を使う。実行前に参照元の最新ルールとpreflightを確認する。既存のClaude.aiサブスクリプション認証を使用し、Anthropic API課金へ無断で切り替えない。参照元が指定する完全なモデル名とthinking effortは、その時点の環境で確認し、推測した値や省略名を固定しない。認証情報をこのリポジトリに保存しない。

今回は全素材の制作が明示されているため、一覧の各モデルにつき成功候補1件を制作する。Claude coordinatorは使用しない。中断・失敗は同じ未完了候補内で再開し、補修前後を別revisionに保存する。原画像・高密度GLB・失敗した候補・動画・測定値を保持する。RTX A1000 6 GBではTRELLISとBlenderの重い処理を直列に実行し、各素材を目視検証してから次へ進む。

## モデルごとの工程

1. **参照画像を用意する。** 原画像を保持し、全体が収まり、輪郭とパーツ間の隙間が読める画像とマスクを準備する。人物は手足が胴体に重ならないリグ用の姿勢、動物は四肢・鼻・尾が読める姿勢を使う。建築物や道具は厚み、開口部、接続箇所を明確にする。画像生成は参照元のZImageまたは認められた同等手段に従う。
2. **TRELLIS-2で形を復元する。** 画像・マスク・seed・解像度・設定と高密度GLBを保存する。失敗した形を後工程でごまかさず、画像や復元条件から改善する。
3. **形を保って軽量化する。** 参照元のremesh／限定的な平滑化／QEM等を対象に合わせて適用する。顔、手、関節、牙、耳、開口部、薄い部材を保ち、表面ノイズを抑える。別モデルの固定三角形数や容量を転用せず、品質と実測性能に応じて密度・LODを決める。
4. **UV・材質・構造を整える。** 重複頂点の統合、必要な補修、smooth／crease-aware normals、TRELLIS由来のalbedoのベイクを行う。自然な肌、毛、木、石、革等の色とPBRを使う。原点・実寸・向き・接続点・可動部を整える。
5. **対象に合うリグと動作を作る。** 下記の仕様に従い、必要なスキンまたはノードアニメーションをGLBへ含める。
6. **最終GLBを検証する。** エディター内の見た目だけで完了にせず、書き出したファイルを再読込し、静止形状・全動作・ゲーム内表示を確認する。

BlenderはTRELLIS由来モデルの補修・軽量化・UV・ベイク・骨格・ウェイト・動作・書き出しに使用する。参照元の1,097三角形、22 bones、1.75 m、1024角テクスチャ、46度の法線処理は、その人物モデルの実測値・設定例であり、全モデルに強制する数値ではない。

## リギングとアニメーション

| 対象 | リグ・構造 | 狩猟追加後に必要な動作 |
| --- | --- | --- |
| クロマニョン人、ネアンデルタール人、オル | 共通命名の人型骨格。骨盤・脊椎・頭・肩・腕・手・脚・足。体型ごとにウェイトを確認 | `Idle_Loop`、`Walk_Loop`、`Run_Loop`、`Gather`、`Craft`、`Give`、`Eat`、`Wave`、`Attack` |
| マンモスなどの四足動物 | 四足用骨格。胴・首・頭・四肢に加え、必要なら鼻・耳・尾のチェーン | `Idle_Loop`、`Walk_Loop`、`Run_Loop`、`Graze_Loop`、`Death` |
| 木、草、茂み | 静的構造、または必要な根元・幹・枝の変形構造 | 揺らす素材には `Sway_Loop` または検証済みの実行時の揺れ |
| テント、橋、乾燥棚、岩、肉などの資源、地形パーツ | 配置用の原点・接続点。動く部材だけ骨／ノードを分離 | 静物は不要。扉や布などを動かす場合はその動作 |
| 槍、石斧、装備 | 持ち手の原点、手の骨への取り付け点 | 本体の動きは人物クリップと連動。必要な可動部だけ個別に作成 |
| 焚き火・川 | 薪・石・岸・水面等のモデルと、効果の発生位置を整理 | 炎・煙・火の粉・水流は実行時エフェクトと組み合わせる |

変形するモデルにはスキンウェイトを設定する。人物は参照先のlandmark-based armatureとbone-heat方式を基礎とし、左右への誤ウェイト、関節の潰れ、手と身体の貫通を修正する。各頂点のinfluenceは最大4、ウェイト合計は1、未割当頂点なし。マンモスに人型骨格をそのまま適用しない。

アニメーション制作は30 fpsを基準とし、移動クリップは **in-place**。ルート骨にワールド移動を焼き込まず、サーバーの位置と二重に移動させない。ループは開始・終了姿勢を一致させる。人物の単発動作は終了後に待機または移動へ戻る。マンモスの `Death` は終了姿勢を保持し、サーバーの `meat` 状態で肉GLBへ表示を切り替える。足の接地、重心、膝・足首・つま先、腕の振りを合わせて調整する。

四足歩行の接地順序や人物の速度はモデルごとに記録する。現在のゲームは歩行1.25 m/s・走行3.5 m/sを切り替える。組み込み時に実速度とクリップの歩幅・再生速度を合わせ、足滑りを確認する。Walk_LoopとRun_Loopは実際の移動速度から再生速度を決定し、移動コンテナをリグから独立させる。

## GLBとゲームへの組み込み

最終形式はテクスチャ、必要なskin、名前付きanimationを含むGLB。Three.js側ではY-up、人物・動物の正面は+Z、実寸を明記し、人物の原点は足元にする。Blenderからの軸変換後の向きを確認する。静物は配置・接続に適した原点を記録する。

制作の正本は参照元の `models/<model-key>/` にまとめ、原画像、conditioning画像、高密度GLB、軽量化前後のGLB、制作スクリプト、候補番号、設定、hash、rig／clip一覧、QAを保持する。ゲームへ取り込む検証済みファイルの配置先は `public/models/<model-key>/model.glb` とする。CRO-MAGNON側にも元ファイルの場所、候補番号、SHA-256、寸法、軸、clip名とループ種別を記録する。クロマニョン人の最終GLBはこの配置に存在し、配信manifestと制作workspaceの候補GLBのSHA-256が一致している。

ユーザーの追加指示により初期のモデル生成コードと旧2D描画を削除し、全モデルを検証済みGLBから配置する。`src/world-assets.js` が配信SHAと埋め込みリソースを確認し、`src/world-scenery.js` が地形・植物・建物・動物を配置する。スキン付きモデルはキャラクターごとに骨格を複製し、それぞれのAnimationMixerで再生する。GLTFLoader、SkeletonUtils、OrbitControls等のaddonはローカル配信済み。クロマニョン人は `src/character-assets.js` と `src/character-animation.js` を通じ、Candidate 1の最終GLBへ素材交換と動作の接続を完了している。

位置・向きはサーバー状態で決める。Idleと移動は `moving` と速度、Waveはサーバーの `emote`、採集・制作・受け渡し・食事は確定した持ち物の変化へ接続する。`Attack` は確定した `attackSequence` と `attackAt` から再生し、攻撃中の補間による微小な移動で中断しない。槍の位置はアニメーション中の手に従い、先端を人物の前方へ向ける。調理は `cookingEndsAt` から進捗を示す。死亡は動物の `phase` と `phaseStartedAt` に同期し、途中参加で最初から倒れ直さない。肉への変更時に動物の再生を止め、再出現時に状態をリセットする。ほかの人物の単発動作は終了時に待機へ戻り、移動開始時は歩行または走行へ切り替える。

GLB読込・検証失敗時はエラーで描画を停止する。旧モデルや他のモデルへのフォールバックは禁止する。モデル確認画面も失敗時に前のモデルを残さない。

## 検証と完了条件

- 原画像・高密度メッシュ・最終GLBを同じ条件で比較し、輪郭、比率、特徴、裏側、開口部、細い部材、表面ノイズを確認する。
- 実際に配信するGLBのテクスチャ、UV、PBR、法線、原点、寸法、正面方向を検証する。
- リグ付きモデルは骨階層、単一のRoot、inverse bind matrices、joint index、正規化ウェイト、未割当頂点なし、変形時の三角形崩壊を確認する。
- 全クリップの名前、時間、動き、ループの継ぎ目、接地、手足・装備・身体の貫通を確認する。正面・背面・左右・斜めと、TPSの肩越し視点で動かす。
- アニメーション動画QAは最終GLBの再読込から作る。参照元どおりBlenderのimport前に30 fpsを設定し、ループ末尾の重複フレームを動画へ含めない。hash、fps、フレーム数、時間を記録する。
- ゲーム内で5人とNPC・動物・環境を同時に表示し、FPS、描画負荷、メモリ、読込時間を測る。必要な軽量化は実測と外観比較から判断する。
- 記録上の「候補生成済み」「リグ／動作検証済み」「採用済み」「ゲーム組み込み済み」を区別する。生成完了だけでモデル交換完了としない。

## 先行制作の履歴と今回の完了条件

最初のクロマニョン人の[制作依頼書](assets/requests/cro-magnon-hunter.md)と完成GLBは保持する。ClaudeとTRELLISによる元データをCodexが引き継いで修正した履歴は [Candidate 1の記録](assets/cro-magnon-candidate-1.md) に残す。この旧環境での5人・60 FPSという観測値を、新しい全素材環境の性能として転用しない。残りの人物・動物・環境・装備も今回の指示で完成し、全素材を使うゲームの5人接続QAを実施した。結果は [全体検証記録](assets/world-game-qa.json)、各素材の状態は [制作進行記録](assets/README.md) に記載する。

先行制作の各素材はCandidate 1として完了し、修正revision、生成設定、原画像、密メッシュ、動画、測定値を保持する。今回の肉モデルとAttack／Death追加版は、実際に配信するGLBの外観・変形・装備・クリップを検証し、草原へ移動、4回の命中、肉の採取、焚き火での調理と中止、食事までを通常のゲーム操作で確認する。5人の共有状態、再出現、途中参加、モバイル操作を確認してから今回の完了を記録する。先行する女性追加QAは保持し、新しい検証結果と区別する。

## Meshmellへのアップロード

将来のMeshmellアップロードは [Meshmell CLI アップロード手順](MESHMELL_UPLOAD.md) に従う。今回の方針ではMeshmell公式CLIだけを使い、Xへの投稿・リポスト・Likeは行わず、回転動画も生成しない。ユーザーから改めて実行指示があるまでモデルをアップロードしない。公開／非公開は推測せず、実行時の明示された指定に従う。

表示と衝突は `shared/scenery-layout.mjs` の同じ配置を使う。`shared/model-bounds.mjs` は配信GLBから測定した外形を記録し、リグの人間・マンモスと静物の不可視の当たり判定を軽量な形で実装する。元のTRELLISモデルを別の可視プリミティブへ置換する処理ではない。
