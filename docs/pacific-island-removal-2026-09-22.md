# 太平洋の架空島の撤去（2026-09-22）

曲がったN字に見える「三つの岸の湾」をユーザー指定で撤去した。
地形への人工的な陸地の追加を外し、地図の表示範囲を元の地球へ戻した。
島の集落・住民・畑・泉・魚場・資源・焚き火・景観・当たり判定・ワープ先と専用案内も撤去。
他の陸地の景観配置は既存の乱数列を保ち、元の海岸データとキャンプの山は変更していない。

## 保存の互換性

`gulfVersion` は3。旧地形定義を `shared/gulf-archive.mts` に保持し、旧バージョンの座標変換後に
`shared/gulf-retirement.mts` で移行する。消えた島の海上にいる保存人物は初期キャンプへ移し、
持ち物などの状態は保持する。舟の旧帰港先はキャンプ付近の実際の海岸へ変更し、
既存の係留処理で舟同士の重なりを避ける。他の陸地にいる人物の座標は維持する。
保存済みの根・香草は引き続き調理できる。撤去した場所への操作や報酬は受け付けない。

## 検証

- 全590テスト合格、失敗・スキップ0。`output/island-removal/tests-final.txt`。
- 撤去された機能の旧118テスト（12ファイル）は `tests/retired-gulf/` にそのまま履歴保存。
  新しい6テストが地形全8,388,608バイト、旧島の海面・衝突、保存移行、旧操作の拒否、既存食材を検証する。
- TypeScriptと設定済みstrict、373 JSファイルの構文、依存境界を確認。
- 実Chrome2画面＋通信3人で旧島の海上を舟で移動し、双方へ同期。島専用の描画物なし。
  地図・メニュー・再読込、1440×900／390×844／844×390を確認し、ブラウザーエラー0。
  4検証群・6画像：`output/playwright/pacific-removal-2026-09-22/`。
- 展示メニュー変更を含むローカル同梱版でも起動4項目と、PS4／Switch Pro表記・人物変更・
  6地点のワープ・持ち物・同期・縦横画面・再読込を確認。
  `output/playwright/exhibition-menu-1790083370820/`。
- 通常3000番の8配信ファイルのSHAをビルドと照合。保存正常・復旧なし。
  `output/island-removal/normal-delivery.json`。

検査用の座標・持ち物の準備は保存しない独立した検査世界だけで行った。
通常サーバーは既存の開発監視による自動更新で反映し、手動再起動・保存の再適用はしていない。

## 配布は保留

ユーザーの「変更まででいい。localで見てからにしたい」を優先し、展示PCへの配布・切替・再起動は実施していない。
準備したオフライン版 `output/exhibition-20260922-pacific-r01/` と同名ZIPはローカルに保持。
ビルドIDは `14b4e2eb1983d0d9dff6865c34875cf1a34ed9ea8ac76678daa5af1e4d285294`、404ファイル・78 GLB。
GitHubへのコミット・pushは追加指示の範囲で行う。公開サイトのデプロイは含めない。
