# マンモスを崖へ登らせない（2026-09-22）

山側のマンモスは旧配置 `(24,85)` から徘徊すると、山肌の
`(21.319,89.205)`、高さ約4.92m・体の中心から外周までの最大勾配約1.30へ進めた。
既存の障害物判定は木・岩・資源との重なりを防ぐが、山の斜面を制限していなかった。

`shared/mammoth-navigation.mts` が山GLBから計測した共通の高さを参照する。
中心の傾斜と、半径の半分／全幅それぞれ16方向の高低差を調べ、勾配0.20を超える
場所へマンモスの体を進めない。サーバーと描画側は同じ判定を使い、画面だけにある
地表テクスチャの小さな凹凸を通信側へ持ち込まない。通常の人物の移動判定は維持する。

山側の初期配置をふもとの `(29.5,68.5)` へ移した。採集資源は移動せず、周囲の飾りの
木・岩だけを除いて体が通れる場所を確保する。既存の乱数列と旧除外領域を保持した後で
新しい場所を空けるので、遠くの地形小物の配置は変わらない。

徘徊の目的地が斜面に入る場合は歩幅を短くし、帰属する半径5mの範囲で経路を選ぶ。
出現、徘徊、騎乗、再出現、クライアント予測と表示の補間に同じ傾斜判定を適用した。
保存を読み込むとき、崖や障害物に重なったホームを安全な初期地点へ戻し、生きている
個体の位置が危険なら安全な場所へ移す。安全な位置・体力・倒した状態は保持する。

検証は `tests/mammoth-ground.test.mjs`、従来の狩猟・騎乗・衝突・保存テスト、
`scripts/qa-mammoth-ground.mjs`。崖への高速移動、2分間の徘徊、騎乗予測とサーバーの一致、
旧保存の読み込み、再出現を検査する。ブラウザー検証は実Chrome1画面と通信4人を用い、
実際のR・W・Shiftで騎乗して崖へ進み、停止・方向転換・降車・再読込・縦横画面を確認する。
準備座標と観察カメラは保存しない独立世界だけへ設定し、ゲーム時計は変更しない。

証拠は `output/playwright/mammoth-ground-2026-09-22/summary.json` と `frames.json`。
自動検証の一覧は `output/mammoth-ground-tests.log`。性能改善を主張する変更ではない。

最終結果：全700テスト、型・設定済みstrict、382JS構文、依存境界、変更コードの書式が通過。
実Chromeの描画3,910件で危険な傾斜への進入0、通信4人で計1,430スナップショットを確認。
4検証群・6画像、ブラウザーエラー0。通常3000番は開発監視で更新され、6配信SHAが一致し、
既存3部屋の生存マンモスとホームは安全な地面へ復元済み。保存状態は正常・復旧なし。
配信と保存の読み取り記録は `mammoth-ground-delivery-2026-09-22.json`。

洞窟r29と合わせた展示版 `exhibition-20260922-gallery-r29-mammoth-ground` をPC0・PC1・PC2へ配置。
各400ファイル・78GLBとビルドID `e567f6be9e7551f1ff2c34b49b3282bdb2e7cedc47b69589b0cdf69c2d2d6267` が一致した。
PC3は確認済み機体名・接続準備が未完了なので未配布。全台揃わないため稼働版の切替・
展示の再起動は未実施。同梱ランタイムの一時ポートによるホスト／クライアント起動4項目は通過。
標準ユーザーで配置し、UAC・昇格・公開デプロイ・コミットはない。
配布記録：`output/exhibition-fleet/20260922-191206-deploy-2b626c2530e749feaecfee1d71c5fadd.json`。
起動検査：`output/exhibition-fleet/gallery-r29-mammoth-ground-launch.json`。
