# 展示メニューの整理（2026-09-22）

展示LANだけを「持ち物」「キャラクターを変える」「ワープする」の3タブへ変更。
クラフト・ガイド、世界・仲間・操作説明、設定のタブと内容はメニューから除去する。
「タイトルへ戻る」は保持。「探索に戻る」と右上の戻るボタンは表示せず、
PS4の×／Switch ProのB（右側4ボタンの下）、メニューボタン、Escで閉じる。
持ち物の操作やキャラクターの確認では、下のボタンで一段戻る既存動作を維持する。
524追従中の「524をキャンプへ帰す」も維持する。

人物選択は同じメニューの右側に表示。既存の確認・取消とサーバー検査を使い、
現在地・持ち物・体力・進行を保持する。デスクトップは4列で7人が収まる。

ワープの写真・名前・説明は開始時と `src/spawn-picker.ts` を共用する。
拠点、マンモス、剣牙、紫尾、大聖城、洞窟の6か所。
サーバーの `spawnChoice` が有効な展示部屋だけ `spawn-*` の行き先IDを許可し、
クライアントからの座標を使わず共有の開始地点と衝突判定で安全な着地点を求める。
既存の乗船・騎乗・肩乗り・跳躍・攻撃・作業・連続ワープの制限を維持。
成功した状態を受信してからメニューを閉じ、人物とカメラを見どころへ向ける。

## 検証

- 独立コピーで全702テスト合格。型・設定済みstrict、383 JS構文、依存境界も合格。
- 6地点×全7人物の安全な着地、持ち物・体力・向き、通常部屋と偽IDの拒否、操作状態の制限を検査。
- 実Chrome2画面＋通信3人、PS4／Switch Proの模擬standard Gamepad、実UIからの食事・人物変更の取消と確定・全6地点へのワープ・相手画面への同期・タイトル復帰・再読込を確認。
- 1440×900／390×844／844×390で3タブと各一覧の末尾への到達・横はみ出しなしを確認。
- 展示メニュー最終単独検証：`output/playwright/exhibition-menu-1790082923540/`。12検証群・19画像、ブラウザーエラー0。
- 島削除を含む作業フォルダーのビルドでも同じ12検証群・19画像・エラー0を確認。
  `output/playwright/exhibition-menu-1790083366858/`。この検査は切替待機の指示時点で既に実行中で、終了後に検査用ブラウザーと別世界サーバーを閉じた。
- 通常メニューの既存4タブとサブタブ：48条件で枠欠け0、方向操作も合格。
  `output/exhibition-menu-source-r01/output/playwright/pause-focus-after-1790083215229/`。
- 物理コントローラーと実際の展示画面は未検証。検査用のベリー1個・体力40は保存なしの別世界だけで準備。

同じフォルダーで太平洋の島削除が同時進行したため、メニュー単独の検証には
`output/exhibition-menu-source-r01/` を使用した。通常メニューに混ざった変更途中のHTMLは
独立コピー側で開始時の内容へ戻してから再検証した。元の作業フォルダーでは、島削除側の変更を保持している。

## 配布は保留

後続のユーザー指定で全変更を統合して配布・切替済み。
最新の結果は `exhibition-simple-2026-09-22.md` を参照。以下は保留時点の記録。

PC0・PC1・PC2は接続確認済み。PC3はSSHタイムアウトし、ユーザーから今回は未配布の指定。
続いてユーザーが「他の変更もあるから、待って」と指定したため、このタスクでは配布・切替・再起動を実施していない。
ほかの変更と合わせた最終版を検証してから配布する。公開デプロイ・コミットなし。

メニュー単独の候補r02は `output/exhibition-menu-source-r01/output/exhibition-20260922-menu-r02/`。
ビルドID `25b6dcd7412c0102d688db6bff5a046b5acda2859eb4b0cba6a31f73ccf384b9`。
400ファイル・78GLB。これは島削除前を基準とした候補で、今後の統合配布にはそのまま使わない。
r01のZIPも途中候補として保持しており配布しない。
