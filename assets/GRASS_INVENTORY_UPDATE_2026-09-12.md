# 草の土台と持ち物の体力表示（2026-09-12）

ユーザー指示：草の株・草むらの下に残った薄緑の土台を削除、左上の「回復（ベリー）」を削除、持ち物に人物画像と体力バーを追加。追補で十字キー↑／Qの自動回復そのものも削除。

## 草の円盤を全体まで削除

前回の4.5 cm／3 cmの切除では円盤の上面が残っていた。配信GLBを計測すると、草の株のほぼ水平な面は6〜8 cm、ぱらぱら草は3〜4 cmに集中していた。`scripts/cut-grass-root.mjs` で8 cm／4 cmまでの三角形を除去し、候補をChromeで確認後に `scripts/adopt-foliage-revision.mjs` で採用した。

| アセット | revision | 三角形数（変更前→後） | 配信SHA256 |
| --- | --- | --- | --- |
| meadow-grass | 05 | 43,848→42,587 | `24b41b18d4c5ea40ca660d7eda16f2154ca856828ebfc721d6c3489efe63f9c0` |
| meadow-sprig | 04 | 11,765→11,546 | `4a4c9060a5d95f0db3dcf3c4ff050c78c86f543b942ec62273471326959483de` |

変更はインデックスのみ。頂点・法線・UV・テクスチャ・材質と元の高さは同一。旧候補を保持。実際に使われる最低頂点は約7.29 cm／3.89 cmとなるため、草の沈め量を0.08 m×配置倍率に変更し、大きい株でも切り口が浮かないようにした。地域材質・追加読み込みの草にも適用し、他の植物の沈め量は保持。

候補の画像と測定は `output/playwright/grass-root-complete/candidates/`。配信版の5人接続（Chrome 1画面＋通信4人）と9画像は同 `final/`。通常LOD、近距離の実メッシュ（株12・ぱらぱら草7）、東と南の草原・俯瞰・再移動後の188株の色の一致、沼地・地図を確認。エラー0。

## 持ち物から回復する

- 左上の体力バーから食事候補の案内を撤去。
- 持ち物の先頭に現在のキャラクターの既存画像と体力バーを追加。数値・幅・色・読み上げ用の値を毎回の状態受信で更新する。画面を開いたまま食べても即座に反映。所持数が変わらない体力変化も反映。
- 十字キー↑／Qの割り当て、クライアントの `heal`、サーバーの自動食事選択、`chooseMeal`／`HEAL_REPEAT_MS` と説明文を撤去。個別の食事処理は保持し、旧 `heal` メッセージでは食べない。
- 体力欄はスクロール中も表示。高さ500 px以下の横長画面では画像と見出しを小さく並べ、食事操作の場所を確保。

## 検証

- 全558テスト合格、strictを含む型検査・286 JS構文・依存境界合格。
- `qa-controller-menus.mjs` 8項目：Q／↑で回復・消費しない、ベリーで40→65、持ち物内の数値・幅・色が更新、メニューを開いたまま食べられる。
- `qa-inventory-context.mjs` 7項目：生肉と焼き肉、消費後のカード消滅、調理、単一フォーカス、キャラクター変更後の画像、390×844／844×390。最終証拠は `output/playwright/inventory-health/compact-final/`。ベリーの前後は同 `controller/`。
- 新しいQAの初回は、サーバーが回復してからブラウザーへ届く前に値を比較して失敗。サーバーの値が実際に画面へ到着するまで待つよう検査を修正した。
- 通常3000番のJS・CSS・草2モデル・配信カタログのSHA一致を確認。ブラウザーの再読み込みで表示を更新する。サーバーの手動再起動、展示・PC2更新、公開デプロイ、コミットは実施していない。

物理コントローラー、全地域の急斜面、長時間負荷は今回の確認対象外。
