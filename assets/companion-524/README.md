# 浮遊する524（2026-09-22）

## 採用モデル

ユーザー指定の隣接リポジトリ `../threed-model-creation` を確認し、最新の完成候補 **Candidate 14** を採用した。元リポジトリの承認済みモデルはCandidate 13だが、その形状・骨・アニメーションを保って黄色だけを調整した最新ファイルが14。元リポジトリの採用状態は変更していない。

- 元: `models/yellow-524-mascot/work/low-poly/batch-016/run-01/attempt-01/candidate.glb`
- 配信: `public/models/yellow-524-mascot/model-c14.glb`
- SHA256: `2652765f480973f6d974d0c51b2bd294c6e4c81e32e369814e09ba6c03c3e418`
- 18,199,668 bytes、449,022三角形、20骨、配信頂点225,403。
- 内蔵アニメーションは **Floating_Ripple** だけ。4秒、121フレーム、60チャンネル。6つの下端が順に波打ち、上下に漂う。独立した喜び・被弾・歩行クリップはない。

GLBはバイト単位で元と同一。形状・法線・材質・テクスチャ・骨・ウェイト・アニメーションを変更していない。`source-c14.json` は元制作レポート、`reference.png` は元の参照画像。`scripts/adopt-companion-524.mjs` で固定SHAを検証して再取り込みできる。

## 動作と操作

各部屋に1体、最初の焚き火から約5.3m離れた `(46, 53.5)` に配置。表示倍率1.15、体の中心を床から0.94m上に置く。通常の待機、追従、帰還、喜び、被弾中のすべてで元の浮遊クリップを継続する。

| 操作 | コントローラー | キーボード／画面 |
| --- | --- | --- |
| 近づいて撫でる | 下側の **×** | **V**／「524を撫でる」 |
| 追従を解除 | **OPTIONS** →「524をキャンプへ帰す」→ **○** | **T**／帰すボタン |
| 攻撃 | 既存の攻撃操作 | **F**／モデルをクリック |

2.6m以内で、壁越しではなく、攻撃・ジャンプ・乗り物・調理などの最中でなければ撫でられる。×はこの条件で撫でる操作になり、それ以外の乗降・メニューの既存動作を維持。撫でると1.8秒の軽い弾みと左右の傾き、消えていく5つのハートが出る。1.2秒の連打制限があり、最後に有効に撫でた1人を追う。

追従ではプレイヤーの後ろ約2.1mを目指し、静的障害物・地形の通行判定に従う。歩行クリップは使わず、床の高さに合わせて浮く。帰還は通った経路を記録して逆にたどり、途中で薪などが再出現して塞がれた点は避ける。接続終了・行動不能・乗船・遠距離ワープでも帰還する。保存時の位置と帰路を保持し、再起動時の追従者は古いプレイヤーIDを追い続けず帰還する。旧保存にはキャンプの524を追加する。

524は自分から攻撃せず、体力・死亡・戦利品も持たない。既存の近接攻撃・魔法の射程、方向、壁、衝突、高さ判定を通った攻撃だけを受ける。1撃ごとに攻撃方向へ小さな速度を加え、指数減衰で約0.74m移動。連続攻撃ではその都度加算し、壁を通り抜けない。650msの浮いた反動と傾きを重ね、倒れて地面に接地する動作にはしない。撫でられていない状態で押された位置はそのまま維持する。

位置、追従者、撫でる・被弾イベントはサーバーが決定し、全員へ同期する。描画側は補間・浮遊・ハートだけを担当。共有するGLB資源は保持し、終了時は個体の骨格・ラベル・エフェクトを解放する。

## 検証

- 全682テストが通過。その後、再出現した資源が帰路を塞ぐ再現ケースを追加し、最終コードの関連15テストが通過。
- 型・strictを含むビルド、373 JSの構文、依存境界、変更ファイルの書式を確認。
- 全46素材の総合検証が通過。524のGLB構造検査も通過し、未ウェイト頂点・不正ウェイト・不正骨参照は0。
- 実Chromeの2描画画面＋通信3人で、初期画面からの選択・徒歩で接近、×で撫でる、ハート、後方追従、OPTIONSから解除・帰還、Fによる4回／3方向の被弾、V操作、タップ、再読み込み、遠近の表示復帰を確認。
- 1280×800、390×844、844×390で表示を確認。コントローラーはGamepad API入力を模擬し、実機の物理ボタンは未検証。
- 配信GLBの全225,403頂点を待機・喜び・被弾の51姿勢で検査。計11,495,553頂点サンプル、床からの最小隙間約0.38m、ループ端の位置差0。
- 実操作と準備を区別：最初の接近と追従は通常の移動入力。方向別被弾のプレイヤー位置・向き、他参加者の退避、メニューのフォーカス、観測カメラだけを独立したメモリ内世界で準備した。ユーザーの保存データへ検証用の位置・持ち物は書き込まない。

再現: `node scripts/qa-companion-524.mjs`。証拠は `output/playwright/companion-524/` に保存。

- `source/inspection.json` と `source/latest-c14.png`: 元モデルと実際のクリップ再生。
- `glb-inspection.json`: 最終GLBの構造検証。
- `game/result.json`: 実ゲームの観測・合否・描画情報。`game/*.png`: 13画面。
- `tests-full.log`、`tests-focused.log`、`assets-validation.log`: 自動検証結果。
- `normal/`: 通常ポート3000の配信照合と起動記録。

通常3000番は作業時点で停止していたため、最新の `scripts/dev.mjs` を非表示で起動した。稼働中の別プロセスは停止していない。現在の保存から起動し、保存状態正常・予備からの復旧なしを確認。配信中の14ファイルのSHAがローカルの最終版と一致し、元Candidate 14のSHAも再照合した。古い保存の再適用、展示／PC2更新、公開デプロイ、コミットは行っていない。

元モデルは449,022三角形で、今回は形状をそのまま採用し、新たなLODは作っていない。85m以遠では描画を止め、影は34m以内。性能改善や安定したフレームレートは主張しない。
