# 自分で移動して、近くで操作する

2026-09-09のユーザー指定。地面／海面クリック移動、マンモスへの自動追従・自動騎乗、炉・畑・住人・魚場・貝場・海岸・狩場・城・探索目標・交換場所への移動ボタン、遠征を削除した。地図は位置と距離の確認に使う。キーボード・左スティック・画面の移動パッドによる移動と走行、カメラ、ジャンプを継続する。

マンモスの生存・空席・距離・遮蔽・攻撃動作・被弾後の待ち・空中状態を共通判定し、乗れる時に△／Rの案内を表示。接近だけでは乗らず、押した時にサーバーが再確認する。船も近接と遮蔽を確認して案内する。過去の目的地コマンドを受理せず、過去の経路を残した人物・乗り物も入力なしでは動かない。住人・敵・野生動物のAI、近くで使う異界の入口、死亡からの回復は継続する。

全359テストが通過。最終の関連20テスト、strict含むビルド・型検査、176 JavaScriptの構文、依存境界を確認。廃止機能を前提としていたテストは入力と停止の検証へ変更し、AIの経路・城の通行判定を保持した。

実Chromeではタイトルからの参加、地面クリック・遠距離R・地図の選択で移動しないこと、全案内画面、手動Wで接近→△案内→△で騎乗→Wで移動→Rで降車、別画面への騎乗状態同期、再読込を検証。1440×900、390×844、844×390で地図と乗車案内を確認し、ブラウザーエラーと目的地コマンド送信は0。プロフィール、Gamepad API、マンモス近く／湾への開始位置、マンモスの待機はQA準備値。実際の接近・乗降・操作はブラウザー入力。物理コントローラーや物理タッチ端末は未検証。

ユーザーの追加許可で3000番へ反映。PID20716のEXIHIBITIONの1セッションを切替直前に新しく保存し、持ち物・装備・位置・進行・資源・畑・世帯・住人位置を照合してPID30044へ復元した。今回限りの `node output/manual-movement/run-local.mjs` を使用し、古いチェックポイントは再利用しない。通常ディスク永続化・展示パッケージ／PC2更新・公開デプロイは追加していない。

再現：`scripts/qa-manual-movement.mjs`、通常版の準備値なし確認：`scripts/qa-manual-movement-local.mjs`。証拠：`output/playwright/manual-movement-20260909/`。切替・復元記録：`output/manual-movement/rollout.json`、`restoration-check.json`。確認版は `http://localhost:3013/`、通常版は `http://localhost:3000/`。
