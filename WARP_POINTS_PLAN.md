# 焚火のワープポイント

各地の既存の三次元焚火を共通のワープ地点として登録する。地図の炎マークをクリック／タップするか地点一覧で選び、「ここへワープ」で確定する。通常の地点は未訪問でも無料で選べる。影の庭だけは既存の旅の証3つの条件を保つ。

サーバーが地点ID、行動状態、3秒の連続移動間隔、着地点の地形・建物・人物との衝突を検証する。徒歩時にどこからでも利用可能。乗船・騎乗・肩乗り・力尽き・跳躍・攻撃・作業中は理由を表示して待つ。持ち物・元気は維持。確定後に地図を閉じ、描画・カメラ・移動予測を着地点へ合わせる。

全地点と全体格の着地、偽造ID、状態制限、複数人の同期、保存と復帰をテストする。別ポートの独立保存で実ブラウザーの地図操作・縦横画面を確認する。展示版・PC2・公開版の更新は今回の対象外。

## 実装と確認結果

- `shared/warp-sites.mts` は三次元配置と同じ30焚火を登録。地形やGLBの追加・置換なし。
- `shared/warping.mts` が固定IDと行動状態を検証し、既存衝突処理で8m以内の空き位置へ移動。既存の保存処理が着地後の位置と進行を保存する。
- 地図は操作欄を上に置き、炎マークも通常のボタンとしてマウス・タッチ・キーボード・既存Gamepadナビゲーションへ接続。世界全図は大陸と湾を収める倍率に変更。選択時に操作欄を表示し、到着の配信で閉じる。
- 移動後の古い地域の破棄で発見した、資源の存在しないラベルを削除する処理も修正。
- 全450テスト（追加10）成功。30地点×7外見の着地、5人が同じ地点へ到着する場合の分離、偽造ID・状態制限・連打、通信2人、保存の出力と復帰、短距離の描画予測、地域の破棄を検証。
- 実Chrome2画面、実時間の独立メモリサーバーで地図から南米と湾へワープ。クリック・地点一覧・キーボード決定・既存の影の庭の条件・3秒の間隔・再読込復帰を確認。1440×900／390×844／844×390で横はみ出しなし、ブラウザーエラー0。実ブラウザー検証は独立メモリサーバー、ディスク保存の検証は既存の全体テストに含まれる。準備値は名前とガイド設定のみ。
- 通常3000番はPID43292の既存終了処理で最終保存成功（revision2266→2267）。最新保存からPID52700を2026-09-10 11:47:34に非表示起動。自動保存、9配信ファイル一致、通常タイトルを確認。
- 物理コントローラー・長時間負荷・全地形は未検証。展示版・PC2・公開版は未更新。

再現は `node scripts/build.mjs`、`node --test tests/warping.test.mjs`、`node scripts/qa-warp-points.mjs`。証跡は `output/playwright/warp-points/`、全テストは `output/warp-tests-final.log`。
