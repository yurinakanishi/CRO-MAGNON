# 通常サーバーの自動保存と再開

**2026-09-09：今回の修正は完了。** この計画の実装・検証・通常版への反映は完了し、残る拡張・未検証条件はすべて [将来対応](FUTURE_WORK.md) に移す。次の負荷検証などを今回の続きとして実施しない。

2026-09-09開始。水やりまでの既存ゲームを継続し、ユーザーが次に進めると承認した「通常サーバーの再起動をまたぐ自動保存」を実装する。新規ゲームの作成ではない。過去の「通常ディスク永続化は今回の範囲外」は前の機能追加の記録で、この計画で通常Node版へ追加する。

## 仕様

- 通常の `node server.mjs` と開発起動で、共有世界と個人の進行を自動保存する。既存の `createGameServer` を直接使うテスト・分離QA・展示側の起動は従来どおり保存を明示しない限り使わない。公開サービス・展示パッケージ・PC2を更新しない。
- 保存先は公開素材やビルドと別の、Git対象外の専用ディレクトリ。10秒ごとの保存と正常終了時の最終保存。強制終了・停電では最後に成功した保存まで戻る可能性がある。書込失敗をログと接続中の画面へ示し、保存成功と誤表示しない。
- 同じ保存先へ二つのプロセスが同時に書かない。完成した一時ファイルを置き換え、前回の有効な保存を予備として保持する。切れた書込・壊れたJSON・未対応版で空の世界を上書きしない。予備へ戻す場合は回復を明示する。両方が使えなければ起動を止め、元のファイルを残す。
- 通常版では無人になった部屋と個人の復帰情報を時間切れで捨てない。既存の世界・人物・持ち物・装備・進行・畑・資源・貝塚・共同食料・世帯・住人の記録を既存export/importで引き継ぐ。保存する個人を100人で黙って切り落とさない。現在の同時接続上限5人は維持し、保存人数と混同しない。
- タイトルへ戻る・タブを閉じる・ブラウザーとサーバーを再起動する時も、同じブラウザーと接続先から自分の人物を再開できる。人物変更で既存の持ち物がリセットされる操作は区別する。復帰用トークンを公開スナップショットや静的配信へ出さない。
- 途中の跳躍・移動入力・交換・採集／料理の作業は再開しない。船／騎乗の復帰は既存の安全な降車・岸への移動を使う。プレイヤーの自動移動・遠征は復活させない。
- 暦・作物の完了日時・世帯の滞在期限は既存の時計基準を継続する。住人は無人／停止中の日数分の採集・水やり・夕食をまとめて行わない。

## 検証と反映

- [x] 保存先・ロック・一時ファイル・予備・検証と回復・書込の直列化。
- [x] 起動時復元、定期保存、正常終了、開発サーバーの再起動、保存失敗の表示。
- [x] 個人の長期復帰、タイトル／タブ／新しいブラウザー画面からの再開、明示した人物変更との区別。
- [x] 実ファイルと再起動、強制停止、破損／予備／未対応版、二重起動、無人、複数プレイヤーと100人超の保存を検証。
- [x] 分離ビルド・全テスト・strict・構文・依存境界・書式、既存素材の保持。
- [x] 実Chromeでプレイ→保存→サーバー再起動→本人と共有世界の再開、複数画面・縦横画面と操作の確認。
- [x] 通常3000番の最新の状態を直前に照合して反映。必要ならその時点の進行を新しく移し、古いチェックポイントを再利用しない。
- [x] MDと証拠を更新し、今回の変更をcommit・pushする。

保存機能はゲーム運用上の仕組みで、考古学的な制度の再現ではない。通常版だけの保存にし、今の世界・タイトル画像・動作・国・種族・武器・水やりの仕様を継続する。

## 完了記録

全391テストと実Chrome2プロフィール、通常3000番の準備値なしの確認を完了。通常サーバーをPID5884へ反映し、古いチェックポイントの再適用なし。検証と制約は [記録](assets/local-save/README.md)、[検証結果](assets/local-save/qa-summary.json)、[反映記録](assets/local-save/rollout.json) を参照。
