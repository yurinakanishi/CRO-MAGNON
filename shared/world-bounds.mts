// Playable extent. The original Earth projection keeps its own coordinates.
export const WORLD_BOUNDS = Object.freeze({
  width: 8192,
  depth: 4096,
  minX: -4288,
  maxX: 3904,
  minZ: -1440,
  maxZ: 2656,
});
