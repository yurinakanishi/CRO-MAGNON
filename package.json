{
  "name": "cro-magnon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A five-player prehistoric gathering game.",
  "scripts": {
    "prepare:vendor": "node scripts/prepare-vendor.mjs",
    "prestart": "npm run prepare:vendor",
    "predev": "npm run prepare:vendor",
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify:assets": "node scripts/verify-world-assets.mjs",
    "check": "node --check server.mjs && node --check src/main.js && node --check src/world3d.js && node --check src/world-assets.js && node --check src/world-scenery.js && node --check src/source-surface-fit.js && node --check src/models.js && node --check src/model-review.js && node --check src/character-assets.js && node --check src/character-animation.js && node --check scripts/prepare-vendor.mjs && node --check scripts/inspect-glb.mjs && node --check scripts/verify-world-assets.mjs && node --check shared/terrain.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "three": "^0.185.1",
    "ws": "^8.18.3"
  }
}
