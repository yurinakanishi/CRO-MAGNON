## 2026-09-11 草の株を作り直し、「ぱらぱら草」を追加、草をまばらに

ユーザー指摘（草の塊も気持ち悪い、もう少しまばらでよい、一、二本ぱらぱらとある感じのも作って）に対応。`meadow-grass` は細い針状の葉が密集した旧復元（134,051三角形）が原因だったため、幅広くなめらかな9枚の葉の参照画像v2をBlenderのアートディレクションレンダーで用意し（このホストに画像生成モデルはない）、ローカルTRELLIS-2 1024・seed 42で復元、元形状保持の軽量化でcandidate-02（44,457三角形、3.45 MB）を採用。開いた葉の縁で二次誤差収縮が35k三角形で止まるため、それ以上は減らしていない（近景1.1 m以内だけ実メッシュ、以遠は従来どおり実GLBのインポスター）。新規 `meadow-sprig`（ぱらぱら草、高さ0.45 m）は1〜2枚の葉の参照画像v1から同じ系統でcandidate-01（12,550三角形、0.94 MB）を採用。両方とも葉のテクセルだけ輝度帯 0.16〜0.36 に較正（針葉樹と同じ）。作物（火根草・香り草）は従来どおり `meadow-grass` を色替えで使う。1回目のTRELLIS実行はCUDA起動エラーで失敗し、ログを `dense-v2-res1024-seed0042.failed-cuda-1.log` として保持して再実行した。

配置は `shared/scenery-layout.mts` の谷の草と区画生成の草を専用の乱数列で60%に間引き、そのうち35ポイントを株、25ポイントをぱらぱら草にした（草原のみ。雪原・砂漠の地表被覆は従来の種類のまま）。谷の草10,015→5,988か所。`src/world-scenery.ts` に `meadow-sprig` の `LandscapeInstances`（距離 1.1／1.1／24 m）、`src/biome-surfaces.ts` に地域材質プロファイル、`assets/world-models.json`・`public/models/world-assets.json` に登録。採用は `scripts/adopt-foliage-revision.mjs`。全470テスト、`npm run check`、3044番の確認用サーバー（保存先 `output/pine-qa-save`、終了済み）の実ブラウザーで両GLBの読込と開始地点の近景を確認、consoleエラー0。通常3000番の停止・再起動・保存再適用・展示更新・公開デプロイなし。区画生成の草の実ブラウザー確認、雪原・砂漠、物理端末は未検証。制作記録は `output/model-generation/models/meadow-grass/`（`source/original/reference-v2.*`、`work/trellis/dense-v2-*`、`work/low-poly/candidate-02`、`qa/adoption-review-rev02.json`）と `meadow-sprig/`（`reference-v1.*`、`candidate-01`、`qa/adoption-review-rev01.json`）、ゲームQAは各 `qa/game-qa-2026-09-11.json`。

## 2026-09-10 松の木を丸みのある樹冠へ作り直し、森を間引き

ユーザー指定で `valley-pine` をrevision v2へ更新した。粒状で不気味に見えていた針葉の見た目を、滑らかな丸い樹冠層へ作り直した（`scripts/adopt-pine-revision.mjs`、参照 reference-v2、ローカルTRELLIS-2 1024 seed42、280,215→12,609三角形、LOD1は5,043三角形）。高さ10 mは維持し既存の配置・スケール計算に変更はない。`assets/world-models.json`・`public/models/valley-pine/asset.json`・`public/models/world-assets.json` のSHA・寸法・provenanceを更新し、今回はClaude（Fable 5.1／Claude Code）が制作したため `claudeUsed: true` を記録（旧Codex製資産は引き続き `false`）。`scripts/verify-world-assets.mjs` の検証を、`claudeUsed` の固定値チェックから型チェック（boolean）＋`provider` の非空文字列チェックへ変更し、両方の由来を許容する。

同じ指定で森全体を大きく間引いた。`shared/scenery-layout.mts` のランダム散布と遠景の `valley-pine` 配置に、既存の乱数列とは独立した `seededRandom(20260910)` による間引き判定（保持率0.4）を追加し、他の草・岩などの配置は変えない。`shared/adventure-layout.mts` の冒険地域の林は12本→6本、`shared/gulf-region.mts` の集落の並木は7本（8 m間隔）→4本（16 m間隔）、湾の240本のグローブ散布は5個中2個のみ保持に変更。`tests/gulf-expansion.test.mjs` は座標固定の1本を見るアサーションから、間引き後も残る大陸側の木の存在チェックへ更新した。

本エントリは、実装セッション終了後の別作業でこのファイルを誤って `git checkout` し、当時の検証記録（テスト件数・実ブラウザー確認など）を含む元の記述を失ったため、残っていたコード・アセット差分から事実のみを復元して書き直したもの。上記以外の検証状況は不明。

## 2026-09-10 橋のたもとの焚き火を集落側へ移動

ユーザー指摘（橋のすぐそばの焚き火が邪魔で通りにくい）で、`shared/scenery-layout.mts` のオルの集落の焚き火を (72, 43) → (75, 40) に移した。旧位置は木橋の東端（x≈71.07、床 z 42.2〜44.9）から1.06 mで渡り口をふさいでいた。新位置は橋端から5.3 m、オル (70, 41) から5.1 m、テント (73, 37) から3.6 mで、オルとテントの間に収まる。座標由来のワープIDは `fire-72-43` → `fire-75-40` になるため `shared/warp-sites.mts` の名前表を更新（表示名「オルの集落」は同じ）。料理は最寄りの焚き火を探すので影響なし。

橋の中心線 z 43.0〜44.0 を半径0.4 mで x±10 m走査し、両端5 m以内の橋の欄干以外の障害物は0（オルは橋の横 z=41 で通路外）。西端から6.5 m西のテント (54.5, 44) は直進線上に残るが「すぐそば」ではないため据え置き。全470テスト、strict含むビルド、248JS構文・依存境界を確認。通常3000番の停止・再起動・保存再適用・展示更新・公開デプロイなし。実ブラウザー目視は未実施。

## 2026-09-10 マンモスと紫尾の巨獣を一回り大きく

ユーザー指定で両者を1.25倍にした。マンモスは `shared/scenery-layout.mts` の配置スケールを `(index ? 0.76 : 1) * 1.25`（1.25／0.95）にし、当たり判定半径（`animalRadius`、mammoth-1は2.95→3.66 m）・自由行動と乗馬の速度・歩行クリップの再生速度は既存設計どおりスケールに追従する（乗馬の歩き0.7→0.875 m/s、走り2.25→2.81 m/s）。紫尾の巨獣は `shared/behemoth-rules.mts` に `scale: 1.25` を追加し、体の半径2.1→2.625 m、尾の届く距離4.35→5.44 mを同じ倍率で導出。`createBehemoth` は `R.scale` を配信し、クライアントは既存の `state.scale` で描画する。噛みつきの余裕0.15 m、速度・体力・縄張り26 m・空き地32 mは据え置き。GLB・配信SHAの変更なし。

乗馬速度を固定値で前提していた `tests/riding.test.mjs`・`tests/riding-server.test.mjs` をスケール参照に更新。全470テスト、strict含むビルド・型、248JS構文・依存境界・書式を確認。確認用に保存先なしのメモリ内フィクスチャ（127.0.0.1:3044、部屋 SCALE-QA、セッション一時フォルダーのスクリプト、`/qa/stage` で人物を移動）を実ブラウザーで開き、mammoth-1（半径3.66 m）と巨獣（半径2.625 m）の前に立って両方の描画とconsoleエラー0を確認後に停止し、`.claude/launch.json` の一時項目も削除した。通常3000番（PID35152）・3022・3032・3033は停止・再起動せず、展示更新・公開デプロイなし。巨獣の攻撃・突進の当たりの体感、大きくなったマンモスの騎乗姿勢の目視、物理端末は未検証。

## 2026-09-10 地図で「ここへ行こう」を仲間へ共有

ユーザー指定で共有ピンを追加。地図の任意の場所または焚き火を選び、「ここへ行こう」で同じ部屋に共有する。名前付き通知・会話履歴、本人の色の菱形を地図とミニマップへ表示。範囲外はミニマップのその方向の端に表示する。仲間一覧からピンへ地図を寄せられる。1人1本で置き直し・本人削除、5分または退出で消える。途中参加へ配信し、ディスク保存には残さない。地図の矢印と「中央にピンを打つ」でコントローラーも指定できる。クリック選択・ドラッグのみでは送信しない。

サーバーが有限座標・世界の範囲・本人の名前と色・期限を決定し、1.5秒の送信間隔と既存通信制限を適用。持ち物・位置・移動・作業・元気・ワープ条件は保持。全470テスト（新規5）、strict含むビルド・型、248JS構文と依存境界・書式・diff、最終関連15件を確認。実Chrome2画面の通知・ミニマップ・一覧・共有・置き直し・本人削除・キーボード・模擬Gamepad、1440×900／390×844／844×390でエラー0。人物位置と持ち物の準備値なし。物理コントローラー・実タッチ端末・長時間負荷は未検証。

確認用 http://127.0.0.1:3033/ はPID59368、保存先は独立した `output/map-pins/preview-save`。6配信SHA一致。通常3000番はPID35152・開始2026-09-10 12:21:27を照合したが、自動承認審査が「実装の依頼は稼働中サーバーの終了・保存更新・反映を許可していない」と拒否。停止・再起動・保存再適用は0、通常反映のみ明示承認待ち。未対応サーバーでは共有ピン操作を非表示にする。承認後は直前のPID・開始時刻と最新保存を再確認し、既存終了ハンドラーを使用する。古い保存を再適用しない。展示／PC2更新・公開デプロイなし。詳細は MAP_PINS.md と output/playwright/map-pins/。

## 2026-09-10 一時停止メニューの残課題を解消（右スティック・スマホ幅・QA更新）

ユーザーの「fix all issues」指定で、タブ化したメニューで未検証・未更新だった項目を潰した。`src/gamepad-ui.ts` の右スティックスクロールは、ダイアログ本体ではなく選択中の項目の祖先か、表示中のパネル（`.pause-panels`）のうちスクロールできる要素を対象にする。`src/style.css` はスマホ幅で内容が横にはみ出していた点を `minmax(0,1fr)`・`min-width:0` で修正（下のボタンとパネルが右へ切れていた）。`src/main.ts` は未使用になった単独の目標ダイアログ `openObjectives` を削除。

`scripts/qa-menu-directions.mjs` をタブ構成に書き換え、タイトルの全画面ボタン・7枚（1000px以下4列・430px以下2列）のキャラクターカード・レール／横帯のタブ移動・キーボードのEnter／ESC（タブ選択、手帳を開く、閉じた後の `display:none`、最後のタブの記憶）・押しっぱなしの↓と右スティックのパネルスクロールを検査する。`scripts/qa-gamepad-menus.mjs` は端で折り返さない仕様に合わせて到達手順を「動かなくなるまで進んで方向転換」にし、操作説明のスクロール判定を `.pause-panels`、タイトルの収まり判定を `.title-menu` に変更。両QAとも実Chromeで全項目通過、ブラウザーエラー0（`output/playwright/menu-directions-20260910/r08`、`output/playwright/gamepad-menus-20260910/r05`）。全455テスト（`local-save` の1件はQA並走中の負荷でタイムアウトし、単独再実行で通過）、strict含むビルド、240JS構文・依存境界を確認。通常3000番は再起動せず、展示更新・公開デプロイなし。物理コントローラーは未検証。

## 2026-09-10 ハンバーガーアイコンを削除

右上のHUDからメニューのハンバーガーアイコンを削除し、空いた最上段へミニマップを移動。接続表示は暗い半透明背景・枠・余白を持つカードにして読みやすくした。メニューはESC／OPTIONSで開く案内とQAを更新。テストはユーザー指定により未実行。

## 2026-09-10 一時停止メニューを左タブ＋右パネルの構成へ

ユーザー指定で、一時停止メニューをよくあるゲームUIの「左側のタブ・右側の内容」に変更。タブは「目標と野営地」「もちもの・道具」「世界と仲間」「設定」「操作説明」の5つで、目標・もちもの・操作説明はメニュー内に直接表示する（`objectivesMarkup`・新規 `inventoryMarkup`／`bindInventory`・`helpMarkup` を I／H の単独ダイアログと共用）。「世界と仲間」には世界地図・探索手帳・三つの国・魚場・貝塚・集落・部族・乗降・手をふるを配置。「設定」は環境音・全画面・視点（遠く／近く／戻す）を項目ごとの行にし、コントローラー接続状態と自動保存の状態を表示。タブの下に「探索に戻る」「部屋・キャラクターを変える」「タイトルへ戻る」のボタンを置いた。右上の「戻る」は保持。旧「設定」横並び・PS4長文説明（`gamepadHelp`）はメニューから外し、`gamepad-connection` の更新は全要素へ反映するように変更。最後に開いたタブを同じセッション内で記憶する。

PCは幅1120px・高さ760pxまでの固定サイズで右パネルだけスクロール。860px以下はタブを上部の横並び、520px以下は下のボタンを2列にする。`data-controller-menu` の識別子は保持し、既存QAの `inventory`／`help`／`title` はタブまたはボタンとして選べる。全455テスト、strict含むビルド、240JS構文・依存境界を確認。稼働中の通常3000番（PID26356）は再起動せず、配信済みの新ビルドを実Chromeで 1440×900・390×844・844×390 の全タブ表示、矢印キーでの移動、I キーの単独もちものを確認しエラー0。ブラウザー操作ツールのEnter／Spaceは既存の「戻る」ボタンも発火しないため、キーボード決定と物理コントローラーは未検証。`scripts/qa-menu-directions.mjs` の移動順の期待値は新配置に未更新。展示更新・公開デプロイなし。

ユーザーの指摘（閉じてもメニューが上に残る）に対応。`dialog:has(.pause-menu)` の `display:flex` がブラウザー既定の閉じたダイアログの非表示を上書きしていたため、`dialog[open]:has(.pause-menu)` にだけ適用するよう修正。実Chromeで「探索に戻る」「戻る」の後に `open=false`・`display:none` を確認しエラー0。

## 2026-09-10 コントローラーの×でジャンプ

ユーザー指定でプレイ中の割り当てを「×＝ジャンプ、○＝調べる・採集、L2＝作業中止」に変更。メニュー内は従来どおり×・○・□・△で決定。HUD、共通あそびかた、旧操作説明、釣り・住人・湾の案内、READMEと操作書を更新した。

関連27テスト、strictを含むビルド、238JS構文・依存境界・対象書式・差分検査を確認。押しっぱなしの連続発火、メニューからの入力持ち越し、L2のメニュー内無効を検査。通常3000番の8モジュールの配信一致を確認。サーバー再起動・保存再適用・展示／PC2更新・公開デプロイなし。実ブラウザー操作と物理コントローラーは今回未検証。旧QAスクリプトの×採集・L2ジャンプ前提は未更新。

## 2026-09-10 大猿が城の中央階段上でスタックする問題

ユーザー指定で、最初の中央階段の上がり口を修正。大猿の半径0.76mに対して左右通路がつながらなかったため、元のTRELLIS城の床を80cm多く残すrevision08を採用。実GLBから床を再計測し変更20セル、材質・画像・全体寸法を保持。床格子に対する体の接触でも、城の回転に合わせて通れる入力成分へ横滑りする。体格・速度・壁と落差の制限は保持。

全460テスト（本修正の新規2、同時作業を含む）、型・strict設定・ビルド・244JS構文・依存境界を確認。全7外見×歩行／走行×左右の28往復、480本の実GLB床レイ検査。実Chrome2画面＋通信3人で大猿の左右往復と下り、予測OFF／ON、PC／縦横画面、1,242位置観測、ブラウザーエラー0。位置・カメラ等は準備値、移動は実キー・実時間。

通常PID52700の起動時刻とポートを照合し、既存終了ハンドラーで最終保存（revision2474→2475）。最新保存からPID35152（2026-09-10 12:21:27）を非表示起動し、自動保存・5配信一致・通常タイトルを確認。古い保存再適用・展示／PC2更新・公開デプロイなし。既存ネアンデルタール男性の参照画像SHA不一致で全制作資料検証は停止したが、城の検査と現行世界素材48GLBのSHA・容量は成功。物理端末・全地形・長時間負荷は未検証。詳細はCASTLE_STAIRS_FIX.mdとassets/castle-stairs/。

## 2026-09-10 今いる場所でキャラクターを変える

ユーザー指定で、メニューに「キャラクターを変える」を追加。7外見の画像を選び、金色の「このキャラクターにする」で確定する。選択だけでは変更しない。名前・部屋の入力や再参加なし。位置・向き・持ち物・元気・進行・個人ID・再開情報を保持する。従来の初期化する参加し直しはオンラインの「部屋を変える（参加し直す）」へ分離した。

サーバーが固定モデルID・状態・攻撃間隔と体格の空きを検査。最終訂正により、大きな体が壁や人物と重なるときは「少し広い場所で変更しよう」と案内する。通行半径の統一案は取り消し、既存体格・移動と攻撃の距離を保持。乗船・騎乗・肩乗り・力尽き・跳躍・攻撃準備・作業中は終わってから選べる。入力・保留攻撃・本人の光弾・肩乗りの誘いを解除する。状態配信で両画面のモデル、予測、HUDと保存プロフィールを更新し、遅れて完了／失敗した旧モデルの読込を破棄する。

全461テスト（今回新規8、同時作業の城3件を含む）、strict含むビルドと型、244JS構文・依存境界・対象書式・diffを確認。42通りの相互切替、偽造ID・状態と衝突・通信2人・保存復帰・予測・非同期読込を検査。実Chrome2画面で全7モデル、1440×900／390×844／844×390、選択・取消・確定、キーボードと再読込、手動歩行・攻撃を確認し最終エラー0。小画面の横はみ出しを修正。初回の城素材更新途中のSHA／長さ不一致と保存通知待ちのタイムアウトは、素材一致後・単独検査後に全体を再実行して成功。プロフィールとガイド設定のみ準備値、位置・時計・持ち物の設定なし。

通常3000番は確認時にPID35152（2026-09-10 12:21:27）へ切替済み。本作業による停止・再起動・保存再適用は0。通常UIの独立した確認用の部屋SWITCH-CHECKで切替・同位置・復帰・タイトルへ戻る、7配信SHAと自動保存成功を確認した。別作業の城差分を保持。展示／PC2・公開デプロイなし。物理Gamepad・長時間負荷・全地形は未検証。詳細は CHARACTER_SWITCH_PLAN.md、scripts/qa-character-switch.mjs、output/playwright/character-switch/。

## 2026-09-10 各地の焚火へ地図からワープ

ユーザー指定で、大陸・探索地域・湾の既存30焚火をワープ地点にした。M／地図から炎マークまたは名前一覧を選び、金色のワープボタンで確定する。地点選択だけでは移動しない。無料、徒歩時はどこからでも利用でき、通常地点は未訪問でも選べる。影の庭は既存の旅の証3つの条件を保持。乗船・騎乗・肩乗り・力尽き・跳躍・攻撃・作業中、移動後3秒は理由を表示する。

サーバーが固定地点IDと状態を検査し、体格・地形・建物・人物の衝突を避けて焚火の8m以内へ着地。持ち物と元気を保持し、入力・保留攻撃・本人の光弾・肩乗りの誘いを解除する。到着通知後に地図を閉じ、配信したwarpSequenceで描画・カメラ・予測を即時更新。地図は大陸と湾を収める倍率へ調整し、名前とワープ操作を地図の上に配置。PC／コントローラー共通のあそびかたにも追記。

長距離移動後の再読込で発見したRegionalSceneryの破棄エラーも修正。資源には存在しないラベルを削除していた旧処理を撤去した。全450テスト（新規10）、strictを含むビルド、JS構文・依存境界を確認。30地点×7外見、5人の着地点の分離、偽造ID・状態制限・通信2人・保存復帰・短距離の予測リセット・地域破棄を検査。実Chrome2画面で大陸間／湾へのワープ、マーカー選択・一覧・キーボード決定、1440×900／390×844／844×390、再読込を確認しエラー0。名前・ガイド設定のみ準備値、移動・時計・持ち物の設定なし。物理コントローラー・長時間負荷・全地形は未検証。

通常3000番のPID43292と起動時刻を照合し、既存終了ハンドラーで最新状態を最終保存（revision2266→2267）。PID52700（2026-09-10 11:47:34）を非表示起動し、自動保存正常・9ファイルの配信一致・通常タイトルを確認。古い保存再適用・展示／PC2更新・公開デプロイなし。詳細はWARP_POINTS_PLAN.md、scripts/qa-warp-points.mjs、output/playwright/warp-points/。

## 2026-09-10 HUDを最小限にし、目標ともちものをメニューへ

ユーザー指定でプレイ中の常時表示を減らした。左上の「目標」カードを削除し、一時停止メニューに「目標と野営地」を追加（3つの目標と野営地の木材・石はここで更新）。右上はメニューのアイコンだけにし「メニュー」「ESC」の文字を削除。右列の「探索手帳」「三つの岸の湾」の起動ボタンと、マンモス・敵の「discovery-card」を削除（手帳・湾はメニューから開く）。中央下は「ジャンプ」ボタンと「生／焼」の肉の数を削除し、攻撃ボタンと状況に応じた調べる・焼く・食べる・乗るだけを残す。木材・石・ベリーと採集・つくる・届ける・交換・走るの欄は、PCは Tab、コントローラーは L3（左スティック押し込み）を押している間だけ表示し、もちものの詳細はメニューの「もちもの・道具」で開く。右下のキー案内には「Tab／L3 もちもの」を追加し、地図・オンライン表示・左下の会話・地域名バナーは保持。

`src/gamepad-input.ts` に `PAD.l3` と `frame.hotbar`、`src/gamepad-ui.ts` に `onHotbar` を追加。あそびかたの注記とコントローラー説明にも Tab／L3 を記載。全441テスト（新規1、キー案内の期待値を更新）、型・strict・236JS構文・依存境界・書式を確認。実Chromeで 1440×900・375×812・844×390 の表示、Tab長押しでの欄の表示、メニューの「目標と野営地」を確認。起動時の `reading 'element'` のエラーは変更前のビルドでも同じ行で発生する既存の問題で、今回の対象外。通常3000番の停止・再起動・保存再適用・展示更新・公開デプロイは0。旧QAスクリプトのうち `#adventure-button` `#gulf-button` `#jump-button` `#discovery-card` を押すもの（qa-jump、qa-violet-behemoth、qa-barter、qa-crops など）は未更新で、メニュー経由への書き換えが必要。物理コントローラーでの L3 は未検証。

## 2026-09-10 あそびかたをPC／コントローラーのタブに統一

ユーザー指定で、タイトルと一時停止メニューの「あそびかた」と、出発直後の事前ガイドを同じ内容・同じ見た目にした。新規 `src/help-content.ts` にPC（キーボード・マウス）とコントローラー（DUALSHOCK 4）の操作カードを持ち、タブで切り替える。開いた時のタブは使用中の入力機器に合わせる。末尾の「まずは木材3と石2で石斧を作ろう」の案内、事前ガイドの前置き・「最初の目標」・脚注、旧コントローラー説明の長文は削除。一時停止メニューの設定欄にあるコントローラー接続状態の表示は保持。

PCではダイアログ・ガイドとも幅1120pxまで横に広げ、カードを自動列数で並べる。1440×900でPC 10枚・コントローラー10枚ともスクロールなし。640px以下は2列、430px以下は1列で縦スクロール。全440テスト（ガイドのテストをタブ前提に更新）、型・strict・236JS構文・依存境界を確認。実Chromeでタイトルからの表示、タブ切替、出発後の事前ガイド、スマホ幅を確認。通常3000番の停止、展示更新、公開デプロイは0。実機コントローラーでのタブ操作は未検証。

## 2026-09-10 キャラクター選択画面の簡素化と展示モードの固定

ユーザー指定で、タイトルの次の画面を「キャラクターを選ぶ」だけにした。「旅支度」「あなたの物語を、ここから。」の見出し・前置き、武器の説明文を削除。人間は「クロマニョン人 女性／男性」「ネアンデルタール人 女性／男性」として独立したカードにし、種族と性別を別々に選ばせない。全7外見に全身ポートレート画像を付け、クロマニョン人・ネアンデルタール人の4枚は現行GLB（Idle_Loop）を三次元描画して420×480で追加。HUDの左上と仲間一覧のポートレートも同じ画像を使う。出発ボタンは金色のワイドボタンに変更。PCでは7枚を1列に並べ、1440×900・1366×768でスクロールなし。1000px以下は4列、640px以下は3列、430px以下は2列でスクロール可。

展示LAN（`MULTIPLAYER_MODE=lan`）では名前と部屋の入力欄を出さず、PC1はプレイヤー1、PC2はプレイヤー2、部屋はEXHIBITIONに固定する（`?room=` と保存済みの部屋は無視）。オンライン版の名前・部屋入力は保持。展示ビルドとCloudflareビルドの同梱一覧に7枚のポートレートを追加（展示版はこれまで大猿の画像も未同梱だった）。

全440テスト、型・strict・237JS構文・依存境界を確認。LAN設定の別ポート配信で入力欄なし・固定名・部屋・HUDポートレートを実Chromeで確認、エラーはLANサーバー未起動のWebSocket失敗のみ。通常3000番の停止・再起動、保存再適用、展示パッケージの再作成、PC2更新、公開デプロイは0。`scripts/qa-menu-directions.mjs` のカード移動の期待順（旧2列）は新しい7列配置に未更新。物理端末・コントローラーでの選択操作は未検証。

## 2026-09-10 肩からの魔法に大猿の移動慣性を加える

ユーザーの後続指定で、光弾へ発射時の大猿の衝突解決後の速度をベクトル加算。前方走行5.4m/sなら光弾は地面に対して12.4m/s。壁で止まる方向の入力速度は加えず、発射後の旋回・停止・降車で光弾を曲げない。合成方向・速度・距離を配信し、既存描画も同じ経路を使う。寿命8/7秒を保持し、地面に対する距離は慣性の分伸びる。静止時の射程・威力・元気・間隔・壁と命中は保持。

全438テスト（新規5）と最終関連27、strict含むビルド・型、235JS構文・依存境界・対象書式を確認。実Chrome2人の実走行で12.4m/s、両画面の光弾を大猿の前方約1.606mで観測、縦横画面と降車後攻撃、エラー0。初回のCommonJS配信を再ビルドで修復、QAのShift指定を既存走行ボタンへ訂正して再確認。速度・時計は設定していない。

通常PID22320の既存終了ハンドラーで最終保存・正常終了し、最新保存からPID36244（08:43:50）を非表示起動。4配信一致、タイトルと自動保存正常。古い保存再適用・展示／PC2更新・公開デプロイなし。同時作業の表示・展示手順差分を保持。詳細は SHOULDER_MAGIC.md の慣性追記と output/playwright/shoulder-inertia/。

## 2026-09-10 魔法使いが大猿の肩から攻撃する

ユーザー指定で、砂耳の魔法使いは肩乗りを維持して魔法攻撃できる。F・画面ボタン・既存Gamepad攻撃操作へ接続し、座った脚と腰を保つ腕の詠唱姿勢を追加。大猿は歩行・走行でき、途中の旋回で魔法の狙いを上書きしない。サーバーが正規外見と相互の肩乗り関係を検査し、威力・元気・連射間隔・射程・壁と床の判定は保持する。担ぐ側・魔法以外の同乗者・乗船とマンモス騎乗の制限は保持。

全433テスト（新規2）、strict含むビルド、234JS構文・依存境界・対象書式を確認。初回の既存実時間テスト1件は単独7件と並列数4の全433件で再確認。実Chrome2人のF・画面ボタン・肩乗り歩行・降車後攻撃、縦横画面、詠唱25描画フレームでエラー0。プロフィール・近接位置・QAの敵と動物の除外・カメラは準備値、攻撃と移動は実時間、時計変更なし。GLB変更なし。

通常3000番の停止を接続と試験bindで確認し、最新自動保存からPID22320（08:12:51）を非表示起動。自動保存正常、9配信一致、通常タイトルを確認。古い保存再適用・展示／PC2更新・公開デプロイなし。物理Gamepad・全地形・長時間負荷は未検証。詳細は SHOULDER_MAGIC.md。

## 2026-09-09 衝突で向きが強制的に変わる不具合を修正

ユーザー指定で、壁沿いの横滑り・敵の押し出しからプレイヤーの向きを計算していた共有移動と描画の二重処理を修正。手動移動は有効な入力方向、無入力・期限切れは直前の向きを保持。描画はそのfacingへ追従する。位置の衝突解決・速度・AIの実移動方向への旋回は保持する。

全428テスト（新規5）、strict含むビルド・型、228JS構文・依存境界・対象書式を確認。実Chromeのテント沿いの実キー移動（予測OFF／ON）と敵接触の押し出しを計248描画観測で検査し方向差0、エラー0。接触位置・カメラ・敵AI待機は準備値、移動と衝突は実時間。通常3000番PID40396を既存終了ハンドラーで最終保存・正常終了し、最新保存からPID43292を非表示起動。通常タイトル・2配信一致・自動保存正常を確認。初回の余分な起動は保存ロックで失敗し、二重書込なし。古い保存再適用・展示更新・公開デプロイなし。詳細は COLLISION_FACING_FIX.md。

## 2026-09-09 力尽きたキャラクターが膝から崩れる

ユーザー指定で選択可能な全7外見に1.8秒のDownedを追加。膝が折れ、両膝をつき、腕と胸から倒れて回復まで終端を保持する。砂耳の魔法使いは尾も横へ落とす。現行GLBへ専用クリップだけを加えたmodel-downed-r01.glbを採用。元GLB、形状・材質・骨格・ウェイトと従来の全クリップを保持する。

既存の致命傷hurtAtとdownedUntilで再生し、読み込み遅延・途中表示は経過時刻へシークする。歩行・攻撃・ジャンプ・騎乗姿勢から切り替え、回復後は通常動作へ戻る。4秒の回復・元気・持ち物・判定は変更しない。

全423テスト（新規7）、strict含むビルド・型、227JS構文、依存境界・対象書式を確認。全7最終GLBを120Hz・各217時点で独立検査し、全スキンの平面への接地・関節と既存データ一致を確認。4方向×6時点の画像と7動画を記録・画像確認。実Chrome2画面と通信3人の5人接続で魔法使い・大猿の被弾・終端保持・回復を観測しエラー0。近接位置・元気10・敵の攻撃開始と3人の通信は準備値、被弾処理と回復は実時間、時計変更なし。

通常3000番が停止していたため最新の自動保存でnode server.mjsを非表示起動（PID40396、ローカル開始2026-09-09 22:59:52）。全7GLB・2JS配信一致、通常UI開始と既存24日目への復帰を確認。古いチェックポイント再適用・展示更新・公開デプロイ0。全地形IK・坂や段差の全表面接触・全自己交差・物理端末／コントローラー・長時間負荷と常時60FPSは未検証。詳細はDOWNED_MOTION_PLAN.mdとassets/downed-motion/README.md。

## 2026-09-09 歩行・走行のかくつきとちらつき

ユーザーの指摘に対応し、LAN予測の表示位置だけを滑らかに補正する。通信で位置が戻った差分を歩幅や向きに使わず、衝突後の実移動の速度・向きを使う。小さい魔法使いの歩幅が1フレーム1.2cm未満だと待機へ戻る判定も解消。15Hzだった影を描画フレームへ同期させた。モデル・GLB・移動速度・サーバーの仕様は保持する。

全419テスト、関連38件、型・strictを含むビルド、225 JS構文・依存境界・対象書式を確認。新規3テストは全7外見の歩行／走行、通信の揺れと重複、停止・衝突・通信切れ・被弾・乗降等。実Chromeの人間・魔法使い・大猿で移動・旋回・停止・ジャンプ・攻撃、390×844／844×390、再読込を確認しエラー0。プロフィールのみ準備値、時計・位置・材料の設定なし。魔法使いの定常歩行に混ざった待機は54/96フレームから0/101へ減少し、再生速度は0.600m/s相当で安定。動画・各フレームを記録。常時60FPS、物理PC2／Gamepad、長時間負荷・全地形は未検証。

確認版は http://127.0.0.1:3023/ 。現在の展示版から2ファイルだけを替えた `output/exhibition-motion-r01`（build `83b51e80dd9db6091b10bf5740ce7a701ce18fedc98bd5aea6d2ed09f5affdcc`）を検証済み。55 GLBを含む残り291ファイルは一致。稼働中の4173番・8081番への切替は、自動承認審査が展示版への明示的な反映許可を要求して拒否したため未実行。PID49056と元ビルドを保持。直前の1部屋・1人の読み取りと復元照合のみ行い、停止・切替・保存の再適用は0。調査用デバッガーは終了した。許可後も新しく直前の状態を保存し、事前確認のスナップショットを再利用しない。

ユーザーの後続質問を踏まえ、PC2は今回の修正の対象外。読み取り接続はタイムアウトし、転送・更新・設定変更は0。別作業の展示ビルド補助・手順書の変更を保持。詳細は MOVEMENT_SMOOTHING_PLAN.md、assets/movement-smoothing/README.md。

## 2026-09-09 全長6mの敵「紫尾の巨獣」

ユーザーの新規指示「create as enemy monster」と支給画像に基づく追加。後続指定で後ろ脚は退化、全長は4mから6mへ訂正。胴体を保持して尾を2m延ばし、専用20骨格（尾7骨）と9動作を制作。imagegen参照v2→ローカルTRELLIS-2（1024、seed42）→形状保持の軽量化・尾の補修→rig revision-03を採用。67,223三角形、14,805,624 bytes。原本・dense・全制作版488ファイルをローカル制作フォルダーに保持し、参照・手順・SHAをassets/violet-behemothへ記録した。

ヨーロッパの草原の集落近く `(95,110)` に配置し、周囲32mを開けた。体力300、前方視界と遮蔽、700msの尾振りから5.15m/sの突進、噛みつき、360度の尾攻撃。大猿の5.4m/sより少し遅い。対象が半径26mの縄張りを出ると攻撃・追跡を中止し、元の位置へ歩いて戻る。未発見の背後からは検知しない。退化した後ろ脚に歩行を付けず、前腕と尾を動かす。旧保存へ追加し、復帰では体力を保持して途中の攻撃を中止する。

全412テスト、専用11件、strict含むビルド・型、217 JS構文・依存境界と対象書式を確認。全9動作の独立60Hz検査、6方向の姿勢と実ブラウザー再生を確認。r02の270画像・9動画を出力し、横倒れを修正したr03の死亡30画像・1動画を追加。表面・材質・残り8クリップはr02と一致。実Chrome2プロフィール＋通信3人で3攻撃・背後・離脱・実時間の帰還・キー移動・再読込・1440×900／390×844／844×390を確認しエラー0。近接位置・攻撃開始状態・死亡・プロフィールは準備値。時計変更0。全地形IK・全面の自己交差・物理端末／コントローラー・長時間負荷・常時60FPSは未検証。

確認用 http://localhost:3022/、起動補助 output/violet-behemoth/run-preview.mjs、保存は独立した output/violet-behemoth/preview-save。通常UIの開始とキー移動、最終配信SHAを確認。通常3000番の停止・再起動、旧保存の再適用、展示／PC2更新・公開デプロイは0。この追加は過去の将来課題全体を再開する指示ではない。詳細は TERRITORIAL_MONSTER_PLAN.md と assets/violet-behemoth/README.md。

## 2026-09-09 今回の修正を完了し、残りを将来対応へ移す

ユーザーの指示により、通常サーバーの自動保存までで今回の修正は終了。未実装・未検証・運用上の残りは FUTURE_WORK.md に集約し、すべて将来対応として扱う。過去の「次に行う」「残る」という記載を根拠に作業を自動継続しない。再開の指示が来てから対象・順序・仕様を決める。今回の終了処理はMarkdownの更新とmainへのcommit・pushのみで、追加実装・負荷検証・サーバー再起動・保存再適用・展示更新・公開デプロイは行わない。

完了済みの水やり・採集／夕食・ジャンプ・自動保存は未実装リストへ戻さず、既存の検証結果と制約を保持する。自動移動や遠征など、明示して削除した仕様は将来作業として復活させない。詳細と将来の一覧は [FUTURE_WORK.md](FUTURE_WORK.md)。

## 2026-09-09 通常サーバーの自動保存と長期の再開

既存ゲーム改善の継続として LOCAL_SAVE_PLAN.md を先に作り、通常 `node server.mjs` と開発起動へ共有世界・個人の進行の自動保存を追加。既定はGit対象外の `.cro-magnon-save/`、10秒ごと・参加／離脱後・正常終了時に保存する。空室と復帰情報を時間切れで捨てず、保存人数を100人で切り捨てない。同時接続上限5人は維持する。同じブラウザーと接続先から「はじめる」で再開し、タイトル・タブ・Chrome・サーバーの終了をまたげる。プロフィール画面での明示した参加し直しは個人のリセットを維持する。

一時ファイルのflushと置換、前回の有効な予備、SHA-256と既存復元処理の検証、同じ実ディレクトリのローカルTCP占有で二重書込を防ぐ。破損した原本を残して予備を復元し、両方が無効・未対応の将来版なら起動を止める。書込失敗と予備復元を画面へ表示し、メニュー更新でフォーカスを失わない。保存先はpublic／dist内を拒否し、トークンを通常の状態配信・静的配信・QA要約へ含めない。最終保存前に入力とtickを停止。Windowsの開発再起動は親IPCで保存後に終了し、失敗時は再起動を止める。

全391テスト、strict含む分離／通常ビルド・型検査、209 JS構文・依存境界・対象書式を確認。新規13テストで実ファイル、書込失敗、予備・破損・将来版・起動失敗・二重起動・111個人の保存・一年相当の無人・実プロセスの正常／強制終了を検査。111人は順次保存で同時負荷ではなく、一年は時計の準備値。初回全体実行で船の検査プロセスが詳細診断なしに終了し、単独4件とChrome終了後の全391件を再確認した。

実Chrome2プロフィールで手動移動、タイトル再開、Chromeと別プロセスのサーバーを閉じて再開、書込失敗と予備復元、1440×900／390×844／844×390、模擬Gamepadを確認しブラウザーエラー0。持ち物・槍先・畑・貝塚・共同食料・住人の持ち水・資源は準備値、時計変更0。実ファイルの保存・終了・復元と移動は実操作。途中の作業・跳躍・交換・プレイヤーの自動移動は復帰させず、無人時の住人の採集・水やり・夕食をまとめて処理しない。

既存68GLB・9タイトルPNGのSHAを保持、新規GLB・依存0。通常3000番の部屋0・参加者0とPID15972の開始時刻・ポートを直前に照合し、PID5884、開始UTC `2026-09-09T11:39:58.1527728Z` の `node server.mjs` を非表示起動。古いチェックポイント再適用0。通常UIの手動移動・タイトル・Chrome終了／再開・再読込・縦横画面と配信を確認し、位置・持ち物・時計の準備値なし。確認用SAVE-LOCALの無人の部屋と人物も正常に自動保存される。今後このサーバーを更新する時も最新の保存を使い、古い復元補助を再実行しない。

これにより過去の「通常ディスク永続化は未追加」は履歴となる。展示パッケージ／PC2、公開デプロイ、料金とCloudflare側は変更しない。強制停止では最後に成功した保存まで戻る場合があり、保存失敗中は10秒以内の損失を保証しない。物理端末・コントローラー、大人数・長時間負荷、複数PCが同じ保存先へ書く運用、停電耐性は未検証。詳細は assets/local-save/README.md、qa-summary.json、rollout.json。再現は scripts/qa-local-save.mjs、qa-local-save-host.mjs、qa-local-save-rollout.mjs。

## 2026-09-09 住人へ畑の水やりを頼む

既存ゲーム改善の継続として RESIDENT_WATERING_PLAN.md を先に作り、植えた作物ごとに住人へ水やりを頼む・取り消す操作を追加。自宅／訪問中の住人が朝と昼、夕食の実を採った後に泉へ歩き、3秒汲んで水を2まで持ち、畑へ歩いて戻って3秒水をやる。ベリーと香り草は水1、火根草は水2で既存の季節の成長時間を使う。一人一日一つまで、空いた担当を日別に巡回。夕べに間に合わなかった運搬は翌朝に続け、会話・旅・夕食と休息を優先する。

5m・遮蔽・作物・依頼・担当・作業・日付・水をサーバーで検査。手動水やりや同時刻の取消と再依頼で二重処理せず、住人の水を保持する。頼んでいない畑、プレイヤーの水・種・持ち物・実績、共同食料を自動で変えず、種まきと収穫は手動。依頼と持ち水・最後の水やりを保存し、旧保存は0と未記録。途中の作業は保存せず、復帰時に担当を選び直す。無人／停止中の日数分をまとめて処理しない。案内と住人の会話／一覧へ接続し、選択・Gamepadフォーカスを保持する。手順・量・泉・日課は創作で、古代の灌漑制度の復元ではない。

全378テスト、最終関連21、strict含むビルド・型検査、205 JS構文・依存境界・対象書式を確認。追加12テストで三作物×四季、全四集落と8訪問客の経路、夕食、翌日、取消・手動操作・保存・無人・8人の通信競合を検査。実Chrome2画面と通信3人の882観測で衝突違反・ブラウザーエラー0。依頼・取消・運搬・収穫・再読込・390×844／844×390・模擬Gamepadを確認。位置・種／水・訪問と日別記録・初期資源・共同焼き根1・3回の時計変更は準備値。歩行と作業は実時間で、時計変更は休息中の作物成長も進める。初回QAの横長画面の入口を通常ポーズメニューへ直し全行程を再確認。既存通信テストの40ms固定待機を実際の切断後の部屋解放を待つ検査にして全件を再確認した。

既存68GLBと9タイトルPNGのSHAを保持、新規GLB・依存0。通常3000番の部屋0・参加者0、PID30044と開始時刻・ポートを直前に照合し、PID15972の `node server.mjs` を非表示起動。古いチェックポイント・保存再適用は0。通常UIで開始・手動移動・遠い畑の操作不可・再読込・縦横画面と10配信モジュール一致を確認し、通常版では位置・材料・時計の準備値なし。展示パッケージ／PC2・公開デプロイ・料金・通常ディスク永続化は変更しない。

自動の種まき・収穫・漁労・狩猟、腐敗・長期栄養収支・人口変化・政治は未実装。物理端末／コントローラー・大人数負荷・長時間・常時60FPSは未検証。詳細は assets/resident-watering/README.md、qa-summary.json、rollout.json、再現は scripts/qa-resident-watering.mjs と qa-watering-rollout.mjs。以下の「自動農耕は未実装」は過去の記録で、今回の依頼された水やりだけを追加した。

## 2026-09-09 タイトル画像とクレジット

ユーザー支給の「CRO-MAGNON XI」画像を、追加許可「元画像をプログラムで切り抜く」に従い
実際のRGBA透過PNGへ加工してタイトルへ採用。原本と無加工コピーは保持。
追加指定で背景の3D世界を見せる元の構成に戻し、画像と「はじめる」「あそびかた」を中央、
透過画像を大きく、クレジットを最下部に配置。準備完了・マルチプレイヤーを削除し、右下はバージョンのみ。
紹介文・操作ヒント・QR説明文は削除。全画面表示は右上のアイコンのみ。
メニューは保存の有無によらず2項目。「はじめる」は保存があれば復帰、なければ旅支度へ進む。
制作は最終訂正により小文字の「yuri」。監修はX表示名の
Ryuichi-typeR、オータニ@AI駆動開発、浦田 勇樹 | 生成AI×新規プロダクト開発@ブレインパッド、
ぬこぬこ / NUKO 🇯🇵の順。識別用の@ユーザーIDは表示しない。全5人のQRをローカル素材にした。
最終指示「クリックとかのlink は入れてはいけない」に従い、名前・QRは静的表示のみ。
クリックリンク・クリックイベントを追加しない。QRを読み取るとプロフィールへ進める。

画像生成ツールの結果はRGBの市松模様だったため不採用。原本と無加工コピーのSHA一致、
プログラム抽出した素材のRGBAと透明画素・四隅のアルファ0を確認。手順はscripts/extract-title-art.py。
画質の追加指摘でrevision 2へ修正。毛並み・衣服・雪の明部を保護し、元画像のRGBを全画素で保持。
輪郭の色変換とCSSの白い発光を撤去。原寸1536×1024、RGBAとRGB一致、通常Chromeと配信SHAを再確認。
後続指定で、画像と同じアルファ輪郭に沿った半透明の白い背面をCSSで追加し、8〜18pxのぼかしで外へ透明にする。
画像自体はrevision 2を保持し、通常Chromeで表示と配信一致を確認。
その後「白いプチプチ」の指定でrevision 3へ変更。小さな白色成分を局所補修し、輪郭外の孤立片を除去。
顔の保護領域と文字のRGB、全体の97.44%のRGBを原本通り保持。透過・寸法・変更量と11配信SHAを検査。
手順はscripts/title_art_cleanup.py。白い輪郭ぼかし背景とレイアウトは保持する。
さらに「外ではなくマンモス等の画像内全体の白い点」と訂正され、revision 4を現採用。
built-in image_genで内部の白い飛沫を毛・衣服・岩の質感へ補修し、編集済みRGBから実透過PNGを抽出。
旧小成分補修は呼び出さない。編集原本と指示はassets/title-credits/cro-magnon-xi-clean-source.png、surface-cleanup.json。
支給原本・白い輪郭背景・配置を保持し、RGBA・編集済みRGB全一致・通常Chrome・11配信SHAを確認。
追加指定でCRO-MAGNON MMO版を別素材public/title/cro-magnon-mmo-transparent.pngに作成。
XI版と現在のタイトル表示は保持。編集元・指示・透過検査はassets/title-creditsのmmo関連ファイル。
展示向けにCRO-MAGNON DUO版も追加作成し、public/title/cro-magnon-duo-transparent.pngへ保存。
編集元・指示・透過検査はassets/title-creditsのduo関連ファイル。既存表示と展示パッケージは未変更。
QRの原寸296pxと表示111pxを独立復号し全5URL一致。strict含むビルド・型検査、
202 JS構文、依存境界、関連23テストを確認。実Chromeの通常／1440×900／390×844／844×390で
配置とスクロール、開始と戻る、方向キーを確認。最終タイトル内のリンク0件、名前とQRの
クリックで移動・タブ生成なし。物理QR読取端末と物理Gamepadは未検証。

通常3000番PID30044の開始時刻を保持してクライアント・素材を反映。配信SHAはassets/title-credits/delivery.json。
再起動・保存再適用・展示パッケージ更新・公開デプロイ・X投稿は0。今後のビルドにはPNGを含める。
詳細はTITLE_CREDITS_PLAN.md、assets/title-credits/README.md、qr-verification.json、delivery.json。
## 2026-09-09 大猿の歩幅・速度と魔法使いの肩乗り

ユーザー指定で歩行1.8m/s・走行5.4m/s（各2倍）、歩幅を歩行55%・走行30%拡大。実骨長と足裏に合わせた2クリップをrevision-13、model-stride-r13.glbとして採用し、元GLB・形状・材質・骨格・ウェイトと残り10動作を保持。近くで大猿が「担ぐ」、砂耳の魔法使いが「肩に乗る」で応じると左肩に座る。R／△／画面操作、拒否、15秒の誘い、肩に乗ったままの手動歩行／走行、双方の降車を追加。サーバーが距離・遮蔽・状態・空席・相手・期限を検査し、被弾・切断で解除する。関係は保存しない。

全376テスト、型・206 JS構文・依存境界、全12クリップの60Hz検査、変更2動作の60画像、2動画を確認／出力。実ブラウザー2人で誘い・拒否・承諾・歩行・走行・両側の降車・再参加、実iframeの390×844／844×390を確認。ゲーム側エラー0。プロフィール・近接位置・キー連続入力は準備値。確認用親ページのObserverエラーはapp保持でも再現し、解消済みとはしない（通常版・ゲームiframeは0）。物理端末／コントローラー、全地形の頭上接触・長時間負荷は未検証。

通常3000番の旧PID49044・開始時刻と直前の部屋0／参加者0を確認し、PID23432を非表示起動。16配信ファイルと通常UIを照合。保存再適用・通常永続化・公開デプロイ・展示更新0。詳細は SHOULDER_CARRY_PLAN.md、assets/shoulder-carry/README.md、output/shoulder-carry/local-rollout.json。以下の「肩乗り未実装」は前回の履歴。

## 2026-09-09 巨腕の大猿を追加

ユーザー採用の画像候補04と名称「巨腕の大猿」に基づき、ローカルTRELLIS-2（1024、seed42）で身長2mの赤毛の大猿を制作。元画像・conditioning・dense・全制作版を保持し、106357三角形、22骨格、12クリップのリグrevision-12を採用。選択可能な7番目の外見、素手の打撃、体格に応じた速度・衝突・カメラ、舟での足の姿勢を追加。既存モデルを保持する。

全369テスト、型・204 JS構文・依存境界を通過。全12クリップの独立60Hz検査、360画像の一覧と選択した原寸画像、実ブラウザー再生を確認し12動画を出力。2画面＋通信3人で5人表示・歩行・走行・ジャンプ・打撃・手振り・騎乗／降車、実iframeの390×844／844×390と再読込を確認しエラー0。プロフィール・連続入力・騎乗前の近接位置とマンモス待機は準備値。物理端末／コントローラー、全地形接触、長時間負荷は未検証。

既存0.78mの砂耳の魔法使いを変えず同一縮尺で座面を測定。膝乗りは寸法比較までで、プレイヤー同士の二人座り操作は未実装。配信参照56GLBのSHAと容量を独立照合し、制作元不足の旧資産の全制作系統検査とは区別する。

通常3000番は起動直前に待受がないことを確認し、PID49044の node server.mjs を非表示起動。古い保存の再適用・通常永続化・公開デプロイ・展示パッケージ更新・Meshmell投稿は0。詳細は GIANT_APE_PLAN.md、assets/giant-ape/README.md、output/giant-ape/local-rollout.json。確認用3018番の座面ビューを保持。

## 2026-09-09 既存キャラクターの全動作を見直し

後続指示「既存キャラの動き全て見直して」で、6外見と住人・オル、カラス人間、大小マンモスの8モデル・65クリップ、24種類の実行時姿勢を確認。16個の歩行・走行を、実寸の骨長・足裏のスキン変形・支持時間に合わせて修正した。上体と既存49クリップを保持し、急な切替の混合と歩行／走行の周期、焼き根料理の Eat を改善。revision-04 を別名 model-motion-r04.glb で採用し、元の59GLBを保持した。通常3000番はPID30044・開始時刻を保持し、再起動や保存再適用なしでクライアントと素材を反映。

採用後の全364テスト、型検査、190 JS構文と依存境界、全8配信GLBの構造・SHA・49クリップ・元の形状／材質／骨格の一致を確認。画像56枚・動画14本を記録。実ブラウザー2画面＋通信3人で5人の表示と歩行・走行・ジャンプ・攻撃・手振り、騎乗Idle／Moveを両画面で確認。プロフィール・連続入力と近接／マンモス待機は準備値。全状態の坂・橋・狭い画面・物理端末・物理コントローラー・長時間負荷は今回未検証。地形IKや指／顔／独立した毛・衣服の物理は追加しない。

生成元の画像と過去の採用記録がこの端末にないため、既存 verify:assets の全制作系統検査は完走できない。今回の配信8モデルは verify-motion-delivery.mjs で独立検査する。旧狩猟の比較は当時の配信アーカイブへ適用し、今回の新しい歩行と混同しない。詳細は CREATURE_MOTION_REVIEW_PLAN.md、assets/creature-motion/README.md、採用・照合JSON。公開デプロイ・展示パッケージ更新は0。以下の「今回はMD」は最初の依頼時点の履歴。

## 2026-09-09 生き物の動きの参考方針

ユーザー指定の [Lystrosaurus in Motion](https://lystrosaurus-motion-study.pages.dev/) と [@shinboson の紹介投稿](https://x.com/shinboson/status/2096390580594995519) を、生き物の動作制作の参考にする。詳細は [CREATURE_MOTION_GUIDE.md](CREATURE_MOTION_GUIDE.md)。人物・住人・動物・生物型の敵について、接地、体重移動、関節と皮膚の変形、動作の移行、耳・尾・鼻などの二次動作を体格ごとに調整する。

今回はMDの作成・更新。参考作品の歩き方や生成サービスを一律に移植せず、既存TRELLIS制作系統・in-place・サーバー確定・手動移動の仕様を継続する。生成映像と見えない部分の推定を区別し、今後の制作では最終GLBとゲーム内の動きを検証してから採用を記録する。

## 2026-09-09 自動移動と遠征を削除

ユーザー指定でプレイヤーの自動歩行・操船・地面／海面クリック移動・マンモスの追従と自動騎乗、全案内の目的地移動ボタン、遠征を削除。地図は位置と距離の確認用。WASD・矢印・左スティック・画面の移動パッドで自分で移動する。乗れる時に△／Rを案内し、押した時だけサーバーが生存・空席・距離・遮蔽・動作・被弾待ちを再確認。古い目的地コマンドを拒否し、過去の経路では人物・乗り物が動かない。住人・動物・敵のAI、近くで使う異界の入口、回復、ジャンプは継続する。

全359テスト・最終関連20テスト、strict含むビルドと型検査、176 JS構文・依存境界・対象コード書式を確認。実Chromeで地面クリック・遠いR・地図が移動を起こさないこと、各案内、手動接近→△案内→△乗車→手動移動→降車、別画面への同期、再読込、1440×900／390×844／844×390を確認。ブラウザーエラー・目的地コマンド送信0。プロフィール・Gamepad API・近接／湾の開始位置とマンモス待機は準備値。物理コントローラー・物理タッチ端末は未検証。

追加指定「通常の3000番サーバー 保持しなくていい変えていい」に従い、PID20716のEXIHIBITIONの1セッションを切替直前に新しく保存し、持ち物・位置・装備・進行・資源・畑・世帯・住人位置を照合してPID30044へ復元。今回限りの起動は `node output/manual-movement/run-local.mjs`。古いチェックポイントを再利用しない。通常版の21配信モジュール／CSS、準備値なしの通常UI・方向移動・停止・地図・縦横画面・再読込を確認。通常ディスク永続化・展示パッケージ／PC2更新・公開デプロイ0。確認版3013のPID40084、ジャンプ確認PID30592を保持。詳細は `MANUAL_MOVEMENT_PLAN.md`、`assets/manual-movement/README.md`、証拠は `output/playwright/manual-movement-20260909/`、切替記録は `output/manual-movement/rollout.json`。以下の自動移動・遠征・PID20716を保持する記載は過去の履歴。

## 2026-09-09 メニューの上下左右を画面の配置に合わせる

ユーザー指定でメニューの下／右が同じ順送りだった処理を修正。タイトル・旅支度・ガイド・ダイアログの十字キー／左スティック／矢印キー／WASDで、見えている配置の上下左右へ選択する。同じ列／行を優先し、隣の高さの差で下が右へ移らず、端では折り返さない。ラジオ・チェックボックスはカードの位置で判定し、無効・非表示を除外する。選択へのスクロール、右上の戻る、決定4ボタン、選択欄の左右切替、文字入力を保持。操作案内へ上下左右を明示した。

関連22テスト、strict含む分離ビルドと作業ツリー型検査、依存境界・JS構文・対象書式を確認。実Chromeの分離版と通常3000番で1440×900／390×844／844×390のキャラクター選択とポーズメニュー、十字キー・左スティック・矢印、ガイドの往復、4ボタン決定、もちもの・戻る、押しっぱなしの縦移動とスクロール、再読込を確認しブラウザーエラー0。Gamepad APIと各検査の開始フォーカスは模擬で、物理コントローラーは未検証。

通常3000番のメニュークラス・共通方向判定・案内の4配信JSだけを差分反映。通常版の既存ガイド／他機能を保持し、分離中のジャンプ等の一括反映は0。PID20716とジャンプ確認PID30592の開始時刻を保持、サーバー再起動・保存再適用・展示LAN／PC2更新・公開デプロイは0。仕様は assets/gamepad-controls.md、再現は scripts/qa-menu-directions.mjs、証拠は output/playwright/menu-directions-20260909/（通常版はlocal/）、配信記録は output/menu-navigation/local-rollout.json。旧qa-gamepad-menus.mjsの「下の順送りで全項目を選ぶ」は旧仕様の検証手順。

## 2026-09-09 キャラクターのジャンプ

ユーザー指定で全6外見にSpace・画面のジャンプ・Gamepad L2を追加。サーバーが開始を確定し、1m・720msの弧と既存骨格の膝を曲げる姿勢を共有する。歩行・走行・経路移動と水平衝突を継続。着地後100msで再使用、空中連打や押しっぱなしでは連続しない。騎乗・乗船・回復待ち・攻撃動作中は開始しない。跳ぶと釣り・採集作業・調理・交換を中止し、未完成の材料を保持。空中の別作業・攻撃・遠征・交換開始は着地を待つ。メニューを開いても着地し、不要な作業中止通知は出さない。既存の被弾判定を保ち、途中のジャンプは保存・再接続へ持ち越さない。

全354テスト、最終通知調整後の関連28テスト、strictを含む分離ビルド・作業ツリー型検査、171 JS構文・依存境界・対象書式を確認。実Chrome2画面で通常開始、跳躍の同期、Spaceの押しっぱなし、歩行・走行、模擬L2、メニュー・解放待ち・チャットの空白、390×844／844×390のタッチ、空中再読込、全6外見の着地後の攻撃を確認。ブラウザーエラー0。Gamepad入力とプロフィールだけが準備値で、位置・時計・品物の準備値はない。初回全体テストの展示設定ファイル欠落を分離コピーに補い全件再確認した。

新規GLB・依存0。通常3000番PID20716と展示LANのPID39092の開始時刻を保持し、再起動しない。分離プレビューは http://localhost:3012/ （PID30592）、9配信モジュールの一致を検査。通常版・展示パッケージへの反映、公開デプロイ、料金変更、古いチェックポイントの再適用は0。物理コントローラー・物理タッチ端末・展示PC2台でのジャンプは未検証。仕様と再現は `JUMP_PLAN.md`、`assets/jump/README.md`、証拠は `output/playwright/jump-20260909/final/`、起動補助は `output/jump/run-preview.mjs`。

## 2026-09-09 住人の採集から炉の夕食へ

既存ゲーム改善の継続として RESIDENT_FORAGING_PLAN.md を先に作り、全8住人の昼間の日課へベリーの採集と運搬を追加。自宅または訪問中の集落の既存12茂みへ歩き、4m・遮蔽なし・停止で4秒作業すると共有資源1個を減らして持つ。一人一日1個まで、在庫2個以下なら採らない。夕べの日課の炉へ戻ってから夕食へ納入し、従来の順番で食べる。共同食料やプレイヤーの畑・持ち物・採集実績を自動消費せず、資源の既存の再生を継続する。

48個の置き場が満杯なら住人が保持し、翌日・旅先にも運ぶ。納入までは次の採集をしない。会話・移動・日課の切替・出発で途中の作業を中止し、未完成では資源を減らさない。最後に採った日と茂み・運搬中の1個・届けた日と集落を保存し、古い保存は未記録。無人・停止中の日数分をまとめて生産せず、途中の作業時間は保存しない。住人一覧・会話・夕食画面へ記録を接続し、選択やGamepadのフォーカスを保持する。手順・在庫の残し方・時間・量は創作で、古代の生産量や制度の復元ではない。

分離ビルドの全335テスト、strictを含む分離ビルドと現在の作業ツリーの型検査、164 JS構文・依存境界・対象書式が通過。新規11テストで全8人の自宅／訪問先の経路、完了時間、残り2個、遮蔽・中断、満杯から翌日と旅先、8人の通信競合、保存・旧保存・無人を確認。実Chrome2画面＋通信3人の953観測で衝突違反・ブラウザーエラー0。二日分の採集・運搬・納入・食事、満杯と空き、再読込、会話、390×844／844×390、模擬Gamepadを確認。訪問・開始位置・初期日別記録と資源・焼き根47・3回の時計変更は準備値。初日の朝から夕べ、および全ての採集作業・歩行・納入は実時間。

既存39モデル・54GLBのSHAを保持、新規GLB・依存0。並行する EXHIBITION_LAN_PLAN.md が通常3000番のPID20716の停止を禁止しているため、今回は3000番を再起動せず、分離ビルドを3011番・PID47228で非表示起動。通常の遠征と旅支度だけを使う別の実Chromeで、位置・時計・品物・世帯の準備値なしに四集落全8人の採集から食事、共同食料1個の保持、再読込・縦横画面、9配信ファイルの一致を確認。3000番の開始時刻を保持し、古いチェックポイント・通常ディスク永続化・公開デプロイ・料金変更は0。LAN作業の未コミット変更を保持して分離する。

通常3000番で稼働中のサーバーには今回の自動採集はまだ未反映。確認は http://localhost:3011/、起動補助は output/resident-foraging/run-preview.mjs。自動農耕・漁労・狩猟、腐敗・長期栄養収支・人口変化・政治は未実装。物理端末・物理コントローラー・大人数負荷・長時間・常時60FPSは未検証。詳細は assets/resident-foraging/README.md、qa-summary.json、preview-qa.json、preview-host.json、再現は scripts/qa-resident-foraging.mjs。

## 2026-09-09 住人が炉で分け合う夕食

既存ゲーム改善の継続として RESIDENT_SUPPER_PLAN.md を先に作り、四つの共同食料置き場へ住人用の夕食を追加。六食品を手持ちから一つずつ取り分け、住人は夕べに炉へ着くと一人一日一つ食べる。通常の共同食料は自動で取らず、夕食と合計48個まで。未使用の夕食を共同食料へ戻せるが、プレイヤーの受け取りは従来の一日3個に従う。訪問中の世帯は滞在先で参加し、道中・会話・移動中は待つ。集落ごとの人数で順番を日ごとに巡回し、食品の選び方も巡回。食事の記録と8秒の炉での待機を既存の動作・案内へ接続した。食料不足で衰弱・死亡・旅の停止を起こさず、住人の全食生活を表すものではない。

焼いた貝の食事で殻1個を残し、99個まで保存。プレイヤーが一つずつ回収して既存の貝塚へ運べる。殻が満杯なら貝以外を選び、食品も殻も消失させない。距離9m・遮蔽・作業・在庫・容量・二重取得をサーバーで確認。夕食・殻・住人の最後の食事の日／場所／食品を保存し、古い保存では0と未記録を補う。無人・停止中の日数をまとめて消費しない。手順・数値・日課・順番・殻の回収は創作で、古代の配給制度や栄養所要量を復元しない。

全324テストと最終関連21テスト、strictを含む分離／通常ビルド・158 JS構文・依存境界・書式を確認。8人の通信競合、全8住人の夕べの歩行、訪問・帰路、集落ごとの8日間の順番を検証。訪問客が囲む小さな炉の石が中心の炉を遮るため、既存の両方の炉で食事判定を行う。実Chrome2画面＋通信3人の最終235観測で衝突違反・ブラウザーエラー0。6食品の取り分け／返却、8人の食事、殻3個と貝塚、翌日・再読込・390×844／844×390・模擬Gamepadの選択保持／スクロール／戻るを確認。近接位置・食品・訪問状態・午後の仕事場・3回の時計変更は準備値で、夕べの歩行と消費は実時間。初回QAの貝塚ボタンIDを修正して完走し、最後の集落ごとの順番調整後に関連21と全324テストを再確認。

既存39モデル・54GLBのSHAを保持、新規GLB・依存0。通常3000番の部屋0・参加者0、PID18104と開始時刻を反映直前に照合し、PID20716の node server.mjs を非表示起動。古いチェックポイント／保存の再適用は0。通常の遠征と旅支度のベリー3個だけを使い、時計を進めずにアルとセノの夕食、共同食料1個の保持、再読込・住人一覧・縦横画面と8配信ファイルの一致を確認。通常ディスク永続化・公開デプロイ・料金変更は追加しない。

腐敗・住人の自動生産・長期栄養収支・人口変化・政治は未実装。物理端末・物理コントローラー・大人数負荷・長時間・常時60FPSは未検証。詳細は assets/resident-supper/README.md、qa-summary.json、rollout.json、再現は scripts/qa-resident-supper.mjs。

## 2026-09-09 集い場で旅人同士が物々交換

既存ゲーム改善の継続として BARTER_PLAN.md を先に作り、多くの炉の集い場へプレイヤー同士の対面交換を追加。中心22m・互いに4m以内で止まり、誘いと参加の後に一人一種類1〜20個を提示する。黒曜石・未装着の刃・木材・石・食料・種・香草・貝殻の19種類。二人が同じ内容へ同意した時だけ双方の持ち物を変更する。内容が変われば二人の同意を取り消し、同意済みの人は先に取り消してから編集する。国・種族の制限はなく、人間の槍・猫耳の刀・魔法を維持する。

サーバーが交換ID・内容の版・参加者・距離・遮蔽・停止・作業・材料・上限99を検査。古い同意・第三者・二重成立・同時編集を拒否し、同じ品同士は差引後の上限を確認する。品物は成立まで手元に残り、中止・移動・別作業・被弾・切断・画面を離れる操作で取消。誘いから120秒、一人一件、誘い直し5秒、結果表示10秒。未成立の交換は保存せず、成立した持ち物は既存の復帰とexport/importへ含まれる。食料置き場の一日3個や採集実績を変更しない。同意手順・分類・数値・場所は創作で、非在地品をすべて交易の証拠とはしない。

全313テスト、交換11テスト、strictを含む分離／通常ビルド・構文・依存境界・書式を確認。8人4組の同時交換で総量を保存。実Chrome2画面＋通信3人、151観測で衝突違反・ブラウザーエラー0。誘いが相手の画面を開かないこと、双方の提示・変更・同意の取消と成立、受け取った刃の装着、貝の食事と殻、再読込、390×844／844×390、模擬Gamepadの選択・編集保持・スクロール・戻るを検査。交換位置・品物・最初の元気はQA準備値、画面離脱イベントとGamepad API入力は模擬。初回QAの次の招待を出してから旧結果画面を閉じる手順が正しく招待を断っていたため、旧画面を先に閉じる再現手順へ修正して最終確認。

既存39モデル・54GLBのSHAを保持し、新規GLB・依存は0。通常3000番の部屋0・参加者0とPID31372・開始時刻を反映直前に照合して更新し、PID18104の node server.mjs を非表示起動。古いチェックポイントを再利用せず、保存の再適用は0。実Chrome2人の通常の遠征・旅支度のベリーだけで1対2の交換、再読込、縦横画面と6配信モジュールの一致を確認した。通常ディスク永続化・公開デプロイ・料金変更は追加しない。

住人の自動消費・腐敗・長期食料収支・人口変化・政治は未実装。物理端末・物理コントローラー・大人数負荷・長時間・常時60FPSは未検証。詳細は assets/barter/README.md、qa-summary.json、rollout.json、再現は scripts/qa-barter.mjs。

## 2026-09-08 四つの集落で食料を分け合う

既存ゲーム改善の継続として SHARED_PANTRY_PLAN.md を先に作り、三国と集い場の既存の炉へ共同の食料置き場を追加。ベリー・焼き肉・焼き魚・焼いた貝・焼き根・香草焼き根を1つずつ預けて受け取る。各場所は合計48個、初めは空。受け取りは四か所共通で一人一日3個、既存の4分のサーバー暦で更新。所属・種族・寄付歴を問わず利用でき、食事・住人の手伝い・世帯の旅支度・宴という既存の用途を保持する。

数量・空き・日付・距離9m・遮蔽・作業中・所持上限・二重取得をサーバーで検査し、共有在庫と個人の受取日・回数を既存の保存と復帰へ含めた。古い保存には空の置き場と未受取のみを補う。湾の案内から食料を選び、数量・残数・操作できない理由を確認できる。状態配信で選択やコントローラーのフォーカスを失わない。日々の配分・容量・無期限の保存は創作で、古代の倉庫建築や実証された配給制度とはしない。

全302テスト、最終の人物・食料置き場11テスト、strict含むビルド・構文・依存境界・書式を確認。8人の最後の一個と一枠の競合・総量保存を検証。実Chrome2画面＋通信3人、128観測で衝突違反・ブラウザーエラー0。貝を食べて殻を得る、ネリへ渡す、魚を宴へ持ち寄る、三つのベリーを世帯の旅支度へ使う、再読込、390×844／844×390、模擬Gamepadの選択・スクロール・戻るを確認。近接位置・食品・日付・宴の残り材料・木材と水はQA準備値で、採集や長距離の実プレイとは区別する。

既存53GLBのSHAを保持。別タスクで採用された砂耳の魔法使いも含め、保存中の旧モデルを含む39モデル・54GLBを照合し、今回の新規GLB・依存は0。通常3000番のPID13304から直前のEMBERの1セッションを新たに保存し、持ち物・装備・位置・進行・資源・畑・世帯・住人位置の一致を確認してPID31372へ復元。起動は今回限りの node output/shared-pantry/run-local.mjs で、次の更新にこのチェックポイントを再利用しない。6配信モジュールと、通常UI・旅支度のベリーだけを使った共有と再読込を確認。通常ディスク永続化・公開デプロイ・料金変更は追加しない。

住人の自動消費・腐敗・長期食料収支・人口変化・政治は未実装。物理端末・物理コントローラー・大人数負荷・長時間・常時60FPSは未検証。詳細は assets/shared-pantry/README.md、qa-summary.json。別タスクのカメラ・メニュー・通知の変更を保持し、今回のコミットには混ぜない。

## 2026-09-08 低い位置から見上げるカメラ

ユーザー指定で上下角の下限を0.06から−0.5 radへ広げた。マウス／タッチのドラッグも右スティックと同じrotateCameraを使う。地面との最低距離を0.25m、水面との最低距離を0.3mにし、小さい魔法使いも低い位置から見上げられる。左右中央、上限1.05 rad、視点リセット、障害物とのカメラ距離判定を継続。

関連15テスト、strictを含むビルド、変更JSの構文・依存境界・書式を確認。実Chromeの人間／魔法使いでマウス、模擬右スティック、通常メニューからの視点リセット、1440×900／390×844／844×390、低い角度での一周と上限を検証。観測位置で地面との余裕・障害物との視線・左右中央を確認し、ブラウザーエラー0。通常3000番の配信SHAを照合。サーバー再起動や保存の再適用は行わず、物理コントローラーと物理端末は未検証。証拠は `assets/low-angle-camera-qa.json`、`output/playwright/low-angle-20260908/`。

## 2026-09-08 中央カメラと動作ポップアップの整理

ユーザー指定でカメラの右0.75mの注視点オフセットを廃止し、キャラクターの左右中央を追う。攻撃・命中・討伐・被弾・採集・食事・製作・乗降・作業開始／完了／中止と、移動開始などの重複ポップアップを非表示にした。サーバーのnoticeへpopupフラグを追加し、成功通知は既定で非表示、音とゲーム状態の更新を保持。材料不足・距離・通信等の失敗理由と、復帰／保護の残り時間は表示する。被弾の中央テキストも消し、赤い効果と元気ゲージを使う。

関連68テスト、strictを含むビルド、151 JS構文・依存境界・変更コードの書式を確認。実Chromeで人間・クノイチ・魔法使いの1440×900／390×844／844×390、カメラ回転と攻撃を検証。食事・石斧製作・調理完了／中止・採集・経路移動・材料不足・敵からの実被弾を検査し、ブラウザーエラー0。専用部屋の材料と採集／戦闘の近接位置はQA準備値。証拠は `assets/camera-feedback-qa.json`、`output/playwright/camera-feedback-20260908/`。

通常3000番のPID49044から反映直前のEMBERの1セッションを新しく保存し、持ち物・装備・進行・位置・資源・畑・世帯・住人位置を検査してPID13304へ復元。起動は `node output/playwright/camera-feedback-20260908/run-local.mjs` による今回だけの復元であり、次の再起動にこの古いチェックポイントを再利用しない。通常の起動画面から攻撃を確認し、6配信ファイルが一致。通常ディスク永続化と公開デプロイは追加していない。

## 2026-09-08 世帯の旅と集い場での滞在

既存ゲーム改善の継続として、三つの国の住人を二人ずつの創作の世帯にまとめ、国の炉で木材2・ベリー3・水1を渡すと集合して集い場へ歩く仕組みを追加。谷道の世帯（ミラ・トヴァン）、白石の世帯（エナ・ロク）、葦辺の世帯（ネリ・ダロ）。アルとセノは集い場に残る。二人が約12m離れると先行者が待ち、会話・混雑が解消すると再開。日課や季節が変わっても道中の目的地を保持する。

到着から二日（8分）だけ既存の天幕・炉・作業場所で暮らし、同じ道を帰り、一日（4分）休む。各プレイヤーが各訪問に一度、二人のどちらかを迎えて三種の種を各1個もらえる。既存の毎日の手伝いは旅先でも続く。集落一覧・会話に出身と滞在先、地図に現在地と残りの道を表示。世帯、名前、贈り物、周期は創作で、婚姻・血縁・古代の制度を復元したものではない。永住・人口増加・複雑な政治・長期食料収支は未実装。

費用・距離・遮蔽・上限・重複をサーバーで検査。世帯の段階・訪問回数・位置・期限・個人のお迎えを既存の保存と復帰へ含め、旧保存は自宅から開始する。5人を世帯人口の上限とせず、8人のコアで検証。既存38モデル・53配信GLBのSHAを保持し、新規GLB・依存パッケージは0。

全294テスト、最終の住人10テスト、strictを含むビルド、145 JSファイルの構文・依存境界・書式を確認。全三経路の往復と滞在中の全日課を物理シミュレーションで確認。実Chrome2画面＋通信3人、最終341観測で衝突違反・ブラウザーエラー0。通常の旅支度・集合・出発と、準備位置からの最終接近・お迎え・再読込・帰路への出発、390×844／844×390を検査。ブラウザーの最後の経路区間・近接位置・材料・滞在を終える時計変更はQA準備値で、全経路の実ブラウザー歩行とは区別する。会話直後の手伝いが450msの連続操作制限に落ちる点を、会話開始後の500ms待機表示で修正。

通常3000番は直前の部屋0・参加者0とPID7768を照合して更新し、PID49044で非表示起動。8配信モジュールの一致と実Chromeの通常UI、国への歩行開始・地図・小さい画面を確認。古いチェックポイント、通常ディスク永続化、公開デプロイ、料金変更は適用しない。詳細は `HOUSEHOLD_JOURNEYS_PLAN.md`、`assets/household-journeys/README.md`、`qa-summary.json`。

## 2026-09-08 湾の天候・海流と航路案内

ユーザーの「continue」で、先に `WEATHER_CURRENT_PLAN.md` を作成し、既存の湾と丸木舟へ凪・海風・雨風・海霧を追加。部屋の開始時刻を使う180秒周期で共有し、次の空模様も表示する。岸近く・湾奥は影響が弱く、沖は流れに沿うと速く逆らうと遅い。基準4／7m/sの55〜125%に抑え、全状態で前進できる。停止・入力切れ・メニュー・釣り中は漂流せず、横流れ物理は追加しない。天候・海流・予報・性能は創作であり、古気候や潮汐の復元ではない。

「空と航路」の案内、地図の流れの矢印、五つの浜への移動、空・霧・雨を追加。既存384粒子と空の材質を再利用し、新規GLB・依存パッケージ0、38モデル・53配信GLBを保持。海霧の霧の遠端は66m。元の海岸格子、5人の既定上限、24隻の船枠、畑・武器・国・元の大陸を保持する。地域外の固有表現は継続。新しい状態をクライアントから指定せず、船の移動はサーバーで確定する。

検証で見つかった、狭い画面での固定ヘッダー背景の横はみ出しと、大きい位置補正で自分の船まで描画範囲から外れてカメラが止まる問題を修正。全285テスト、strict含むビルド、142 JS構文・依存境界・書式。実Chrome2画面＋通信3人で5人・5隻、731観測、衝突違反・ブラウザーエラー0。対岸への通常操船・上陸、順流・逆流、釣り・予報による停止、天候切替、再接続、390×844／844×390、模擬Gamepadを確認。位置・船・資材・時計の準備値を記録し、実際に採集した証拠と混同しない。

通常3000番のPID19656を確認し、直前の進行を新しく保存・検査してPID7768へ復元。持ち物・進行・位置（乗船中は乗船した岸）・畑・拠点を照合し、10配信モジュールと実Chromeの通常UIを確認。過去のチェックポイント、通常ディスク永続化、公開デプロイ、料金変更は追加しない。物理端末・物理コントローラー・大人数負荷・長時間・常時60FPSは未検証。詳細は `assets/weather-current/README.md` と `qa-summary.json`。世帯移住・複雑な政治・長期食料収支は残る改善。

## 2026-09-08 メニューは右側4ボタンで決定、戻るは画面内のボタン

ユーザー指定で、タイトル・旅支度・ガイド・ダイアログでは ×／○／□／△ のすべてを決定に統一。○によるメニューの取消を廃止し、ウィンドウ右上に文字付きの「戻る」を表示する。旅支度の既存の戻るを使い、ガイドにも旅支度への戻るを追加。ゲーム中の採集・攻撃・乗降・中止と、スティックの傾きによる走行は維持する。複数ボタン同時押しは決定1回、画面をまたぐ押しっぱなしは解放待ち。右上の戻るは説明スクロール中も残し、十字キーで選んだ項目が固定欄の下へ隠れない余白を設けた。

関連17テスト、strictを含むビルド、依存境界・対象コードの書式とQAスクリプトの構文を確認。実Chromeと専用サーバーで、4ボタンそれぞれの開始・旅支度の戻る・もちもの選択・ウィンドウの戻る、ガイド往復、同時押し・押しっぱなし、マウス・キーボード、390×844／844×390を確認。入力だけを模擬し、物理コントローラーは未検証。最終のブラウザーエラー0。通常3000番の5配信ファイルの一致を確認し、サーバー再起動・公開デプロイは行わない。仕様は `assets/gamepad-controls.md`、証拠は `assets/gamepad-menu-qa.json` と `output/playwright/gamepad-menus-20260908/r05/`、再現は `scripts/qa-gamepad-menus.mjs`。

## 2026-09-08 追加作物と炉の料理

ユーザーの「continue」で既存ゲームの農耕を継続。`CROP_EXPANSION_PLAN.md` を先に作り、共同畑48区画へ創作の火根草・香り草を追加した。全四集落でベリー2と各種の種2を交換し、種1を植える。火根草は水2・200〜320秒で火根3と種2、香り草は水1・100〜160秒で香草3と種2。従来のベリーを保持し、国・種族・参加上限で栽培を独占しない。

火根1から焼き根1（元気+35）、火根1・香草1から香草焼き根1（+50）を既存の炉で3秒調理する。どちらも1個を宴の食料3として持ち寄れる。材料・上限99・距離・遮蔽・水・時刻・二重取得はサーバーが確定し、調理中止では食材を保持。古い保存の畑はベリー、新持ち物は0として復元する。作物・種・料理・日数と効果は創作であり、実在作物の考古学的同定ではない。

地上部は既存の低木と草GLBを共有し、草の株の大きさ・2種類の材質で区別。95m以内の作物を描画し、未使用12秒で追加材質を解放する。地中の根や料理の可視モデル、新規GLB・依存パッケージ・可視プリミティブは0。既存38モデル・53配信GLBのバイト列を保持。畑でE／×による作物選択、もちもの・炉の料理画面を追加し、同期後もコントローラーの選択を保持する。

検証で、成長完了が通常90msの送信間隔の途中に起きると更新を失う既存不具合を発見。`gulfChanged` を即時送信条件へ追加し、20ms後に実る再現テストで確認。全272テスト、strict含むビルド、137 JS構文、依存境界・書式が通過。実Chrome2画面＋通信3人の5人、最終717観測で衝突違反・ブラウザーエラー0。種交換・徒歩での水運び・三作物・他人の収穫・再植付け・再読込・調理中止・食事・宴・390×844／844×390・模擬Gamepad選択・材質の解放と再表示を確認。時計、追加ベリー1、食事用の元気、宴の他材料と描画検査の配置は明示的な準備値。

通常3000番の部屋0・参加者0とPID31344を照合して更新し、PID19656で非表示起動。9配信モジュールの一致と、通常UIで火根草の種交換・水運び・植付け・給水・描画を再確認。古いチェックポイント、通常ディスク永続化、公開デプロイ、料金変更は追加しない。詳細は `assets/crop-expansion/README.md`、`qa-summary.json`、`rollout/report.json`。世帯移住、複雑な政治、天候・海流、長期食料収支は残る改善。物理端末・物理コントローラー・大人数負荷・常時60FPSは未検証。

## 2026-09-08 集落の住人・日課・今日の手伝い

ユーザーの「commit then push, then Continue」に従い、釣り・貝塚・黒曜石の槍までを `5a638c0` としてmainへpushしてから継続。`VILLAGE_LIFE_PLAN.md` を先に作り、四集落に各2人、計8人の住人を追加。人間4外見を再利用し、各国にクロマニョン／ネアンデルタールの両方が暮らす。国や種族の専業化、5人を世界人口の上限にする設計はしない。

現行4分の日を朝・昼・夕べ・休息の各60秒に分け、集落の採集場所・畑／作業場・炉・天幕の周りを歩く。近くでE／×による会話、各人一日一回の材料受け渡しとお礼。日課は共有作物・資源を自動消費しない。台詞・慣習・日課・報酬は創作。サーバーが時刻、移動、距離3.2m、遮蔽、材料、上限99、日別の重複受取を確定。会話で最大20秒止まり、相手が離れると戻る。住人は攻撃対象ではない。個人の日別進行と住人の位置を既存の復帰・export/importへ含め、古いデータを補完する。

新規GLB・依存パッケージ・可視プリミティブは0。既存人物プロバイダーのgeometry・texture・9動作を共有。65m以内を描画し、範囲外12秒で個別の骨格・動作・ラベルを解放する。元の人物テンプレートは既存キャッシュに残る。38モデル・53配信GLBのSHAを保持。

全262テスト、最終通知の短縮後の関連11テスト、strict含むビルドを確認。既存遠征テストの準備値が新持ち物を省いていた比較の揺れを修正。実Chrome2画面＋通信3人の5人で全8人の会話・手伝い・個別報酬・翌日・再読込・390×844／844×390を確認。321観測で静物／住人との衝突違反0、描画エラー0。遠方の国への準備位置・材料・時刻進行は通常操作と分けて記録。物理端末・物理コントローラー・大人数負荷・常時60FPSは未検証。

通常3000番の部屋0・参加者0と旧PID27060を照合して更新し、PID31344の `node server.mjs` を非表示起動。専用部屋で7モジュールの配信一致、住人8人、通常導線からの会話と手伝いを確認。旧チェックポイント、Nodeの通常ディスク永続化、公開デプロイ、料金変更は追加しない。詳細は `assets/village-life/README.md`、`qa-summary.json`、`rollout/report.json`。世帯移住・複雑な政治・追加作物・海流／天候は残るロードマップ。

## 2026-09-08 貝塚と黒曜石の槍

ユーザー指定を `COASTAL_CRAFT_PLAN.md` に先に追記して実装。五つの浜で貝採集（2.4秒）、既存の焚き火で調理（3秒）、食事で元気20回復と貝殻1。四集落の指定場所へ殻を最大10個ずつ積み、1／12／40個で共有の貝塚が現れ、成長する。成長先に人物や生き物がいれば納入を待つ。既存八露頭で黒曜石を採掘し、四つの石器作業場で原石2個を4秒削ると刃1個。打ち石の石1個は繰り返し使い、刃1・木材1で木槍に装着する。人間系4種のみ木槍（威力15）で開始し、黒曜石の槍は威力30。ユーザーの回答に従い猫耳の刀・魔法使いの魔法を維持。全員が採集と刃製作に参加できる。

材料・距離・遮蔽物・在庫・装着・攻撃をサーバーで確定。移動・被弾・メニュー・切断で未完成の作業を中止し、完成前には材料を消費しない。新持ち物・装備・貝場・貝塚を既存の復帰とexport/importへ保存し、古いデータの欠けた項目を補完。通年在庫、場所、成長段階、レシピ、秒数と威力は創作。三つの国・農耕・釣り・世界の縮尺は保持する。

貝と貝塚はimagegen→ローカルTRELLIS-2 1024→源形状の軽量化→exact GLB QA。木槍・黒曜石の槍・刃は既存の採用済み石槍から派生。新規配信GLBは5、従来33モデルのSHAを保持し全38モデル・53配信GLBを検証した。静物の不要なリグ、可視プリミティブの代替、失敗時フォールバックは追加しない。既存石槍の欠けた歴史的原画像やdense、旧neanderthal-hunterのadoption-reviewは捏造していない。

全252テスト、strict含むビルド、131 JS構文・依存境界・書式を確認。実Chrome2画面＋通信3人で5人、3,448観測で静物／海岸の違反0、console/pageエラー0。通常UIで貝採集→徒歩持帰り→調理・食事→最初の貝塚、黒曜石採掘→削り・中止→装着。成長確認の殻40個、資源間の移動位置、別の6外見確認の刃・木材はQA準備値として区別。6種の武器・再読込、390×844／844×390を確認。物理端末・実物コントローラー・大人数負荷・長期稼働は未検証。

通常3000番は直前の部屋／参加者0とPID40220を照合して更新し、PID27060の `node server.mjs` を非表示で起動。53GLB配信SHAと通常案内、改善後のスマホ寸法を確認。古いチェックポイント、通常ディスク永続化、公開デプロイ、料金変更は追加しない。詳細は `assets/coastal-craft/README.md` と `assets/coastal-craft/rollout/report.json`。NPCの日課・国固有の暮らし・追加作物・天候／海流は残るロードマップ。

## 2026-09-08 湾の釣り・調理・持ち寄り

ユーザーの「continue」により、拡張済みの既存世界へ釣りを追加。`FISHING_PLAN.md` を先に作成。浜5か所・沖2か所、木材3＋石1の繰り返し使える釣り道具、停止して6秒で生魚1、焚き火で3秒の調理、焼き魚で元気30回復。集い場では焼き魚1を食料3として宴へ持ち寄れる。既存の保存互換用 `stores.berry` を食料数として使い、ベリーも従来どおり使える。岸の在庫6・45秒で1回復、沖の在庫10・30秒で1回復。これらと魚場・習慣は創作。国・畑48区画・黒曜石・世界の広さは保持。

停止・距離・遮蔽・船の占有・在庫・二重取得・所持上限はサーバーで検査。移動・被弾・攻撃・乗降・メニュー・切断で釣りを中止。メニューが状態受信より先に開いても中止要求を送り、その要求では通常行動の待ち時間を更新しない。既存の復帰とexport/importに道具・釣果・魚・魚場の在庫を保存し、進行中の釣りは復元しない。新規GLBと依存パッケージは0。岸では既存の作業動作を使い武器を隠す。魚・糸・漁具の可視メッシュは未追加。

全240テスト、最終関連17テスト、strict・Cloudflareを含むビルド、122 JS構文・依存境界・書式を確認。33モデル・48配信GLBのSHAと構造を検証。実Chrome2画面＋通信3人で岸釣り、約306mの沖への操船と帰還・上陸、調理・食事・寄付、模擬Gamepad入力、釣り中の再読込を検査。3106観測で静物・海岸違反0、console/pageエラー0。開始資材とBの浜への配置は明示的な準備値で、魚と6秒の経過は実プレイ。390×844の狭いボタンと開き直し時のスクロールを修正し、844×390も再確認。最終ビルドを別の実Chromeで検査し、作業姿勢・武器非表示とメニューによる中止を確認（476観測、違反・エラー0）。物理端末・物理コントローラー・大人数負荷・常時60 FPSは未検証。2画面と再読込を含むFPSの疎な観測は27〜60。

通常3000番の旧PID49108は反映直前に部屋0・参加者0を確認して停止し、`node server.mjs`（PID40220）を非表示で起動。旧チェックポイントは再利用していない。実Chromeで釣りの案内と6配信ファイルの最終SHAを確認。通常Nodeのディスク永続保存・公開Cloudflareデプロイ・料金変更は追加していない。計画と仕様は `FISHING_PLAN.md`、`assets/fishing/README.md`、検証は `assets/fishing/qa-summary.json`、再現補助は `scripts/qa-fishing.mjs`。NPC世帯の日課・追加作物・天候海流などは引き続き未実装。

## 2026-09-08 最新：砂漠の動物案を魔法使いへ採用

ユーザーが5候補の2番「砂耳の旅人」を選択。旧垂れ耳の衣装変更は中止し、未採用の途中出力を保持。新しい `desert-fennec-mage` Candidate 1 / revision 04を、選択画像→ローカルTRELLIS-2 1024→形を保つ軽量化→25骨・9動作→exact GLB QAで制作・採用した。大きい立ち耳、キツネの顔、砂色の毛、獣皮と毛皮の服、素手・素足、尾を保持する。103,314三角形・15,370,112 bytes・高さ0.78 m。内部種族 `bear`、魔法・速度・カメラを継続し、尾の余裕として衝突半径を0.32 mへ変更。選択名・肖像は「砂耳の魔法使い」。可視プリミティブや旧モデルのフォールバックは追加しない。

230テスト、33モデルの配信検証、TypeScript・構文・依存境界、ローカル公開ビルド48 GLBを確認。実Chrome＋IABと通信参加者3の5人で魔法・採集・移動・騎乗降車・再読込後の木材保持、390×844を検査。1735通信状態で静物貫通0、6人目拒否。ゲーム観測51〜60 FPSで常時60 FPS・物理スマートフォン・長時間稼働は未検証。動的物体全組や全メッシュの貫通検査ではない。

ブランチ `codex/mage-prehistoric-clothing` の専用ローカル画面へ反映。3000番の手動再起動・Cloudflare公開・課金変更は行っていない。詳細は `assets/desert-fennec-mage-request.json`、`assets/desert-fennec-mage-game-qa.json`。

## 2026-09-08 三つの岸の湾を4倍の面積へ拡張

ユーザーの「4xのworld scale」「continue」により、既存ゲームを拡張。面積4倍＝幅・奥行2倍として、世界の範囲を8192×4096 m（worldVersion 4）、創作の湾を1480×1340 m（gulfVersion 2）へ変更。元の4096×2048 mの地球投影・大陸の座標と人物・建物・舟の縮尺を保持する。外周全体に4倍の完成コンテンツがあるという意味ではない。湾は西へ移動し、6休み場、19岩のランドマーク、追加2か所を含む5舟着き浜、6地点を記録して集い場へ戻る旅路のお礼（木材6・種4、1回のみ）を追加。3国・農耕48区画・黒曜石・宴を保持。上限は既定5人のままで、世界規模とは独立。

海岸は共有2 m格子。地図に湾の全図を追加。最終監査で北東の矩形範囲が元の北米の一角を含み、31本の木と13個の岩を除外していた問題を修正。旧湾の外の元の静物14,685件をすべて照合し、欠落0。全230テスト、strict・Cloudflareを含むビルド、120 JS構文・依存境界・書式を確認。新規GLBは0、33モデル・48配信GLBのSHAと構造を検証。従来のneanderthal-hunterのadoption-review.json欠落は制作履歴の未解決事項として保持。

実Chrome2画面＋通信3人、4,732状態で静物衝突違反0、console・描画エラー0。集い場→谷道→泉の徒歩と記録、約646 mの操船・上陸→東の国への徒歩・所属、植付け→水運び→実時間での成長→収穫、露頭での黒曜石、再読込を確認。他4休み場・戻り位置・船の開始位置と木材12は明示的なQA準備値。390×844・844×390も確認。FPSは16観測中15件が58〜60、再読込直後の1件が8。常時60 FPS・物理端末・大人数負荷は未検証。

通常3000番は初回反映直前に参加者・復帰待ち・部屋が0になっており、その時点の新しい空のチェックポイントを取得して反映。以前に取得した参加者ありのチェックポイントは無視対象のバックアップに保持し、再適用していない。境界修正の反映では新しく作ったSCALE-ROLLOUTのQA参加者1名を新しいチェックポイントで復元し、同じID・位置・持ち物・旅支度を確認。現在PID 49108の `node output/gulf-expansion/run-local.mjs` は今回だけの復元用起動。古いチェックポイントで再起動しない。通常のNodeディスク永続保存、公開Cloudflare更新、料金変更は追加していない。

計画 `WORLD_EXPANSION_PLAN.md`、現在の仕様 `assets/gulf/README.md`、検証 `assets/gulf/expansion/qa-summary.json` と `rollout-qa.json`。再現補助 `scripts/qa-gulf-expansion.mjs`。漁、NPC世帯の移動、複雑な政治・文化、追加作物、天候・海流は未実装であり、世界観案全体を完了扱いにしない。

## 2026-09-08 タイトル画面からのゲーム導線とHUDの再設計

ユーザーの「Webアプリではなくゲームとして自然なUI/UX」に合わせ、起動時の自動接続と左上の常時タイトル・上部ナビ・チップ群を廃止。`src/screens.ts` にタイトル／旅支度／旅のはじめに（操作ガイド）の全画面スクリーン、地名バナー、操作案内バーを追加し、`src/main.ts` でタイトル → はじめる → 旅支度 → 出発 → 接続 → ガイド → HUD の順に進める。再読み込み時は「つづきから」、`?autostart=1` はQAスクリプト用に画面を省略して参加する。ESC / OPTIONS のポーズメニュー（もちもの・地図・手帳・三つの国・部族・あそびかた・設定・タイトルへ戻る）に旧チップの機能を集約し、M / I / J / H / G のショートカットを追加。`src/style.css` は新しいシェルに合わせて書き直し、既存の狩猟・騎乗・船・湾・冒険のUIはIDを維持。ゲームパッドのメニュー操作はダイアログだけでなくスクリーンでも動くよう `GamepadControls` に `menu` / `closeMenu` を追加。5本のQAスクリプトの接続URLへ `autostart=1` を付与。全226テスト、strict含むビルド、構文・依存境界・書式、実Chromeで 1440×900・375×812・844×390 の表示と導線を確認。

## 2026-09-08 PS4コントローラーと傾きによる走行

ユーザー指定でローカル版にDUALSHOCK 4の操作を追加。ブラウザーの標準Gamepad配列を毎フレーム読む。左スティックは浅く倒すと歩行、78%以上で走行、68%未満で歩行へ戻す。16%のデッドゾーン、傾きに応じた速度、斜めの最大速度制限。2回倒し・L3で走る方式にしない。キーボードの走行切替はスティックへ適用しない。右スティックでカメラ、×で採集・決定、□／R2で攻撃、△で船・マンモスに乗降、○で中止、OPTIONSでメニュー。十字キーのメニュー移動・地図の選択欄・右スティックの説明スクロールを追加。名前とチャットはキーボード入力。

切断・画面離脱・文字入力・メニュー・復帰時は停止し、押しっぱなしの入力を次の画面へ持ち越さない。採集・攻撃・移動・乗降・製作は既存のサーバー検査を使う。新規依存パッケージとGLBは0。全221テスト、最終の関連15テスト、strictを含むTypeScriptビルド、構文・依存境界・書式を確認。実Chrome1画面＋通信4人で5人同期、歩行・走行・攻撃・採集・製作・メニュー・マンモス・船・切断・再読込を検証。390×844、844×390の操作説明とスクロールも確認。Gamepad API入力のみ模擬し、実物のPS4コントローラー、USB/Bluetooth実機接続、PS4本体、物理スマートフォンは未検証。

詳細は `assets/gamepad-controls.md`、`assets/gamepad-qa.json`、再現スクリプトは `scripts/qa-gamepad.mjs`。通常3000番で更新5ファイルのSHA一致を確認。サーバー再起動、旧チェックポイント復元、公開デプロイ、料金変更は行っていない。

## 2026-09-08 三つの国・共同の畑・黒曜石

ユーザーの「既存ゲームを改善」「実装開始」「農耕、三つの国、ネアンデルタール人は意図的なファンタジーとして残す」という最新指定を実装。考古学の調査は `WORLD_SETTING_PLAN.md` に保持し、厳密な時代再現を最新要件にしない。太平洋上の予約区画 740×670 m に創作の湾と連続した岸を追加。既存4096×2048 mの大陸・元の陸地セル・区画外の海岸と旧コンテンツを保持。地図・地表・陸と船の衝突は同じ更新済み海岸格子を使う。

長い谷の国、白い尾根の国、葦の岸の国と「多くの炉の集い場」。所属は種族と独立し、各国の炉で選択。48の共同ベリー畑、種、6回分の水袋、植付け→給水→成長→収穫、8か所の黒曜石採集、集い場での交換・持ち寄り・宴を追加。成長は季節により150〜240秒で、暦と農耕は創作。各国に木材・石・野生ベリー・水場がある。近接・遮蔽・費用・上限・二重取得をサーバーで検査し、新しい状態は既存の復帰・export/importへ保存する。Nodeの通常ディスク永続化を追加したものではない。

接続上限は引き続き既定5人。`createGameCore({ playerLimit })` と表示を可変にし、同じ48区画で8人のコアを検証。共有船の上限は24隻へ分離。公開Cloudflareの上限・契約・料金は変更しない。新規GLBは0、既存GLB・geometry・textureを再利用。国の材質、黒曜石の材質、成長段階の低木、石の畑目印を追加。48配信GLB／33モデルのSHAと構造を検査。全制作履歴の検証は既存の `output/model-generation/models/neanderthal-hunter/qa/adoption-review.json` 不在で未完了。

全211テスト通過。実Chrome2画面＋通信3人で5人接続、通常UIで植付け・水運び・収穫・再読込、西の国と露頭への徒歩・所属・黒曜石の採集・持ち帰り交換を検証。別の実Chrome検査で西岸から東岸へ約318 mの操船・上陸・東の国への徒歩・所属と、持ち寄りから宴までのUIを検証。作物待ちはQA用サーバー時刻だけを進め、船の試験開始位置・木材と宴の試験資材は明示的な準備値。これらを実際の採集や徒歩の証拠と混同しない。390×844、844×390も確認。物理スマートフォン、大人数負荷、常時60 FPSは未検証。詳細は `assets/gulf/README.md`、`assets/gulf/qa-summary.json`。

通常3000番は停止中だったため `node server.mjs` を非表示で新規起動（PID 19076）。専用QA部屋の実Chromeで素材読込、湾への遠征、旅支度の受取を確認し、描画・consoleエラー0。旧チェックポイントの適用・公開デプロイは行っていない。分離した3110番QAサーバーは終了。`assets/gulf/rollout-qa.json` を参照。

## 2026-09-08 山・谷・裏世界と探索依頼

ユーザーの世界観・コンテンツ拡張の依頼と、その後のブランチ作成指定により `codex/imp-world-expansion` へ実装。天風の山道、こだまの峡谷、星影の石の森、琥珀のオアシス、蒼氷の回廊、裏世界・影の庭の6地域。探索手帳、18道標、現地採集、番人4体、協力討伐、個別報酬、焚き火の休息を追加。地上の依頼3つを報告すると石の森の裂け目から影の庭へ入り、帰還できる。進行と報酬、移動先をサーバーが確定する。既存の復帰・保存に探索進行を含む。

既存大陸と海岸判定、32モデル・47配信GLBを保持。新規GLBは0。既存TRELLISの山・氷壁・岩・植物・水・敵を共有し、66ランドマーク・48静物・18資源を配置。山は山裾や壁の間を歩く構成で、斜面の自由登山ではない。影の庭は同じワールド内の遠い創作区画に紫の空・暗い地面・浮遊光を付けたもので、別サーバーや地下洞窟物理ではない。地域材質はgeometry・textureを共有し、未使用12秒後に解放。粒子396点。泉は既存水GLBに岸の切り抜きと波の材質を適用。

全6依頼の実プレイ、2つの実Chrome＋通信3人の5人到着、裂け目の往復、再読み込み、390×844／844×390を検証。1,818観測で静物・動的物体の衝突違反0、描画エラー0。報告直後の帰還が共通450 ms制限に拒否される不具合を修正し、裂け目固有の3秒制限を保持。全187テスト中186通過、既存の `output/open-world-restart/run.mjs` 不在による1件ロード失敗。関連24テストと161 JSファイルの構文検査は通過。47 GLBのSHA検証も通過。従来の全制作履歴検証は `output/model-generation/models/neanderthal-hunter/qa/adoption-review.json` 不在で未完了。欠けた履歴を捏造していない。

記録は `assets/adventure/README.md`、`assets/adventure/game-qa/r04/report.json`、`assets/adventure/visual-qa/r03/`、`assets/adventure/qa-summary.json`。観測FPS46〜60、最大69地形区画。物理スマートフォン・長時間・常時60 FPSは未検証。通常3000番の自動反映を確認。手動再起動・古いチェックポイント再適用・公開版へのデプロイ・料金変更は行っていない。

## 2026-09-08 木材で船を作って海を渡る

ユーザー指定で丸木舟をローカル版へ追加。木材12個、海岸で製作、Bで乗降、WASD／海面クリックで操船、Shiftで高速。1隻1人、共有5隻まで。「海岸へ」は近くの造船可能な岸へ走る。費用・海岸・占有・衝突・乗降はサーバーで検証する。切断時は最後の安全な乗船地点へ船と人物を戻し、持ち物を保持。共有船は既存ワールド保存にも含める。公開版へのデプロイ・課金契約変更は今回していない。

`dugout-canoe` Candidate 1 / revision 02をimagegen→ローカルTRELLIS-2→形状を保つ軽量化→exact GLB QAから制作・採用。全長3.8 m、62,145三角形、約4.7 MB。元画像・dense・全revisionを保持。既存31モデルは変更せず、全32モデル・47配信GLBを検証。6種類の既存骨格を座らせ、静物の船に不要なリグや可視プリミティブの代用は追加しない。

実Chrome 2画面＋通信3人の5隻同時操船、対岸への上陸、全6種類の実GLB、モバイル寸法、再読み込みを確認。1,529状態で海岸違反・乗り手の同期不一致0。通常開始地点から造船できる岸への走行も検査。全体176テスト中175通過、既存の無視対象 `output/open-world-restart/run.mjs` 不在で1件ロード失敗。関連14テストと151ファイルの構文検査は通過。記録は `assets/boat/README.md` と `assets/boat/game-qa/r02/report.json`。物理スマートフォン・全海域・長時間連続・常時60 FPSは未検証。通常3000番が停止中だったため新しく開発サーバーを起動した。過去のチェックポイントは再適用していない。

## 2026-09-07 Cloudflare公開と料金条件

ユーザーは「必要なら月額$5の基本料は払ってよいが、従量課金は払ってはいけない」と指定した。超過請求を確実に防ぐため、今回のゲームは新しい専用アカウント `CRO-MAGNON Free` の Workers Free に公開済み。URLは `https://cro-magnon-free.cro-magnon.workers.dev`。既存アカウントの契約は変更せず、基本料の契約も追加していない。課金画面で Workers Free / Active、支払い方法なしを確認した。月額$5の許可を従量課金の許可と解釈しない。PaidへのアップグレードやR2等の従量課金サービスの追加をしない。

Static AssetsとSQLite Durable Objectのみを使う。公開版はEMBERの1部屋・最大5人。無料上限または保存障害でゲームを停止する。共有ワールドと復帰用データを保存し、同じタブのトークンで最大7日間・直近100セッションへ復帰できる。位置は最大30秒前まで戻る場合があり、別端末ログインではない。Node版は既存の2分間メモリ復帰を保持する。

公開URLで5人接続・6人目拒否・移動・攻撃・再接続・全46 GLBのSHA一致・実3D画面と地図を確認済み。SQLiteの再起動復元と上限／保存障害時の停止はローカルエミュレーターで検証。実運用の強制再起動は行っていない。詳細は `CLOUDFLARE_DEPLOYMENT.md` と `assets/cloudflare/`。全体テスト154件通過、過去の無視対象補助ファイル `output/open-world-restart/run.mjs` 不在で既存1件がロード失敗している。

## 2026-09-08 小さい魔法使いを指定画像から交換

ダウンロードの `ChatGPT Image Sep 8, 2026, 12_13_47 AM.png` 左の大きな主役から、`floppy-ear-mage` Candidate 1を制作・採用。元画像→imagegenでAポーズへ整理→ローカルTRELLIS-2 1024→形を保った軽量化→22骨・9動作→exact GLB QAを完了した。垂れ耳、クリーム色の顔、赤と生成りの服、革のかばん・手袋・ブーツを保持。旧こぐまの制作物とGLBは履歴として保存し、可視プリミティブやフォールバックは追加しない。

最終revision 12、68,212三角形・約14.1 MB・高さ0.78 m。耳・袖・かばんのウェイトを修正し、元PBRマップを保持した。内部種族 `bear`、光の魔法、速度、カメラを継続し、選択名と肖像は「垂れ耳の魔法使い」へ更新。

169テスト・全32モデルを確認。実IAB2画面＋通信3人の5人で魔法・採集・移動・騎乗降車・再読込復帰を検証。390×844も確認し、3,105通信状態で静物貫通0。複数描画画面で32〜35 FPS、単独のゲーム画面では60 FPSを観測。常時60 FPS、物理スマートフォン、長時間稼働は保証しない。3000番へ城の別タスクと協調して反映済み。検査用の旧プロフィールは復元せず、通常の永続保存を追加したものではない。詳細は `assets/floppy-ear-mage-request.json`、`assets/floppy-ear-mage-game-qa.json`。

## 2026-09-08 追加：始まりの谷のそばの大きな多層城

ユーザー指定の「カラス人間は始まりの谷のそばの大きい城の中」「階層があり、階段が何個もある城」を実装。`valley-castle` Candidate 1 / revision 06 をimagegen参照画像→ローカルTRELLIS-2 1024→元形状を保つ軽量化と補修→exact GLB QAで制作した。幅約70 m・奥行約65.5 m・高さ約26.1 m、134,354三角形・8.23 MB。既存の白羽の呪術師を上階広間へ移し、元の人物・動物・資源と配信GLBのバイト列を保持した。同時進行の `floppy-ear-mage` への変更は別のユーザー指定として扱う。

始まりの谷の北東 `(140, 0)` に配置。城門の階段、下段・中段・上段テラス、左右の複数階段、上階広間を歩ける。地図の「白羽の大城の城門」「大城の広間の白羽の呪術師」から実際に歩く。塔の頂上や復元メッシュ内部の隠れた板状部分は歩ける部屋ではない。城門と上階の扉を元メッシュに沿って開き、既存の階段・床の三角形を再利用して接続した。可視プリミティブへの置換はない。

配信GLBから0.35 m格子で床を計測し、クライアントとサーバーが同じ高さ・壁・段差判定を使う。入口の6セルだけ隣接する実階段の踏面から段鼻を補間する。経路は城門と各階の踊り場を通り、階の違う相手には刀・槍・魔法・杖が当たらない。カメラは実GLBの全三角形を空間索引で検査する。近隣で城のGLBを読み込み、単一geometry・textureを共有し、未使用後に解放する。未検証の粗いLODは採用しない。

城の組み込み時点で169テスト、32モデル（保管中の元Croを含む）の配信検証を通過。実ブラウザー2＋通信参加者3の5人、390×844で城門→上階→城門の通常操作、刀と魔法による討伐・再出現、倒された人物の持ち物を保持した復帰を確認。29,730通信状態・232,999物体観測で海岸・静物・城壁・動的物体の貫通は0。5人の整列と戦闘の間合いは専用サーバーのstdinで準備し、登城・下城の証拠とは分けた。2つのD3D11/Intel Iris Xe画面で観測45〜60 FPS。物理スマートフォン・長時間連続稼働・常時60 FPSは未検証。`assets/valley-castle-request.json`、`assets/valley-castle-game-qa.json`、`assets/valley-castle-network-qa.json`、`assets/valley-castle-performance-qa.json`を参照。

3000番へ反映済み。反映直前の参加者は同時進行タスクが確認した進行なしのQAのみで、公開状態を保存し、同タスクの復元不要指定に従って再起動した。旧PID 55264だけを停止し、新PID 2064の `node server.mjs` で城GLBのSHAと上階の敵配置を確認。古いチェックポイントの適用や通常の永続保存は追加していない。詳細は `assets/valley-castle-rollout-qa.json`。 元画像・dense・全revision・QAの92ファイルを隣の `threed-model-creation/models/valley-castle/` に排他的に追加保存し、全SHAを照合した。
## 2026-09-07 最新：氷河時代の地球の大陸

ユーザー指定の「ネアンデルタール人の時代の世界の大陸の形を保ち、現実的な広さへ縮小」を実装。広さはユーザーが選択した約4 km × 2 km、実装値は4,096 × 2,048 m、worldVersion 3。約5万年前を基準に、NOAA ETOPO1とSpratt & Lisiecki (2016)の海面 −68.3 mから海岸線の近似を作った。現代地形への一様な海面補正であり、精密な古地理・氷床復元ではない。投影の歪み、小島の省略、気候帯の簡略化は `assets/geography/README.md` に記録する。最終氷期最盛期の約2.1万年前と混同しない。

地図・地表の切り抜き・海面・サーバーの陸地判定は同じ2 m格子を共有する。海は歩けず、13野営地への「遠征」で大陸間を移動できる。同じ大陸の目的地は経路を探して走る。遠征はサーバーが行き先・空き場所・3秒の間隔を検証し、持ち物・採集進行・石斧を保持する。騎乗中や倒れている間は遠征できない。遠征先はゲーム上の配置で、ネアンデルタール人がアメリカ・サフル・南極に住んでいたという意味ではない。左右端のラップ、船、泳ぎはない。

全31モデルの配信GLBを保持し、新規生成・差し替えは0。既存のTRELLIS地面と `river-water` を配置し、材質で海岸を切り抜く。草原・雪・氷・砂漠・火山の地面、雪の材質、火山本体、氷壁、サボテン、玄武岩も共用。サボテン48本は北米に、玄武岩18組は火山帯へ再配置した。静物と採集資源、14か所の焚き火を陸上へ置く。世界全体を読み込まず、32 m区画を半径128 mだけ保持する。元データ、加工条件、SHA、再生成スクリプトを保存した。

162テスト、段階検証時の126 JSファイルの構文検査、全31モデルの検証を通過。実ブラウザー2＋通信参加者3の5人で13野営地を検査し、3,795通信状態・30,327物体観測で海岸違反・静物貫通・動的物体貫通は0。実画面で大陸地図、グリーンランド、砂漠、火山、氷の海岸への22 mの走行、海選択の案内を確認。390×844のアプリ内ブラウザーで地図・雪原への遠征・刀の自由攻撃を確認した。物理スマートフォン、長時間稼働、常時60 FPSは未検証。描画が同時に動くタブ数などでFPSに大きな差が出たため、実測値と条件を `assets/paleo-world-game-qa.json` に残した。

3000番に反映済み。直前の現在の進行を保存し、特定した元の開発サーバーと監視プロセスのみ停止して更新した。元のプロフィールで再接続し、持ち物・採集進行・石斧・位置・拠点資材が一致することを確認した。現在の起動は `node output/open-world-restart/run.mjs 3000 <今回のチェックポイント>`。古いチェックポイントを再利用しない。これは今回の更新時だけの復元で、通常の永続保存ではない。既存の2分間の再接続復帰は保持する。記録は `assets/paleo-world-request.json`、`assets/paleo-world-network-qa.json`、`assets/paleo-world-rollout-qa.json`。下の640 m四方の記載と当時のQAは履歴として保持する。

## 2026-09-07 ゲーム全体の不具合調査と修正

ユーザーの「バグをすべて調べて修正」の指示で、入力・画面・採集・戦闘・狩猟・調理・騎乗・地域移動・通信・素材読込を調査。既存31モデルを保持し、可視モデルの新規制作や差し替えは行っていない。詳細は `assets/bug-audit.md`、実測値は `assets/bug-audit-qa.json`。

チャットや画面離脱時の移動停止、修飾キー・物理WASD、刀の接近距離と死体選択、騎乗中の操作案内、遮蔽物越しの採集・交易・調理、満杯資源の選択、動作の競合、採集目標の誤計数を修正。地形インスタンスの境界球更新、混雑後の全体状態の再送、持ち物・仲間・地図の更新、素材エラー時の操作停止、LANでのSHA-256とテクスチャ共有も修正した。

通信切断と同じタブの再読み込みでは、サーバーが2分間プレイヤーの状態を保持する。空席があれば同じID・持ち物・位置・元気・石斧・採集進行へ復帰する。満員時の再試行も所持品を保持する。切断時の騎乗と調理は解除し、生肉は残す。プロフィールから明示的に参加し直す操作は個人の進行をリセットする。これはサーバーのメモリ内の短時間復帰で、サーバー再起動をまたぐ永続保存ではない。以前の「通常の再接続復帰を追加していない」は当時の履歴として保持する。

152テスト・全31モデル・118ファイルの構文検査を通過。Chromeの実画面2＋通信参加者3の最大5人、6種類の人物、4地域の境界走行、採集・交易・狩猟・調理・騎乗、390×844／844×390／360×640を検査した。28,721通信状態で静物・動的物体の貫通と乗り手の不一致は0。物理スマートフォンと長期連続稼働は未検証。常時60 FPSや不具合が一切ないとは記さない。通常3000番の専用部屋で反映を確認し、手動再起動・古いチェックポイントの再適用は行っていない。

## 2026-09-07 追加指定：地域固有の形の制作

ユーザーは基本モデルの共有方針に加え、各地域に特有の形を持つモデルは一から制作してよいと指定した。`desert-cactus` と `volcanic-basalt-columns` を各Candidate 1として参照画像→ローカルTRELLIS-2→形を保つ軽量化・補修→exact GLB QA→ゲーム組み込みまで完了。既存の氷壁・火山本体・地面・共通モデルと地域材質を保持する。以前の新規20モデルの旧計画を一括再開する指示ではない。

サボテンはrevision 03、約2.4 m・24,435三角形、LODは9,773／3,419。初期の粗いLODと法線転写実験を保持し、元の曲面を多く残した版を採用した。玄武岩はrevision 01、約4.6 m・62,337三角形、LODは15,460を1件だけ採用。1024復元は6 GB GPUのメモリが上限となり停止記録を保持し、512で復元。底面2%の復元ノイズを既存輪郭に沿って補修した。可視プリミティブの代用や不要なリグはない。

砂漠48本・火山18組を道路、野営地、資源、既存静物を避けて固定配置し、既存SCENERYのhashと配信GLBを保持。各種のgeometry・material・textureを共有し、近隣で読み込み、12秒後に未使用素材を解放する。実GLBの全三角形を投影した0.25／0.5 mセルから当たり判定を作成。枝の下を通ることや山登りの物理を追加するものではない。

全31モデルの配信検証と135テスト、実ブラウザー2＋通信参加者3の5人接続、390×844の地図・走行・自由攻撃、素材の解放と再読込を確認した。7,070通信状態で静物・動的物体への貫通0。136点の観測FPSは58〜60で、常時60とは記さない。`assets/region-specific-request.json`、`assets/region-specific-game-qa.json`、`assets/region-specific-network-qa.json`を参照。

元画像・dense・全revision・QAの計452ファイルを隣の `threed-model-creation/models/` の該当2フォルダーへ追加保存し、全SHAを照合。既存ファイルは上書きしない。通常の3000番サーバーで新作と地域処理の配信を確認し、`assets/region-specific-rollout-qa.json` に記録した。3000番は今回の組み込み前に停止していたため開発サーバーを起動した。古いチェックポイントを再適用したものではなく、通常の永続保存を追加するものでもない。

## 2026-09-07 基本モデルを共有する地域表現

ユーザーの最新指定は「基本モデルを使い回す」「雪は元のモデルに乗せる」「メモリを節約する」。各地域の外観を適切にするため、採用済みTRELLISモデルのgeometry・texture・既存LODを同じ参照で共有し、地域用の材質で雪・霜・灰・砂岩色を重ねる。今回の新規採用GLBは0、全29モデルのバイト列を保持する。開始済みの新規雪モデル制作はこの指定を受けて中止し、未採用の参照画像と途中出力を履歴として残した。新規モデル20件の旧計画を再開しない。

雪原は木・岩・木材・低木・テントの屋根・焚き火の外周に積雪表現。氷原・火山・砂漠に針葉樹を配置しない。砂漠は枯れ草と砂岩色、火山は黒い岩と炭化した木材にする。雪は元の面に沿う材質であり、厚みのある別メッシュや追加当たり判定ではない。地域用の材質・インスタンス行列・256pxの実GLB遠景画像には追加メモリを使う。元のメッシュ・テクスチャの複製は0。未使用の地域材質は12秒後に解放する。雪の木では粗い中距離LODの大きな面が目立ったため、14m以遠に詳細GLBから描いた遠景画像を用いる。

127テスト、全29モデルの配信検証、7素材×7方向の比較、実ブラウザー2＋通信参加者3の5人接続、4地域の境界を通常走行で越える操作、390×844の地図・走行・自由攻撃を確認した。8,710通信状態で静物・動的物体への貫通0。記録した130点のFPSは60、常時60や他端末での保証とはしない。最大69地形区画、地域用材質は観測時最大14。`assets/biome-assets-request.json`、`assets/biome-surface-qa.json`、`assets/biome-game-qa.json`、`assets/biome-network-qa.json`を参照。比較画面は `/biome-review.html`。

## 2026-09-07 追加：マンモスへの騎乗

生きているマンモスにR／「近づいて乗る」で乗れる。遠い場合は移動する相手を1.2秒ごとに追い、近づいた時にサーバーへ乗車を要求する。R、WASD、別の目的地・アクション、画面離脱で接近を中止。1頭1人、既存の2頭を利用し、男女4種類・猫耳・こぐまの6種類すべてが乗れる。WASD／クリックで移動し、Shift／走行ボタンで走り、Rで降りる。大きい個体は歩行0.7 m/s・走行2.25 m/s、小さい個体は0.76倍。元の歩行・走行クリップを速度に合わせて再生する。

乗車距離・空席・生存・遮蔽物、移動と降車位置はサーバーが確定する。元のマンモス全体の半径で衝突を処理し、16方向に安全な降車場所を探す。塞がれている間は騎乗を維持する。切断時は占有と移動を解除し、降車後はその場所を中心に巡回する。狭い橋や森ではマンモスが通れないことがあり、見つからない目的地への経路検索を止めて降車を案内する。全地域の野営地へ騎乗したまま行けることは保証しない。

騎乗中は移動用の操作欄を表示し、攻撃・採集・食事は降車後に行う。仲間が乗るマンモスは狩猟対象から外し、敵の杖は高所の乗り手を対象にしない。地上の自由攻撃と狩猟は保持する。

可視モデルの追加・差し替えはない。採用済みTRELLIS GLBのバイト列・骨格・既存クリップを保持し、実行時の着座姿勢をマンモスのSpineへ追従させる。降車時に骨の姿勢を戻す。カメラは9 mへ引き、騎乗の視線を覆う近くの木だけを描画から省く。メッシュ・テクスチャ・AnimationMixerの追加や可視モデルのフォールバックはない。

121テストと全29モデルの配信検証を通過。6種類のスキンで座面追従と降車後の復帰を検査し、実ブラウザー2＋通信参加者3の5人で2頭の占有・歩行・走行・同期を確認した。3,233通信状態で静物／動的物体の貫通0、乗り手の同期不一致0。390×844で自動接近→騎乗→降車→自由攻撃を確認。観測FPSは43〜60で、常時60とは記さない。

記録は `assets/riding-game-qa.json`、`assets/riding-network-qa.json`、`assets/riding-request.json`。3000番の開発サーバーがコード変更で自動更新したため、今回は手動でのサーバー再起動は行っていない。元の名前・種族で画面を再読込し、騎乗を実画面で検証した。更新前の公開状態を記録したが、通常の永続保存・再接続復元を追加したものではない。

## 2026-09-07 最新：五つの地域をつなぐオープンワールド

ユーザー指定の雪・氷・火山・砂漠・草原を、640 × 640 mの有限の共有ワールドへ実装済み。座標範囲は各軸 -256〜384 m。地域の切り替え画面を挟まず、世界地図の地域ボタンで野営地まで実際に走る。元の人物6種類、マンモス、白羽の呪術師、採集・狩猟は保持する。追加4野営地を含む6か所の焚き火で調理できる。

新しい `snow-ground`、`ice-ground`、`desert-ground`、`volcanic-ground`、`glacier-spires`、`volcanic-cone` は、各Candidate 1としてimagegen→ローカルTRELLIS-2→元形状に沿う軽量化・補修→exact GLB QAを完了。雪の上面UV修正と砂の元メッシュをレイで計測した再メッシュを別revisionで保持する。火山の粗いLODは斜面が崩れたため不採用とし、13,382三角形の採用版を2か所で共用する。旧モデルや可視プリミティブへのフォールバックはない。静物に不要なリグは付けない。

32 mの地形区画を半径128 mだけ保持し、確認時は最大69区画。インスタンス描画、距離LOD、2本までの地域読込、1フレーム2区画／3 msまでの追加、12秒後の未使用素材解放、遠方の静物・人物・動作の省略、テクスチャの共有を実装した。雪・灰・砂塵は固定384点のシェーダー。FPS上限60・影15 Hz・画素比上限1.25を継続する。空と霧の色空間も一致させる。

全29モデルの配信検証、110テスト、5人接続（実ブラウザー2＋通信参加者3）、390×844の地図・走行選択・自由攻撃を確認。草原→砂漠、草原→雪原は通常の移動処理で境界を越えた。GLBと足場の6,000点照合は最大約2.2 cm、氷壁・火山は採用GLBの輪郭を1 mセルで計測して当たり判定に使う。近接用の保守的な足形であり、自由な山登り物理ではない。通信検査6,150状態で静物貫通0件、描画エラー0件。実機の観測範囲は43〜60 FPSで、常時60とは記さない。

現在のプレイヤー・持ち物・拠点・経過日数を保存して3000番へ反映済み。これは今回の更新時の復元であり、通常の永続保存・再接続保証を追加したものではない。制作状態は `assets/open-world-request.json`、ゲームQAは `assets/open-world-game-qa.json`、足場QAは `assets/open-world-surface-qa.json`、更新確認は `assets/open-world-rollout-qa.json`。元画像・dense・全revision・QAの計550ファイルを隣の `threed-model-creation/models/` の該当6フォルダーへ追加保存し、全SHAを照合した。既存の制作物は上書きしない。

## 2026-09-07 追加制作：猫耳のクノイチとこぐまの魔法使い

ユーザーは猫耳の女性キャラクター（刀を使うクノイチ）と、小さなかわいいくま風の魔法使いを選択可能にするよう指示した。`cat-kunoichi`、`bear-mage`、装備 `kunoichi-katana` を各Candidate 1として、imagegen参照画像→ローカルTRELLIS-2→形状を保つ軽量化→必要なリグ・9動作→exact GLB QAの工程で制作する。こぐまは独自の外見とする。Claude・可視プリミティブへの置換・モデル読込フォールバックは使わない。

刀は267 msの斬撃、魔法は400 ms後に発射される光弾。Fは対象なしでも発動し、サーバーが命中・遮蔽物・ダメージを決定する。こぐまには体格に合わせた速度とカメラの高さを使う。既存の男女4種類は保持する。制作・採用・ゲームQAを `assets/fantasy-playables-request.json` で区別し、完了を先取りしない。

今回の追加は実装・採用・ゲームQA完了。猫耳はrig revision 06、こぐまはrig revision 08、刀はlow-poly revision 08。全23モデル・97テスト、2つのブラウザーと3つの通信参加者による5人の実画面、390×844での選択・攻撃を確認した。通常ワールドで刀・魔法それぞれ3発による討伐を5人に同期し、静物・動的物体の貫通検査も通過。`assets/fantasy-game-qa.json` と `assets/fantasy-combat-qa.json` を参照。描画は通常59–60 FPS、移動や画面切替時に45–49 FPSの観測もあり、常時60 FPSとは記さない。

## 2026-09-07 追加制作：敵のカラス人間

ユーザーは5つの候補画像から **3番「白羽の呪術師」**（白い冠羽と杖）を選択した。選択画像は `output/imagegen/crow-enemy-candidates-20260907/03-crow-shaman.png`。白い冠羽と頬の羽、黒いカラスの頭・くちばし・羽毛、原始的な獣皮の衣装、石を結んだ木の杖を維持する。`crow-shaman` Candidate 1をローカルTRELLIS-2で制作し、形状を保つ軽量化・リグ・待機／歩行／走行／杖攻撃／被弾／死亡を検証して敵として組み込む。Claude、可視プリミティブへの置換、モデル読込フォールバックは使用しない。

新しい敵の制作と実装は今回の範囲に含まれる。下の2026-09-06の「新しい敵を作らない」という記録は、その時点のFキー修正の範囲を記した履歴であり、今回の選択を制限しない。制作・採用・ゲーム検証の進行は `assets/crow-shaman-request.json` に分けて記録し、未検証の段階で完了と記さない。

今回の1体はCandidate 1 / revision 06を採用し、全20モデル・86テストと5人の実画面検証を完了。杖は450ms・中心距離1.40mで実モデルと接触を照合。復帰後の混雑を避ける経路再探索と60FPS上限も確認した。`assets/crow-game-qa.json`を参照。

## 2026-09-06 最新修正：自由な槍攻撃

ユーザー指定: 「Fはマンモスがいなくても発動できるべき。敵にも使える」。F／5／攻撃ボタンは対象なしで現在の前方に槍を突く。発動と命中を分け、850 msの間隔、333 ms後の前方±55度・射程・遮蔽物で相手1体への命中を判定する。遠い対象・死体・無効なIDで攻撃動作を止めない。近くの有効な相手をクリックした場合のみ向きを補助する。調理中の攻撃は生肉を残して調理を中止する。

共通の攻撃処理は `shared/combat.mjs`。マンモス固有の死亡後の肉処理は `shared/hunting.mjs` に残す。`room.enemies` の `hostile: true` の敵にもダメージと死亡を適用し、味方の人物・交易NPCは攻撃対象に含めない。今回新しい敵の種類やスポーンを作る指示はない。キーボードは物理 `KeyF` も受け付け、文字入力・IME変換・修飾キーの操作は奪わない。

## 2026-09-06 追加：マンモス狩猟・調理

ユーザーは「マンモスは、開けたところに配置して動き回らせる」「槍で攻撃して倒すと肉になって取れます。焚火で焼くと食べられる」と指示した。北西 `(25, 21)` と南西 `(24, 85)` の開けた草原2か所でマンモスを巡回させ、サーバーがHP、攻撃、肉の残数、調理を確定する。

- 今回はマンモスの肉 `mammoth-meat` をTRELLISのimage-to-3Dで追加し、全19モデルの構成にする。男女4種類の人物は `Attack` を含む9動作、マンモスは `Death` を含む5動作。既存の形状・リグ・以前のクリップと制作履歴を保持し、変更版を別revisionとして保存する。
- F／5／「槍で攻撃」で槍を突き出す。動作0.70秒、命中判定は開始333 ms後に距離と遮蔽物を再確認、1発25ダメージ、攻撃間隔850 ms。HP 100のマンモスは4発の命中で倒れる。
- `Death`を1.2秒再生して共有の生肉4個へ変わる。近くで E または肉のクリックで1個ずつ採取し、二重採取を防ぐ。取り切ってから90秒後に再出現する。
- 中央の焚き火から3.4 m以内で生肉を3秒焼く。生肉の消費と焼き肉の追加は完成時に確定する。明示的な中止や火から離れる操作では生肉を失わない。焼き肉を食べると元気45回復。
- 槍は検証済みGLBを手に取り付け、攻撃クリップ中は先端を人物の前方へ向ける。攻撃と死亡はサーバーの時刻へ同期し、途中参加時に古い攻撃を再生しない。肉も検証済みGLBのみを読み込み、フォールバックしない。

今回の狩猟追加は完了。全19モデルの配信検証、48テスト、5人接続での狩猟→採取→調理→食事、390×844の画面操作を確認済み。結果は `assets/hunting-game-qa.json`、`assets/hunting-network-qa.json`、`assets/hunting-mobile-qa.json` に記録した。 以前の女性追加時の全18素材・30テストは履歴として保持する。

## 2026-09-06 追加制作：女性キャラクター

クロマニョン人・ネアンデルタール人の成人女性版をTRELLISで新規制作し、女性を初期選択にする。男性モデルは保持して選択可能にする。ユーザーの外見指定は「もっと女性的な体つき、顔つきで。アイドルみたいな。」、衣装指定は「原始的な人が着る服装にするべきです。」。顔・体型と衣装の両方を参照画像で確定し、旧案を上書きせずrevisionとして保存する。プロフィールに性別を保存し、未設定時は女性にする。生成済みだけで採用・ゲーム検証完了と扱わない。

## 2026-09-06 最新指示：残り全モデルをCodexが制作

ユーザーは「claudeは使わなくていいから、他のすべての3dモデルについても、あなたの方でimage to threed を使って修正してください。」と明示した。Claudeを起動せず、Codexが残りの全可視3Dモデルをimagegen参照画像とローカルTRELLIS-2から制作・修正・必要なrig／animation・検証・ゲーム組み込みまで行う。今回の範囲は `assets/world-models.json` の全素材であり、以前の1体で停止する区切りや1指示1モデルの制限より、この全素材の明示指示を優先する。形状の制作系統、既存候補の保持、性能・外観検証は維持する。

# CRO-MAGNON 制作ルール

## 3Dモデル制作

ユーザー指定: **生き物に限らず、全3DモデルをCodexが参照画像とTRELLISのimage-to-3D方式で制作する。Claudeは使用しない。必要なリギングとアニメーションまで含める。**

モデルを新規作成・交換・大幅修正するときは、先に [ASSET_WORKFLOW.md](ASSET_WORKFLOW.md) を読む。実行方式と品質基準は隣の `../threed-model-creation` リポジトリを参照する。

生き物のリグ・アニメーションを制作・修正するときは、ユーザー指定の参考作品と適用基準をまとめた [CREATURE_MOTION_GUIDE.md](CREATURE_MOTION_GUIDE.md) も読む。

- 対象は人物、動物、植物、岩、地形、建築物、橋、焚き火の構成物、道具、装備、資源など、ゲーム中の全可視3Dモデル。
- 2026-09-06のユーザー指示「claude 停止したので、代わりにあなたがやって」により、現在の制作はCodexが引き継ぐ。仕様整理・TRELLIS-2実行・後処理・必要なリグ／アニメーション・GLB書き出し・検証・ゲーム組み込みを担当し、Claudeの再起動を前提にしない。既存のClaude制作物と制作系統は保持する。
- `参照画像 → TRELLIS-2 dense mesh → 形状を保った軽量化／補修 → UV・材質・構造 → 必要なrig／animation → exact GLB QA` の系統を維持する。
- Three.jsやBlenderのプリミティブ、曲線、Geometry Nodes等で可視モデルを一から組み立てて、TRELLIS由来の完成品の代わりにしない。静物にも同じルールを適用する。
- 2026-09-06の追加指示により、初期のコード生成モデル、旧2D描画、その復元用バックアップを削除する。検証済みGLBだけを読み込み、読み込み失敗時はエラーで描画を止める。旧モデルへのフォールバックを追加しない。
- 変形する生き物には骨格・スキンウェイト・アニメーションクリップを用意する。静物に不要な骨は付けず、可動部分には用途に応じた骨またはノードと動作を用意する。
- カメラ、照明、不可視の当たり判定、配置、LOD、物理、GLB再生、水や火のシェーダー／粒子などの実行時制御はコードで実装できる。これを理由に可視モデルの制作工程を省略しない。
- 今回は全素材の制作が明示されているため、各モデルにつき成功候補1件を作る。完成候補を上書きせず、修正は同じ候補内の別revisionとして保持する。RTX A1000 6 GBではTRELLISとBlenderの重い処理を直列に実行する。
- 実際に配信するGLBの外観・材質・スケール・向き・リグ・クリップを検証し、TPS画面と5人接続で確認する。生成、採用、組み込みの状態を混同しない。
- この方針の記録や「MD更新」だけを根拠に、モデル生成ジョブ、外部公開、SNS投稿を開始しない。

## 狩猟追加前の完了記録

両種族の女性版もCodexがTRELLIS-2から制作し、初期選択を女性に更新済み。女性追加時点で男女4種類、全18素材、5人接続を確認し、30テスト通過。女性追加QAの履歴は `assets/female-game-qa.json` と `assets/female-network-qa.json`。女性モデルは当時各Candidate 1 / final revision 05。参照画像の履歴・不採用案も保持する。顔と体型は成人のアイドル風、衣装は原始的な毛皮・獣皮を採用。男女の能力差はない。

クロマニョン人 Candidate 1 はCodexが胴体ウェイト修正と最終QAを完了し、ゲームへ組み込み済み。既存GLBを保持する。旧Claude batchのfailed・成功0は履歴であり、再開理由にしない。残り15素材もCodexが制作・ゲーム組み込み・5人接続QAを完了。最新の生成・採用・ゲーム検証の状態は `assets/world-models.json` と `assets/README.md` を参照し、配信候補の保存だけでゲーム交換完了と扱わない。
## 2026-09-10 操作注意を抑え、動いたまま肩乗りする

ユーザー指定で、対象がない△／Rや遠い資源への採集など、操作不可時の注意ポップアップを抑制。クライアントの同種通知も削除し、サーバーのアクション応答はpopup:false。検査と成功音、保存・接続障害の通知は保持。肩乗りは双方の停止条件を外し、体の半径の和に加える距離を0.85mから2mへ拡大。移動だけでは誘いを取消さず、承諾時も大猿の手動移動を継続する。距離・壁・状態・期限・正規外見・空席の検査は保持。

全440テスト（新規2）、strict含むビルド、236JS構文・依存境界・対象書式を確認。実Chrome2人で中心間2.68mから双方歩行中の招待・承諾・移動継続と模擬△降車、PCとスマホ縦横を確認。空振りのR／△／E／×／Bで注意0、ブラウザーエラー0。位置・プロフィール・QA対象除外・カメラ・Gamepad APIは準備値、移動と操作は実時間、時計変更なし。

追加承認後、通常3000番PID36244を直前照合して既存終了ハンドラーで最終保存・終了。最新保存からPID26356（2026-09-10 09:03:10）を非表示起動。自動保存正常、5配信一致、通常タイトルを確認。初回デバッグ準備の自動承認拒否は後続明示承認で解決し、接続口の終了も確認。古い保存再適用・展示／PC2更新・公開デプロイなし。詳細はQUIET_ACTIONS_CARRY.md。

## 2026-09-10 地図を全画面にし、ワープ操作を簡素化

ユーザー指定でBotW／TotKの地図画面を参照し、地図を全画面化。後続指定により地図のタイトル・前置き・操作説明・資料の長文を削除。PCは右、縦長は下に地点名と「ここへワープ」を固定し、地図の焚き火選択→決定で移動する。全30地点と名前の選択欄、既存のサーバー条件を保持。地図専用の配色・海岸線、緑のワープ地点と金色の現在地／選択、地名の重なり抑制、密集地点の件数と候補、ドラッグ・ホイール／＋−・現在地へ戻る操作、探索情報の切替を追加。地図選択後のGamepadフォーカスと接続時のボタン更新も接続した。

全454テストと最終関連34（地図3・Gamepadフォーカス1を新規追加）、strict含むビルド、239 JS構文・依存境界を確認。新規Gamepadテストの初回DOMモック不足は補完後成功。実ブラウザーで南アメリカ・帰還・湾へのワープ、密集候補、影の庭の条件、ドラッグ・ホイールと現在地復帰、1440×900／390×844／844×390、再読込を確認。検証中エラー0、位置・元気・持ち物・時計の設定なし。通常3000番の7配信SHA一致、再起動・保存再適用・展示／PC2更新・公開デプロイなし。同時作業のコントローラーボタン変更を保持。物理Gamepadとタッチ端末、長時間負荷は未検証。詳細は MAP_UI_PLAN.md と output/playwright/map-ui/delivery.json。
