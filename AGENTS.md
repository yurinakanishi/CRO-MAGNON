## 2026-09-08 貝塚と黒曜石の槍

ユーザー指定を `COASTAL_CRAFT_PLAN.md` に先に追記して実装。五つの浜で貝採集（2.4秒）、既存の焚き火で調理（3秒）、食事で元気20回復と貝殻1。四集落の指定場所へ殻を最大10個ずつ積み、1／12／40個で共有の貝塚が現れ、成長する。成長先に人物や生き物がいれば納入を待つ。既存八露頭で黒曜石を採掘し、四つの石器作業場で原石2個を4秒削ると刃1個。打ち石の石1個は繰り返し使い、刃1・木材1で木槍に装着する。人間系4種のみ木槍（威力15）で開始し、黒曜石の槍は威力30。ユーザーの回答に従い猫耳の刀・魔法使いの魔法を維持。全員が採集と刃製作に参加できる。

材料・距離・遮蔽物・在庫・装着・攻撃をサーバーで確定。移動・被弾・メニュー・切断で未完成の作業を中止し、完成前には材料を消費しない。新持ち物・装備・貝場・貝塚を既存の復帰とexport/importへ保存し、古いデータの欠けた項目を補完。通年在庫、場所、成長段階、レシピ、秒数と威力は創作。三つの国・農耕・釣り・世界の縮尺は保持する。

貝と貝塚はimagegen→ローカルTRELLIS-2 1024→源形状の軽量化→exact GLB QA。木槍・黒曜石の槍・刃は既存の採用済み石槍から派生。新規配信GLBは5、従来33モデルのSHAを保持し全38モデル・53配信GLBを検証した。静物の不要なリグ、可視プリミティブの代替、失敗時フォールバックは追加しない。既存石槍の欠けた歴史的原画像やdense、旧neanderthal-hunterのadoption-reviewは捏造していない。

全252テスト、strict含むビルド、131 JS構文・依存境界・書式を確認。実Chrome2画面＋通信3人で5人、3,448観測で静物／海岸の違反0、console/pageエラー0。通常UIで貝採集→徒歩持帰り→調理・食事→最初の貝塚、黒曜石採掘→削り・中止→装着。成長確認の殻40個、資源間の移動位置、別の6外見確認の刃・木材はQA準備値として区別。6種の武器・再読込、390×844／844×390を確認。物理端末・実物コントローラー・大人数負荷・長期稼働は未検証。

通常3000番は直前の部屋／参加者0とPID40220を照合して更新し、PID27060の `node server.mjs` を非表示で起動。53GLB配信SHAと通常案内、改善後のスマホ寸法を確認。古いチェックポイント、通常ディスク永続化、公開デプロイ、料金変更は追加しない。詳細は `assets/coastal-craft/README.md` と `assets/coastal-craft/rollout/report.json`。NPCの日課・国固有の暮らし・追加作物・天候／海流は残るロードマップ。

## 2026-09-08 湾の釣り・調理・持ち寄り

ユーザーの「continue」により、拡張済みの既存世界へ釣りを追加。`FISHING_PLAN.md` を先に作成。浜5か所・沖2か所、木材3＋石1の繰り返し使える釣り道具、停止して6秒で生魚1、焚き火で3秒の調理、焼き魚で元気30回復。集い場では焼き魚1を食料3として宴へ持ち寄れる。既存の保存互換用 `stores.berry` を食料数として使い、ベリーも従来どおり使える。岸の在庫6・45秒で1回復、沖の在庫10・30秒で1回復。これらと魚場・習慣は創作。国・畑48区画・黒曜石・世界の広さは保持。

停止・距離・遮蔽・船の占有・在庫・二重取得・所持上限はサーバーで検査。移動・被弾・攻撃・乗降・メニュー・切断で釣りを中止。メニューが状態受信より先に開いても中止要求を送り、その要求では通常行動の待ち時間を更新しない。既存の復帰とexport/importに道具・釣果・魚・魚場の在庫を保存し、進行中の釣りは復元しない。新規GLBと依存パッケージは0。岸では既存の作業動作を使い武器を隠す。魚・糸・漁具の可視メッシュは未追加。

全240テスト、最終関連17テスト、strict・Cloudflareを含むビルド、122 JS構文・依存境界・書式を確認。33モデル・48配信GLBのSHAと構造を検証。実Chrome2画面＋通信3人で岸釣り、約306mの沖への操船と帰還・上陸、調理・食事・寄付、模擬Gamepad入力、釣り中の再読込を検査。3106観測で静物・海岸違反0、console/pageエラー0。開始資材とBの浜への配置は明示的な準備値で、魚と6秒の経過は実プレイ。390×844の狭いボタンと開き直し時のスクロールを修正し、844×390も再確認。最終ビルドを別の実Chromeで検査し、作業姿勢・武器非表示とメニューによる中止を確認（476観測、違反・エラー0）。物理端末・物理コントローラー・大人数負荷・常時60 FPSは未検証。2画面と再読込を含むFPSの疎な観測は27〜60。

通常3000番の旧PID49108は反映直前に部屋0・参加者0を確認して停止し、`node server.mjs`（PID40220）を非表示で起動。旧チェックポイントは再利用していない。実Chromeで釣りの案内と6配信ファイルの最終SHAを確認。通常Nodeのディスク永続保存・公開Cloudflareデプロイ・料金変更は追加していない。計画と仕様は `FISHING_PLAN.md`、`assets/fishing/README.md`、検証は `assets/fishing/qa-summary.json`、再現補助は `scripts/qa-fishing.mjs`。NPC世帯の日課・追加作物・天候海流などは引き続き未実装。

## 2026-09-08 三つの岸の湾を4倍の面積へ拡張

ユーザーの「4xのworld scale」「continue」により、既存ゲームを拡張。面積4倍＝幅・奥行2倍として、世界の範囲を8192×4096 m（worldVersion 4）、創作の湾を1480×1340 m（gulfVersion 2）へ変更。元の4096×2048 mの地球投影・大陸の座標と人物・建物・舟の縮尺を保持する。外周全体に4倍の完成コンテンツがあるという意味ではない。湾は西へ移動し、6休み場、19岩のランドマーク、追加2か所を含む5舟着き浜、6地点を記録して集い場へ戻る旅路のお礼（木材6・種4、1回のみ）を追加。3国・農耕48区画・黒曜石・宴を保持。上限は既定5人のままで、世界規模とは独立。

海岸は共有2 m格子。地図に湾の全図を追加。最終監査で北東の矩形範囲が元の北米の一角を含み、31本の木と13個の岩を除外していた問題を修正。旧湾の外の元の静物14,685件をすべて照合し、欠落0。全230テスト、strict・Cloudflareを含むビルド、120 JS構文・依存境界・書式を確認。新規GLBは0、33モデル・48配信GLBのSHAと構造を検証。従来のneanderthal-hunterのadoption-review.json欠落は制作履歴の未解決事項として保持。

実Chrome2画面＋通信3人、4,732状態で静物衝突違反0、console・描画エラー0。集い場→谷道→泉の徒歩と記録、約646 mの操船・上陸→東の国への徒歩・所属、植付け→水運び→実時間での成長→収穫、露頭での黒曜石、再読込を確認。他4休み場・戻り位置・船の開始位置と木材12は明示的なQA準備値。390×844・844×390も確認。FPSは16観測中15件が58〜60、再読込直後の1件が8。常時60 FPS・物理端末・大人数負荷は未検証。

通常3000番は初回反映直前に参加者・復帰待ち・部屋が0になっており、その時点の新しい空のチェックポイントを取得して反映。以前に取得した参加者ありのチェックポイントは無視対象のバックアップに保持し、再適用していない。境界修正の反映では新しく作ったSCALE-ROLLOUTのQA参加者1名を新しいチェックポイントで復元し、同じID・位置・持ち物・旅支度を確認。現在PID 49108の `node output/gulf-expansion/run-local.mjs` は今回だけの復元用起動。古いチェックポイントで再起動しない。通常のNodeディスク永続保存、公開Cloudflare更新、料金変更は追加していない。

計画 `WORLD_EXPANSION_PLAN.md`、現在の仕様 `assets/gulf/README.md`、検証 `assets/gulf/expansion/qa-summary.json` と `rollout-qa.json`。再現補助 `scripts/qa-gulf-expansion.mjs`。漁、NPC世帯の移動、複雑な政治・文化、追加作物、天候・海流は未実装であり、世界観案全体を完了扱いにしない。

## 2026-09-08 タイトル画面からのゲーム導線とHUDの再設計

ユーザーの「Webアプリではなくゲームとして自然なUI/UX」に合わせ、起動時の自動接続と左上の常時タイトル・上部ナビ・チップ群を廃止。`src/screens.ts` にタイトル／旅支度／旅のはじめに（操作ガイド）の全画面スクリーン、地名バナー、操作案内バーを追加し、`src/main.ts` でタイトル → はじめる → 旅支度 → 出発 → 接続 → ガイド → HUD の順に進める。再読み込み時は「つづきから」、`?autostart=1` はQAスクリプト用に画面を省略して参加する。ESC / OPTIONS のポーズメニュー（もちもの・地図・手帳・三つの国・部族・あそびかた・設定・タイトルへ戻る）に旧チップの機能を集約し、M / I / J / H / G のショートカットを追加。`src/style.css` は新しいシェルに合わせて書き直し、既存の狩猟・騎乗・船・湾・冒険のUIはIDを維持。ゲームパッドのメニュー操作はダイアログだけでなくスクリーンでも動くよう `GamepadControls` に `menu` / `closeMenu` を追加。5本のQAスクリプトの接続URLへ `autostart=1` を付与。全226テスト、strict含むビルド、構文・依存境界・書式、実Chromeで 1440×900・375×812・844×390 の表示と導線を確認。

## 2026-09-08 PS4コントローラーと傾きによる走行

ユーザー指定でローカル版にDUALSHOCK 4の操作を追加。ブラウザーの標準Gamepad配列を毎フレーム読む。左スティックは浅く倒すと歩行、78%以上で走行、68%未満で歩行へ戻す。16%のデッドゾーン、傾きに応じた速度、斜めの最大速度制限。2回倒し・L3で走る方式にしない。キーボードの走行切替はスティックへ適用しない。右スティックでカメラ、×で採集・決定、□／R2で攻撃、△で船・マンモスに乗降、○で中止、OPTIONSでメニュー。十字キーのメニュー移動・地図の選択欄・右スティックの説明スクロールを追加。名前とチャットはキーボード入力。

切断・画面離脱・文字入力・メニュー・復帰時は停止し、押しっぱなしの入力を次の画面へ持ち越さない。採集・攻撃・移動・乗降・製作は既存のサーバー検査を使う。新規依存パッケージとGLBは0。全221テスト、最終の関連15テスト、strictを含むTypeScriptビルド、構文・依存境界・書式を確認。実Chrome1画面＋通信4人で5人同期、歩行・走行・攻撃・採集・製作・メニュー・マンモス・船・切断・再読込を検証。390×844、844×390の操作説明とスクロールも確認。Gamepad API入力のみ模擬し、実物のPS4コントローラー、USB/Bluetooth実機接続、PS4本体、物理スマートフォンは未検証。

詳細は `assets/gamepad-controls.md`、`assets/gamepad-qa.json`、再現スクリプトは `scripts/qa-gamepad.mjs`。通常3000番で更新5ファイルのSHA一致を確認。サーバー再起動、旧チェックポイント復元、公開デプロイ、料金変更は行っていない。

## 2026-09-08 三つの国・共同の畑・黒曜石

ユーザーの「既存ゲームを改善」「実装開始」「農耕、三つの国、ネアンデルタール人は意図的なファンタジーとして残す」という最新指定を実装。考古学の調査は `WORLD_SETTING_PLAN.md` に保持し、厳密な時代再現を最新要件にしない。太平洋上の予約区画 740×670 m に創作の湾と連続した岸を追加。既存4096×2048 mの大陸・元の陸地セル・区画外の海岸と旧コンテンツを保持。地図・地表・陸と船の衝突は同じ更新済み海岸格子を使う。

長い谷の国、白い尾根の国、葦の岸の国と「多くの炉の集い場」。所属は種族と独立し、各国の炉で選択。48の共同ベリー畑、種、6回分の水袋、植付け→給水→成長→収穫、8か所の黒曜石採集、集い場での交換・持ち寄り・宴を追加。成長は季節により150〜240秒で、暦と農耕は創作。各国に木材・石・野生ベリー・水場がある。近接・遮蔽・費用・上限・二重取得をサーバーで検査し、新しい状態は既存の復帰・export/importへ保存する。Nodeの通常ディスク永続化を追加したものではない。

接続上限は引き続き既定5人。`createGameCore({ playerLimit })` と表示を可変にし、同じ48区画で8人のコアを検証。共有船の上限は24隻へ分離。公開Cloudflareの上限・契約・料金は変更しない。新規GLBは0、既存GLB・geometry・textureを再利用。国の材質、黒曜石の材質、成長段階の低木、石の畑目印を追加。48配信GLB／33モデルのSHAと構造を検査。全制作履歴の検証は既存の `output/model-generation/models/neanderthal-hunter/qa/adoption-review.json` 不在で未完了。

全211テスト通過。実Chrome2画面＋通信3人で5人接続、通常UIで植付け・水運び・収穫・再読込、西の国と露頭への徒歩・所属・黒曜石の採集・持ち帰り交換を検証。別の実Chrome検査で西岸から東岸へ約318 mの操船・上陸・東の国への徒歩・所属と、持ち寄りから宴までのUIを検証。作物待ちはQA用サーバー時刻だけを進め、船の試験開始位置・木材と宴の試験資材は明示的な準備値。これらを実際の採集や徒歩の証拠と混同しない。390×844、844×390も確認。物理スマートフォン、大人数負荷、常時60 FPSは未検証。詳細は `assets/gulf/README.md`、`assets/gulf/qa-summary.json`。

通常3000番は停止中だったため `node server.mjs` を非表示で新規起動（PID 19076）。専用QA部屋の実Chromeで素材読込、湾への遠征、旅支度の受取を確認し、描画・consoleエラー0。旧チェックポイントの適用・公開デプロイは行っていない。分離した3110番QAサーバーは終了。`assets/gulf/rollout-qa.json` を参照。

## 2026-09-08 山・谷・裏世界と探索依頼

ユーザーの世界観・コンテンツ拡張の依頼と、その後のブランチ作成指定により `codex/imp-world-expansion` へ実装。天風の山道、こだまの峡谷、星影の石の森、琥珀のオアシス、蒼氷の回廊、裏世界・影の庭の6地域。探索手帳、18道標、現地採集、番人4体、協力討伐、個別報酬、焚き火の休息を追加。地上の依頼3つを報告すると石の森の裂け目から影の庭へ入り、帰還できる。進行と報酬、移動先をサーバーが確定する。既存の復帰・保存に探索進行を含む。

既存大陸と海岸判定、32モデル・47配信GLBを保持。新規GLBは0。既存TRELLISの山・氷壁・岩・植物・水・敵を共有し、66ランドマーク・48静物・18資源を配置。山は山裾や壁の間を歩く構成で、斜面の自由登山ではない。影の庭は同じワールド内の遠い創作区画に紫の空・暗い地面・浮遊光を付けたもので、別サーバーや地下洞窟物理ではない。地域材質はgeometry・textureを共有し、未使用12秒後に解放。粒子396点。泉は既存水GLBに岸の切り抜きと波の材質を適用。

全6依頼の実プレイ、2つの実Chrome＋通信3人の5人到着、裂け目の往復、再読み込み、390×844／844×390を検証。1,818観測で静物・動的物体の衝突違反0、描画エラー0。報告直後の帰還が共通450 ms制限に拒否される不具合を修正し、裂け目固有の3秒制限を保持。全187テスト中186通過、既存の `output/open-world-restart/run.mjs` 不在による1件ロード失敗。関連24テストと161 JSファイルの構文検査は通過。47 GLBのSHA検証も通過。従来の全制作履歴検証は `output/model-generation/models/neanderthal-hunter/qa/adoption-review.json` 不在で未完了。欠けた履歴を捏造していない。

記録は `assets/adventure/README.md`、`assets/adventure/game-qa/r04/report.json`、`assets/adventure/visual-qa/r03/`、`assets/adventure/qa-summary.json`。観測FPS46〜60、最大69地形区画。物理スマートフォン・長時間・常時60 FPSは未検証。通常3000番の自動反映を確認。手動再起動・古いチェックポイント再適用・公開版へのデプロイ・料金変更は行っていない。

## 2026-09-08 木材で船を作って海を渡る

ユーザー指定で丸木舟をローカル版へ追加。木材12個、海岸で製作、Bで乗降、WASD／海面クリックで操船、Shiftで高速。1隻1人、共有5隻まで。「海岸へ」は近くの造船可能な岸へ走る。費用・海岸・占有・衝突・乗降はサーバーで検証する。切断時は最後の安全な乗船地点へ船と人物を戻し、持ち物を保持。共有船は既存ワールド保存にも含める。公開版へのデプロイ・課金契約変更は今回していない。

`dugout-canoe` Candidate 1 / revision 02をimagegen→ローカルTRELLIS-2→形状を保つ軽量化→exact GLB QAから制作・採用。全長3.8 m、62,145三角形、約4.7 MB。元画像・dense・全revisionを保持。既存31モデルは変更せず、全32モデル・47配信GLBを検証。6種類の既存骨格を座らせ、静物の船に不要なリグや可視プリミティブの代用は追加しない。

実Chrome 2画面＋通信3人の5隻同時操船、対岸への上陸、全6種類の実GLB、モバイル寸法、再読み込みを確認。1,529状態で海岸違反・乗り手の同期不一致0。通常開始地点から造船できる岸への走行も検査。全体176テスト中175通過、既存の無視対象 `output/open-world-restart/run.mjs` 不在で1件ロード失敗。関連14テストと151ファイルの構文検査は通過。記録は `assets/boat/README.md` と `assets/boat/game-qa/r02/report.json`。物理スマートフォン・全海域・長時間連続・常時60 FPSは未検証。通常3000番が停止中だったため新しく開発サーバーを起動した。過去のチェックポイントは再適用していない。

## 2026-09-07 Cloudflare公開と料金条件

ユーザーは「必要なら月額$5の基本料は払ってよいが、従量課金は払ってはいけない」と指定した。超過請求を確実に防ぐため、今回のゲームは新しい専用アカウント `CRO-MAGNON Free` の Workers Free に公開済み。URLは `https://cro-magnon-free.cro-magnon.workers.dev`。既存アカウントの契約は変更せず、基本料の契約も追加していない。課金画面で Workers Free / Active、支払い方法なしを確認した。月額$5の許可を従量課金の許可と解釈しない。PaidへのアップグレードやR2等の従量課金サービスの追加をしない。

Static AssetsとSQLite Durable Objectのみを使う。公開版はEMBERの1部屋・最大5人。無料上限または保存障害でゲームを停止する。共有ワールドと復帰用データを保存し、同じタブのトークンで最大7日間・直近100セッションへ復帰できる。位置は最大30秒前まで戻る場合があり、別端末ログインではない。Node版は既存の2分間メモリ復帰を保持する。

公開URLで5人接続・6人目拒否・移動・攻撃・再接続・全46 GLBのSHA一致・実3D画面と地図を確認済み。SQLiteの再起動復元と上限／保存障害時の停止はローカルエミュレーターで検証。実運用の強制再起動は行っていない。詳細は `CLOUDFLARE_DEPLOYMENT.md` と `assets/cloudflare/`。全体テスト154件通過、過去の無視対象補助ファイル `output/open-world-restart/run.mjs` 不在で既存1件がロード失敗している。

## 2026-09-08 小さい魔法使いを指定画像から交換

ダウンロードの `ChatGPT Image Sep 8, 2026, 12_13_47 AM.png` 左の大きな主役から、`floppy-ear-mage` Candidate 1を制作・採用。元画像→imagegenでAポーズへ整理→ローカルTRELLIS-2 1024→形を保った軽量化→22骨・9動作→exact GLB QAを完了した。垂れ耳、クリーム色の顔、赤と生成りの服、革のかばん・手袋・ブーツを保持。旧こぐまの制作物とGLBは履歴として保存し、可視プリミティブやフォールバックは追加しない。

最終revision 12、68,212三角形・約14.1 MB・高さ0.78 m。耳・袖・かばんのウェイトを修正し、元PBRマップを保持した。内部種族 `bear`、光の魔法、速度、カメラを継続し、選択名と肖像は「垂れ耳の魔法使い」へ更新。

169テスト・全32モデルを確認。実IAB2画面＋通信3人の5人で魔法・採集・移動・騎乗降車・再読込復帰を検証。390×844も確認し、3,105通信状態で静物貫通0。複数描画画面で32〜35 FPS、単独のゲーム画面では60 FPSを観測。常時60 FPS、物理スマートフォン、長時間稼働は保証しない。3000番へ城の別タスクと協調して反映済み。検査用の旧プロフィールは復元せず、通常の永続保存を追加したものではない。詳細は `assets/floppy-ear-mage-request.json`、`assets/floppy-ear-mage-game-qa.json`。

## 2026-09-08 追加：始まりの谷のそばの大きな多層城

ユーザー指定の「カラス人間は始まりの谷のそばの大きい城の中」「階層があり、階段が何個もある城」を実装。`valley-castle` Candidate 1 / revision 06 をimagegen参照画像→ローカルTRELLIS-2 1024→元形状を保つ軽量化と補修→exact GLB QAで制作した。幅約70 m・奥行約65.5 m・高さ約26.1 m、134,354三角形・8.23 MB。既存の白羽の呪術師を上階広間へ移し、元の人物・動物・資源と配信GLBのバイト列を保持した。同時進行の `floppy-ear-mage` への変更は別のユーザー指定として扱う。

始まりの谷の北東 `(140, 0)` に配置。城門の階段、下段・中段・上段テラス、左右の複数階段、上階広間を歩ける。地図の「白羽の大城の城門」「大城の広間の白羽の呪術師」から実際に歩く。塔の頂上や復元メッシュ内部の隠れた板状部分は歩ける部屋ではない。城門と上階の扉を元メッシュに沿って開き、既存の階段・床の三角形を再利用して接続した。可視プリミティブへの置換はない。

配信GLBから0.35 m格子で床を計測し、クライアントとサーバーが同じ高さ・壁・段差判定を使う。入口の6セルだけ隣接する実階段の踏面から段鼻を補間する。経路は城門と各階の踊り場を通り、階の違う相手には刀・槍・魔法・杖が当たらない。カメラは実GLBの全三角形を空間索引で検査する。近隣で城のGLBを読み込み、単一geometry・textureを共有し、未使用後に解放する。未検証の粗いLODは採用しない。

城の組み込み時点で169テスト、32モデル（保管中の元Croを含む）の配信検証を通過。実ブラウザー2＋通信参加者3の5人、390×844で城門→上階→城門の通常操作、刀と魔法による討伐・再出現、倒された人物の持ち物を保持した復帰を確認。29,730通信状態・232,999物体観測で海岸・静物・城壁・動的物体の貫通は0。5人の整列と戦闘の間合いは専用サーバーのstdinで準備し、登城・下城の証拠とは分けた。2つのD3D11/Intel Iris Xe画面で観測45〜60 FPS。物理スマートフォン・長時間連続稼働・常時60 FPSは未検証。`assets/valley-castle-request.json`、`assets/valley-castle-game-qa.json`、`assets/valley-castle-network-qa.json`、`assets/valley-castle-performance-qa.json`を参照。

3000番へ反映済み。反映直前の参加者は同時進行タスクが確認した進行なしのQAのみで、公開状態を保存し、同タスクの復元不要指定に従って再起動した。旧PID 55264だけを停止し、新PID 2064の `node server.mjs` で城GLBのSHAと上階の敵配置を確認。古いチェックポイントの適用や通常の永続保存は追加していない。詳細は `assets/valley-castle-rollout-qa.json`。 元画像・dense・全revision・QAの92ファイルを隣の `threed-model-creation/models/valley-castle/` に排他的に追加保存し、全SHAを照合した。
## 2026-09-07 最新：氷河時代の地球の大陸

ユーザー指定の「ネアンデルタール人の時代の世界の大陸の形を保ち、現実的な広さへ縮小」を実装。広さはユーザーが選択した約4 km × 2 km、実装値は4,096 × 2,048 m、worldVersion 3。約5万年前を基準に、NOAA ETOPO1とSpratt & Lisiecki (2016)の海面 −68.3 mから海岸線の近似を作った。現代地形への一様な海面補正であり、精密な古地理・氷床復元ではない。投影の歪み、小島の省略、気候帯の簡略化は `assets/geography/README.md` に記録する。最終氷期最盛期の約2.1万年前と混同しない。

地図・地表の切り抜き・海面・サーバーの陸地判定は同じ2 m格子を共有する。海は歩けず、13野営地への「遠征」で大陸間を移動できる。同じ大陸の目的地は経路を探して走る。遠征はサーバーが行き先・空き場所・3秒の間隔を検証し、持ち物・採集進行・石斧を保持する。騎乗中や倒れている間は遠征できない。遠征先はゲーム上の配置で、ネアンデルタール人がアメリカ・サフル・南極に住んでいたという意味ではない。左右端のラップ、船、泳ぎはない。

全31モデルの配信GLBを保持し、新規生成・差し替えは0。既存のTRELLIS地面と `river-water` を配置し、材質で海岸を切り抜く。草原・雪・氷・砂漠・火山の地面、雪の材質、火山本体、氷壁、サボテン、玄武岩も共用。サボテン48本は北米に、玄武岩18組は火山帯へ再配置した。静物と採集資源、14か所の焚き火を陸上へ置く。世界全体を読み込まず、32 m区画を半径128 mだけ保持する。元データ、加工条件、SHA、再生成スクリプトを保存した。

162テスト、段階検証時の126 JSファイルの構文検査、全31モデルの検証を通過。実ブラウザー2＋通信参加者3の5人で13野営地を検査し、3,795通信状態・30,327物体観測で海岸違反・静物貫通・動的物体貫通は0。実画面で大陸地図、グリーンランド、砂漠、火山、氷の海岸への22 mの走行、海選択の案内を確認。390×844のアプリ内ブラウザーで地図・雪原への遠征・刀の自由攻撃を確認した。物理スマートフォン、長時間稼働、常時60 FPSは未検証。描画が同時に動くタブ数などでFPSに大きな差が出たため、実測値と条件を `assets/paleo-world-game-qa.json` に残した。

3000番に反映済み。直前の現在の進行を保存し、特定した元の開発サーバーと監視プロセスのみ停止して更新した。元のプロフィールで再接続し、持ち物・採集進行・石斧・位置・拠点資材が一致することを確認した。現在の起動は `node output/open-world-restart/run.mjs 3000 <今回のチェックポイント>`。古いチェックポイントを再利用しない。これは今回の更新時だけの復元で、通常の永続保存ではない。既存の2分間の再接続復帰は保持する。記録は `assets/paleo-world-request.json`、`assets/paleo-world-network-qa.json`、`assets/paleo-world-rollout-qa.json`。下の640 m四方の記載と当時のQAは履歴として保持する。

## 2026-09-07 ゲーム全体の不具合調査と修正

ユーザーの「バグをすべて調べて修正」の指示で、入力・画面・採集・戦闘・狩猟・調理・騎乗・地域移動・通信・素材読込を調査。既存31モデルを保持し、可視モデルの新規制作や差し替えは行っていない。詳細は `assets/bug-audit.md`、実測値は `assets/bug-audit-qa.json`。

チャットや画面離脱時の移動停止、修飾キー・物理WASD、刀の接近距離と死体選択、騎乗中の操作案内、遮蔽物越しの採集・交易・調理、満杯資源の選択、動作の競合、採集目標の誤計数を修正。地形インスタンスの境界球更新、混雑後の全体状態の再送、持ち物・仲間・地図の更新、素材エラー時の操作停止、LANでのSHA-256とテクスチャ共有も修正した。

通信切断と同じタブの再読み込みでは、サーバーが2分間プレイヤーの状態を保持する。空席があれば同じID・持ち物・位置・元気・石斧・採集進行へ復帰する。満員時の再試行も所持品を保持する。切断時の騎乗と調理は解除し、生肉は残す。プロフィールから明示的に参加し直す操作は個人の進行をリセットする。これはサーバーのメモリ内の短時間復帰で、サーバー再起動をまたぐ永続保存ではない。以前の「通常の再接続復帰を追加していない」は当時の履歴として保持する。

152テスト・全31モデル・118ファイルの構文検査を通過。Chromeの実画面2＋通信参加者3の最大5人、6種類の人物、4地域の境界走行、採集・交易・狩猟・調理・騎乗、390×844／844×390／360×640を検査した。28,721通信状態で静物・動的物体の貫通と乗り手の不一致は0。物理スマートフォンと長期連続稼働は未検証。常時60 FPSや不具合が一切ないとは記さない。通常3000番の専用部屋で反映を確認し、手動再起動・古いチェックポイントの再適用は行っていない。

## 2026-09-07 追加指定：地域固有の形の制作

ユーザーは基本モデルの共有方針に加え、各地域に特有の形を持つモデルは一から制作してよいと指定した。`desert-cactus` と `volcanic-basalt-columns` を各Candidate 1として参照画像→ローカルTRELLIS-2→形を保つ軽量化・補修→exact GLB QA→ゲーム組み込みまで完了。既存の氷壁・火山本体・地面・共通モデルと地域材質を保持する。以前の新規20モデルの旧計画を一括再開する指示ではない。

サボテンはrevision 03、約2.4 m・24,435三角形、LODは9,773／3,419。初期の粗いLODと法線転写実験を保持し、元の曲面を多く残した版を採用した。玄武岩はrevision 01、約4.6 m・62,337三角形、LODは15,460を1件だけ採用。1024復元は6 GB GPUのメモリが上限となり停止記録を保持し、512で復元。底面2%の復元ノイズを既存輪郭に沿って補修した。可視プリミティブの代用や不要なリグはない。

砂漠48本・火山18組を道路、野営地、資源、既存静物を避けて固定配置し、既存SCENERYのhashと配信GLBを保持。各種のgeometry・material・textureを共有し、近隣で読み込み、12秒後に未使用素材を解放する。実GLBの全三角形を投影した0.25／0.5 mセルから当たり判定を作成。枝の下を通ることや山登りの物理を追加するものではない。

全31モデルの配信検証と135テスト、実ブラウザー2＋通信参加者3の5人接続、390×844の地図・走行・自由攻撃、素材の解放と再読込を確認した。7,070通信状態で静物・動的物体への貫通0。136点の観測FPSは58〜60で、常時60とは記さない。`assets/region-specific-request.json`、`assets/region-specific-game-qa.json`、`assets/region-specific-network-qa.json`を参照。

元画像・dense・全revision・QAの計452ファイルを隣の `threed-model-creation/models/` の該当2フォルダーへ追加保存し、全SHAを照合。既存ファイルは上書きしない。通常の3000番サーバーで新作と地域処理の配信を確認し、`assets/region-specific-rollout-qa.json` に記録した。3000番は今回の組み込み前に停止していたため開発サーバーを起動した。古いチェックポイントを再適用したものではなく、通常の永続保存を追加するものでもない。

## 2026-09-07 基本モデルを共有する地域表現

ユーザーの最新指定は「基本モデルを使い回す」「雪は元のモデルに乗せる」「メモリを節約する」。各地域の外観を適切にするため、採用済みTRELLISモデルのgeometry・texture・既存LODを同じ参照で共有し、地域用の材質で雪・霜・灰・砂岩色を重ねる。今回の新規採用GLBは0、全29モデルのバイト列を保持する。開始済みの新規雪モデル制作はこの指定を受けて中止し、未採用の参照画像と途中出力を履歴として残した。新規モデル20件の旧計画を再開しない。

雪原は木・岩・木材・低木・テントの屋根・焚き火の外周に積雪表現。氷原・火山・砂漠に針葉樹を配置しない。砂漠は枯れ草と砂岩色、火山は黒い岩と炭化した木材にする。雪は元の面に沿う材質であり、厚みのある別メッシュや追加当たり判定ではない。地域用の材質・インスタンス行列・256pxの実GLB遠景画像には追加メモリを使う。元のメッシュ・テクスチャの複製は0。未使用の地域材質は12秒後に解放する。雪の木では粗い中距離LODの大きな面が目立ったため、14m以遠に詳細GLBから描いた遠景画像を用いる。

127テスト、全29モデルの配信検証、7素材×7方向の比較、実ブラウザー2＋通信参加者3の5人接続、4地域の境界を通常走行で越える操作、390×844の地図・走行・自由攻撃を確認した。8,710通信状態で静物・動的物体への貫通0。記録した130点のFPSは60、常時60や他端末での保証とはしない。最大69地形区画、地域用材質は観測時最大14。`assets/biome-assets-request.json`、`assets/biome-surface-qa.json`、`assets/biome-game-qa.json`、`assets/biome-network-qa.json`を参照。比較画面は `/biome-review.html`。

## 2026-09-07 追加：マンモスへの騎乗

生きているマンモスにR／「近づいて乗る」で乗れる。遠い場合は移動する相手を1.2秒ごとに追い、近づいた時にサーバーへ乗車を要求する。R、WASD、別の目的地・アクション、画面離脱で接近を中止。1頭1人、既存の2頭を利用し、男女4種類・猫耳・こぐまの6種類すべてが乗れる。WASD／クリックで移動し、Shift／走行ボタンで走り、Rで降りる。大きい個体は歩行0.7 m/s・走行2.25 m/s、小さい個体は0.76倍。元の歩行・走行クリップを速度に合わせて再生する。

乗車距離・空席・生存・遮蔽物、移動と降車位置はサーバーが確定する。元のマンモス全体の半径で衝突を処理し、16方向に安全な降車場所を探す。塞がれている間は騎乗を維持する。切断時は占有と移動を解除し、降車後はその場所を中心に巡回する。狭い橋や森ではマンモスが通れないことがあり、見つからない目的地への経路検索を止めて降車を案内する。全地域の野営地へ騎乗したまま行けることは保証しない。

騎乗中は移動用の操作欄を表示し、攻撃・採集・食事は降車後に行う。仲間が乗るマンモスは狩猟対象から外し、敵の杖は高所の乗り手を対象にしない。地上の自由攻撃と狩猟は保持する。

可視モデルの追加・差し替えはない。採用済みTRELLIS GLBのバイト列・骨格・既存クリップを保持し、実行時の着座姿勢をマンモスのSpineへ追従させる。降車時に骨の姿勢を戻す。カメラは9 mへ引き、騎乗の視線を覆う近くの木だけを描画から省く。メッシュ・テクスチャ・AnimationMixerの追加や可視モデルのフォールバックはない。

121テストと全29モデルの配信検証を通過。6種類のスキンで座面追従と降車後の復帰を検査し、実ブラウザー2＋通信参加者3の5人で2頭の占有・歩行・走行・同期を確認した。3,233通信状態で静物／動的物体の貫通0、乗り手の同期不一致0。390×844で自動接近→騎乗→降車→自由攻撃を確認。観測FPSは43〜60で、常時60とは記さない。

記録は `assets/riding-game-qa.json`、`assets/riding-network-qa.json`、`assets/riding-request.json`。3000番の開発サーバーがコード変更で自動更新したため、今回は手動でのサーバー再起動は行っていない。元の名前・種族で画面を再読込し、騎乗を実画面で検証した。更新前の公開状態を記録したが、通常の永続保存・再接続復元を追加したものではない。

## 2026-09-07 最新：五つの地域をつなぐオープンワールド

ユーザー指定の雪・氷・火山・砂漠・草原を、640 × 640 mの有限の共有ワールドへ実装済み。座標範囲は各軸 -256〜384 m。地域の切り替え画面を挟まず、世界地図の地域ボタンで野営地まで実際に走る。元の人物6種類、マンモス、白羽の呪術師、採集・狩猟は保持する。追加4野営地を含む6か所の焚き火で調理できる。

新しい `snow-ground`、`ice-ground`、`desert-ground`、`volcanic-ground`、`glacier-spires`、`volcanic-cone` は、各Candidate 1としてimagegen→ローカルTRELLIS-2→元形状に沿う軽量化・補修→exact GLB QAを完了。雪の上面UV修正と砂の元メッシュをレイで計測した再メッシュを別revisionで保持する。火山の粗いLODは斜面が崩れたため不採用とし、13,382三角形の採用版を2か所で共用する。旧モデルや可視プリミティブへのフォールバックはない。静物に不要なリグは付けない。

32 mの地形区画を半径128 mだけ保持し、確認時は最大69区画。インスタンス描画、距離LOD、2本までの地域読込、1フレーム2区画／3 msまでの追加、12秒後の未使用素材解放、遠方の静物・人物・動作の省略、テクスチャの共有を実装した。雪・灰・砂塵は固定384点のシェーダー。FPS上限60・影15 Hz・画素比上限1.25を継続する。空と霧の色空間も一致させる。

全29モデルの配信検証、110テスト、5人接続（実ブラウザー2＋通信参加者3）、390×844の地図・走行選択・自由攻撃を確認。草原→砂漠、草原→雪原は通常の移動処理で境界を越えた。GLBと足場の6,000点照合は最大約2.2 cm、氷壁・火山は採用GLBの輪郭を1 mセルで計測して当たり判定に使う。近接用の保守的な足形であり、自由な山登り物理ではない。通信検査6,150状態で静物貫通0件、描画エラー0件。実機の観測範囲は43〜60 FPSで、常時60とは記さない。

現在のプレイヤー・持ち物・拠点・経過日数を保存して3000番へ反映済み。これは今回の更新時の復元であり、通常の永続保存・再接続保証を追加したものではない。制作状態は `assets/open-world-request.json`、ゲームQAは `assets/open-world-game-qa.json`、足場QAは `assets/open-world-surface-qa.json`、更新確認は `assets/open-world-rollout-qa.json`。元画像・dense・全revision・QAの計550ファイルを隣の `threed-model-creation/models/` の該当6フォルダーへ追加保存し、全SHAを照合した。既存の制作物は上書きしない。

## 2026-09-07 追加制作：猫耳のクノイチとこぐまの魔法使い

ユーザーは猫耳の女性キャラクター（刀を使うクノイチ）と、小さなかわいいくま風の魔法使いを選択可能にするよう指示した。`cat-kunoichi`、`bear-mage`、装備 `kunoichi-katana` を各Candidate 1として、imagegen参照画像→ローカルTRELLIS-2→形状を保つ軽量化→必要なリグ・9動作→exact GLB QAの工程で制作する。こぐまは独自の外見とする。Claude・可視プリミティブへの置換・モデル読込フォールバックは使わない。

刀は267 msの斬撃、魔法は400 ms後に発射される光弾。Fは対象なしでも発動し、サーバーが命中・遮蔽物・ダメージを決定する。こぐまには体格に合わせた速度とカメラの高さを使う。既存の男女4種類は保持する。制作・採用・ゲームQAを `assets/fantasy-playables-request.json` で区別し、完了を先取りしない。

今回の追加は実装・採用・ゲームQA完了。猫耳はrig revision 06、こぐまはrig revision 08、刀はlow-poly revision 08。全23モデル・97テスト、2つのブラウザーと3つの通信参加者による5人の実画面、390×844での選択・攻撃を確認した。通常ワールドで刀・魔法それぞれ3発による討伐を5人に同期し、静物・動的物体の貫通検査も通過。`assets/fantasy-game-qa.json` と `assets/fantasy-combat-qa.json` を参照。描画は通常59–60 FPS、移動や画面切替時に45–49 FPSの観測もあり、常時60 FPSとは記さない。

## 2026-09-07 追加制作：敵のカラス人間

ユーザーは5つの候補画像から **3番「白羽の呪術師」**（白い冠羽と杖）を選択した。選択画像は `output/imagegen/crow-enemy-candidates-20260907/03-crow-shaman.png`。白い冠羽と頬の羽、黒いカラスの頭・くちばし・羽毛、原始的な獣皮の衣装、石を結んだ木の杖を維持する。`crow-shaman` Candidate 1をローカルTRELLIS-2で制作し、形状を保つ軽量化・リグ・待機／歩行／走行／杖攻撃／被弾／死亡を検証して敵として組み込む。Claude、可視プリミティブへの置換、モデル読込フォールバックは使用しない。

新しい敵の制作と実装は今回の範囲に含まれる。下の2026-09-06の「新しい敵を作らない」という記録は、その時点のFキー修正の範囲を記した履歴であり、今回の選択を制限しない。制作・採用・ゲーム検証の進行は `assets/crow-shaman-request.json` に分けて記録し、未検証の段階で完了と記さない。

今回の1体はCandidate 1 / revision 06を採用し、全20モデル・86テストと5人の実画面検証を完了。杖は450ms・中心距離1.40mで実モデルと接触を照合。復帰後の混雑を避ける経路再探索と60FPS上限も確認した。`assets/crow-game-qa.json`を参照。

## 2026-09-06 最新修正：自由な槍攻撃

ユーザー指定: 「Fはマンモスがいなくても発動できるべき。敵にも使える」。F／5／攻撃ボタンは対象なしで現在の前方に槍を突く。発動と命中を分け、850 msの間隔、333 ms後の前方±55度・射程・遮蔽物で相手1体への命中を判定する。遠い対象・死体・無効なIDで攻撃動作を止めない。近くの有効な相手をクリックした場合のみ向きを補助する。調理中の攻撃は生肉を残して調理を中止する。

共通の攻撃処理は `shared/combat.mjs`。マンモス固有の死亡後の肉処理は `shared/hunting.mjs` に残す。`room.enemies` の `hostile: true` の敵にもダメージと死亡を適用し、味方の人物・交易NPCは攻撃対象に含めない。今回新しい敵の種類やスポーンを作る指示はない。キーボードは物理 `KeyF` も受け付け、文字入力・IME変換・修飾キーの操作は奪わない。

## 2026-09-06 追加：マンモス狩猟・調理

ユーザーは「マンモスは、開けたところに配置して動き回らせる」「槍で攻撃して倒すと肉になって取れます。焚火で焼くと食べられる」と指示した。北西 `(25, 21)` と南西 `(24, 85)` の開けた草原2か所でマンモスを巡回させ、サーバーがHP、攻撃、肉の残数、調理を確定する。

- 今回はマンモスの肉 `mammoth-meat` をTRELLISのimage-to-3Dで追加し、全19モデルの構成にする。男女4種類の人物は `Attack` を含む9動作、マンモスは `Death` を含む5動作。既存の形状・リグ・以前のクリップと制作履歴を保持し、変更版を別revisionとして保存する。
- F／5／「槍で攻撃」で槍を突き出す。動作0.70秒、命中判定は開始333 ms後に距離と遮蔽物を再確認、1発25ダメージ、攻撃間隔850 ms。HP 100のマンモスは4発の命中で倒れる。
- `Death`を1.2秒再生して共有の生肉4個へ変わる。近くで E または肉のクリックで1個ずつ採取し、二重採取を防ぐ。取り切ってから90秒後に再出現する。
- 中央の焚き火から3.4 m以内で生肉を3秒焼く。生肉の消費と焼き肉の追加は完成時に確定する。明示的な中止や火から離れる操作では生肉を失わない。焼き肉を食べると元気45回復。
- 槍は検証済みGLBを手に取り付け、攻撃クリップ中は先端を人物の前方へ向ける。攻撃と死亡はサーバーの時刻へ同期し、途中参加時に古い攻撃を再生しない。肉も検証済みGLBのみを読み込み、フォールバックしない。

今回の狩猟追加は完了。全19モデルの配信検証、48テスト、5人接続での狩猟→採取→調理→食事、390×844の画面操作を確認済み。結果は `assets/hunting-game-qa.json`、`assets/hunting-network-qa.json`、`assets/hunting-mobile-qa.json` に記録した。 以前の女性追加時の全18素材・30テストは履歴として保持する。

## 2026-09-06 追加制作：女性キャラクター

クロマニョン人・ネアンデルタール人の成人女性版をTRELLISで新規制作し、女性を初期選択にする。男性モデルは保持して選択可能にする。ユーザーの外見指定は「もっと女性的な体つき、顔つきで。アイドルみたいな。」、衣装指定は「原始的な人が着る服装にするべきです。」。顔・体型と衣装の両方を参照画像で確定し、旧案を上書きせずrevisionとして保存する。プロフィールに性別を保存し、未設定時は女性にする。生成済みだけで採用・ゲーム検証完了と扱わない。

## 2026-09-06 最新指示：残り全モデルをCodexが制作

ユーザーは「claudeは使わなくていいから、他のすべての3dモデルについても、あなたの方でimage to threed を使って修正してください。」と明示した。Claudeを起動せず、Codexが残りの全可視3Dモデルをimagegen参照画像とローカルTRELLIS-2から制作・修正・必要なrig／animation・検証・ゲーム組み込みまで行う。今回の範囲は `assets/world-models.json` の全素材であり、以前の1体で停止する区切りや1指示1モデルの制限より、この全素材の明示指示を優先する。形状の制作系統、既存候補の保持、性能・外観検証は維持する。

# CRO-MAGNON 制作ルール

## 3Dモデル制作

ユーザー指定: **生き物に限らず、全3DモデルをCodexが参照画像とTRELLISのimage-to-3D方式で制作する。Claudeは使用しない。必要なリギングとアニメーションまで含める。**

モデルを新規作成・交換・大幅修正するときは、先に [ASSET_WORKFLOW.md](ASSET_WORKFLOW.md) を読む。実行方式と品質基準は隣の `../threed-model-creation` リポジトリを参照する。

- 対象は人物、動物、植物、岩、地形、建築物、橋、焚き火の構成物、道具、装備、資源など、ゲーム中の全可視3Dモデル。
- 2026-09-06のユーザー指示「claude 停止したので、代わりにあなたがやって」により、現在の制作はCodexが引き継ぐ。仕様整理・TRELLIS-2実行・後処理・必要なリグ／アニメーション・GLB書き出し・検証・ゲーム組み込みを担当し、Claudeの再起動を前提にしない。既存のClaude制作物と制作系統は保持する。
- `参照画像 → TRELLIS-2 dense mesh → 形状を保った軽量化／補修 → UV・材質・構造 → 必要なrig／animation → exact GLB QA` の系統を維持する。
- Three.jsやBlenderのプリミティブ、曲線、Geometry Nodes等で可視モデルを一から組み立てて、TRELLIS由来の完成品の代わりにしない。静物にも同じルールを適用する。
- 2026-09-06の追加指示により、初期のコード生成モデル、旧2D描画、その復元用バックアップを削除する。検証済みGLBだけを読み込み、読み込み失敗時はエラーで描画を止める。旧モデルへのフォールバックを追加しない。
- 変形する生き物には骨格・スキンウェイト・アニメーションクリップを用意する。静物に不要な骨は付けず、可動部分には用途に応じた骨またはノードと動作を用意する。
- カメラ、照明、不可視の当たり判定、配置、LOD、物理、GLB再生、水や火のシェーダー／粒子などの実行時制御はコードで実装できる。これを理由に可視モデルの制作工程を省略しない。
- 今回は全素材の制作が明示されているため、各モデルにつき成功候補1件を作る。完成候補を上書きせず、修正は同じ候補内の別revisionとして保持する。RTX A1000 6 GBではTRELLISとBlenderの重い処理を直列に実行する。
- 実際に配信するGLBの外観・材質・スケール・向き・リグ・クリップを検証し、TPS画面と5人接続で確認する。生成、採用、組み込みの状態を混同しない。
- この方針の記録や「MD更新」だけを根拠に、モデル生成ジョブ、外部公開、SNS投稿を開始しない。

## 狩猟追加前の完了記録

両種族の女性版もCodexがTRELLIS-2から制作し、初期選択を女性に更新済み。女性追加時点で男女4種類、全18素材、5人接続を確認し、30テスト通過。女性追加QAの履歴は `assets/female-game-qa.json` と `assets/female-network-qa.json`。女性モデルは当時各Candidate 1 / final revision 05。参照画像の履歴・不採用案も保持する。顔と体型は成人のアイドル風、衣装は原始的な毛皮・獣皮を採用。男女の能力差はない。

クロマニョン人 Candidate 1 はCodexが胴体ウェイト修正と最終QAを完了し、ゲームへ組み込み済み。既存GLBを保持する。旧Claude batchのfailed・成功0は履歴であり、再開理由にしない。残り15素材もCodexが制作・ゲーム組み込み・5人接続QAを完了。最新の生成・採用・ゲーム検証の状態は `assets/world-models.json` と `assets/README.md` を参照し、配信候補の保存だけでゲーム交換完了と扱わない。
